# 第三十章. 生产构建、测试与可观测性

## 30.1 生产构建、一键即起与 Nginx

### 30.1.1 本节目标

​        学完本节后，你将能够：

​        换句话说，第一，理解为什么项目需要一键启动和停止脚本；第二，让 API 容器启动时自动执行数据库迁移；第三，使用 Docker Compose 健康检查控制服务启动顺序；第四，为 Nginx 补齐 SSE、WebSocket 和上传文件静态路径配置；第五，区分本地开发命令和 Compose 生产化启动命令；第六，使用轻量测试检查生产启动配置是否完整。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 30.1.2 最终效果

​        本节结束后，项目根目录会新增：

```Plain
scripts/start.sh
scripts/stop.sh
```

​        启动服务：

```Bash
./scripts/start.sh
```

​        如果需要重新构建镜像：

```Bash
BUILD=true ./scripts/start.sh
```

​        停止服务：

```Bash
./scripts/stop.sh
```

​        API 容器启动时会自动执行：

```Bash
uv run alembic upgrade head
```

​        Nginx 会继续作为统一入口：

```Plain
http://localhost:8088
```

​        并支持：

```Plain
/                -> UI
/api             -> API
/sandbox-api     -> Sandbox API
/sandbox-vnc     -> VNC WebSocket
/uploads         -> 上传文件静态访问预留
```

### 30.1.3 本节要解决的问题

​        到第 29 章为止，项目已经有很多服务：

```Plain
PostgreSQL
Redis
API
UI
Sandbox
Nginx
```

​        如果每次启动都靠手动记命令，就会出现几个问题：

​        从实现顺序看，第一，忘记执行数据库迁移；第二，启动顺序不稳定，Nginx 先起来但 API 还没健康；第三，SSE 流式响应被 Nginx 缓冲，前端看起来像“一次性返回”；第四，VNC WebSocket 需要升级请求头，普通 HTTP 代理不够；第五，Docker Compose 命令越来越长，不适合最终项目验收。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本节把“能跑”升级成“更像一个可交付项目的启动方式”。

### 30.1.4 本节技术方案

​        本节新增三类能力：

```Plain
启动脚本
  |
  +-- scripts/start.sh
  +-- scripts/stop.sh

API 容器启动脚本
  |
  +-- backend/api/scripts/start.sh
  +-- 先迁移，再启动 uvicorn

网关和 Compose 增强
  |
  +-- Nginx SSE / WebSocket / uploads
  +-- Compose healthcheck
  +-- depends_on condition: service_healthy
```

​        本节暂时不做：

​        放到工程语境里看，第一，不配置 HTTPS 证书；第二，不做生产域名；第三，不做容器日志采集；第四，不做蓝绿部署；第五，不做 CI/CD。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些内容会在后续测试、可观测性、安全和验收章节继续增强。

### 30.1.5 新增和修改的文件

```Plain
.env.example
README.md
backend/api/README.md
backend/api/Dockerfile
backend/api/scripts/start.sh
docker-compose.yml
nginx/README.md
nginx/default.conf
scripts/start.sh
scripts/stop.sh
tests/test_production_startup_config.py
docs/course/chapters/46-production-startup-nginx.md
```

### 30.1.6 实施步骤
#### 30.1.6.1 先写生产启动配置测试

​        创建：

```Plain
tests/test_production_startup_config.py
```

​        完整代码如下：

```Python
from pathlib import Path
import unittest

ROOT = Path(__file__).resolve().parents[1]

class ProductionStartupConfigTest(unittest.TestCase):
    def test_root_start_stop_scripts_exist_and_use_compose(self) -> None:
        start_script = ROOT / "scripts" / "start.sh"
        stop_script = ROOT / "scripts" / "stop.sh"

        self.assertTrue(start_script.exists())
        self.assertTrue(stop_script.exists())
        self.assertIn("docker compose up -d", start_script.read_text())
        self.assertIn("docker compose down", stop_script.read_text())

    def test_api_container_runs_start_script_with_migrations(self) -> None:
        dockerfile = (ROOT / "backend" / "api" / "Dockerfile").read_text()
        start_script = (ROOT / "backend" / "api" / "scripts" / "start.sh").read_text()

        self.assertIn("COPY scripts ./scripts", dockerfile)
        self.assertIn('CMD ["./scripts/start.sh"]', dockerfile)
        self.assertIn("alembic upgrade head", start_script)
        self.assertIn("uvicorn app.main:app", start_script)

    def test_nginx_has_stream_websocket_and_upload_rules(self) -> None:
        config = (ROOT / "nginx" / "default.conf").read_text()

        self.assertIn("proxy_read_timeout", config)
        self.assertIn("X-Accel-Buffering", config)
        self.assertIn("location /sandbox-vnc/", config)
        self.assertIn("proxy_set_header Upgrade $http_upgrade", config)
        self.assertIn("location /uploads/", config)

if __name__ == "__main__":
    unittest.main()
```

.1.6.1.1 代码讲解

​        这个测试不启动 Docker，也不访问网络。

​        它只检查生产启动最容易遗漏的配置：

​        展开来看，第一，根目录是否有启动和停止脚本；第二，API 容器是否使用启动脚本；第三，API 启动脚本是否会执行迁移；第四，Nginx 是否有流式响应、WebSocket 和上传静态路径配置。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这种测试很轻，但很实用。以后如果有人误删脚本或改坏 Nginx 配置，测试会直接失败。

#### 30.1.6.2 新增根目录启动脚本

​        创建：

```Plain
scripts/start.sh
```

​        完整代码如下：

```Bash
#!/usr/bin/env bash
set -euo pipefail

# ===================== 第1步：进入项目根目录 =====================
# 这样无论用户从哪个目录执行脚本，docker compose 都能读到正确的配置文件。
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"

# ===================== 第2步：按需选择是否重新构建镜像 =====================
# BUILD=true ./scripts/start.sh 会在启动前重新构建 API、UI 和 Sandbox。
if [[ "${BUILD:-false}" == "true" ]]; then
  docker compose up -d --build
else
  docker compose up -d
fi

# ===================== 第3步：展示服务状态，方便用户确认入口是否可用 =====================
docker compose ps

GATEWAY_PORT="${NGINX_PORT:-8088}"

cat <<EOF

AtlasAgent is starting.

Gateway:
  http://localhost:${GATEWAY_PORT}

Useful checks:
  curl http://localhost:${GATEWAY_PORT}/api/status
  curl http://localhost:${GATEWAY_PORT}/api/status/database
EOF
```

​        执行：

```Bash
chmod +x scripts/start.sh
```

.1.6.2.1 代码讲解

​        `set -euo pipefail` 用来让脚本遇到错误时尽快退出。

​        `ROOT_DIR` 让脚本可以从任意目录执行。

​        `BUILD=true` 是一个轻量开关：

```Bash
BUILD=true ./scripts/start.sh
```

​        表示启动前先重新构建镜像。

​        如果只是普通启动：

```Bash
./scripts/start.sh
```

​        脚本会直接复用已有镜像和容器。

#### 30.1.6.3 新增停止脚本

​        创建：

```Plain
scripts/stop.sh
```

​        完整代码如下：

```Bash
#!/usr/bin/env bash
set -euo pipefail

# ===================== 第1步：进入项目根目录 =====================
ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "${ROOT_DIR}"

# ===================== 第2步：停止 Compose 服务 =====================
# CLEAN_VOLUMES=true ./scripts/stop.sh 会同时删除数据库、Redis 和上传文件卷。
if [[ "${CLEAN_VOLUMES:-false}" == "true" ]]; then
  docker compose down -v
else
  docker compose down
fi
```

​        执行：

```Bash
chmod +x scripts/stop.sh
```

.1.6.3.1 代码讲解

​        默认停止服务时不删除数据卷：

```Bash
./scripts/stop.sh
```

​        这样 PostgreSQL、Redis、上传文件还会保留。

​        如果确实想清空本地数据，可以执行：

```Bash
CLEAN_VOLUMES=true ./scripts/stop.sh
```

​        这个命令会删除数据卷，下一次启动会得到一个更干净的环境。

#### 30.1.6.4 新增 API 容器启动脚本

​        创建：

```Plain
backend/api/scripts/start.sh
```

​        完整代码如下：

```Bash
#!/usr/bin/env bash
set -euo pipefail

# ===================== 第1步：按需执行数据库迁移 =====================
# 容器启动时先执行迁移，避免用户忘记手动运行 alembic upgrade head。
# 本地排查时可以通过 RUN_MIGRATIONS=false 临时跳过。
if [[ "${RUN_MIGRATIONS:-true}" == "true" ]]; then
  uv run alembic upgrade head
fi

# ===================== 第2步：启动 FastAPI 服务 =====================
# 使用 exec 让 uvicorn 成为容器主进程，Docker 停止容器时信号能正确传递。
exec uv run uvicorn app.main:app --host 0.0.0.0 --port 8000
```

​        执行：

```Bash
chmod +x backend/api/scripts/start.sh
```

.1.6.4.1 代码讲解

​        以前启动 API 容器时，容器只做一件事：

```Plain
启动 uvicorn
```

​        这会带来一个问题：如果数据库迁移没有执行，新接口访问到新表时会报错。

​        现在容器启动流程变成：

```Plain
执行 alembic upgrade head
  |
  v
启动 uvicorn
```

​        `RUN_MIGRATIONS=false` 是排查开关。

​        例如你只想临时验证 API 代码，不想让容器碰数据库迁移：

```Bash
RUN_MIGRATIONS=false docker compose up -d api
```

#### 30.1.6.5 更新 API Dockerfile

​        打开：

```Plain
backend/api/Dockerfile
```

​        加入脚本复制和执行权限：

```Dockerfile
COPY scripts ./scripts
RUN chmod +x ./scripts/start.sh
```

​        最后把 `CMD` 改成：

```Dockerfile
CMD ["./scripts/start.sh"]
```

.1.6.5.1 为什么这样设计

​        把迁移逻辑放进 Dockerfile 的 `RUN` 阶段是不对的。

​        因为镜像构建时还没有连接运行中的 PostgreSQL。

​        迁移应该发生在容器启动时，也就是 `CMD` 执行的阶段。

​        所以这里使用：

```Plain
Dockerfile CMD -> backend/api/scripts/start.sh -> alembic -> uvicorn
```

#### 30.1.6.6 更新 Docker Compose 健康检查和依赖

​        打开：

```Plain
docker-compose.yml
```

​        在 API 环境变量中加入：

```YAML
RUN_MIGRATIONS: ${RUN_MIGRATIONS:-true}
```

​        API 依赖 PostgreSQL 和 Redis 健康：

```YAML
depends_on:
  postgres:
    condition: service_healthy
  redis:
    condition: service_healthy
```

​        UI 等 API 健康后再启动：

```YAML
depends_on:
  api:
    condition: service_healthy
```

​        Nginx 等 UI、API、Sandbox 健康后再启动：

```YAML
depends_on:
  ui:
    condition: service_healthy
  api:
    condition: service_healthy
  sandbox:
    condition: service_healthy
```

.1.6.6.1 代码讲解

​        `depends_on` 的普通写法只保证“创建顺序”，不保证服务已经可用。

​        本节改成：

```Plain
condition: service_healthy
```

​        这样启动链路更稳：

```Plain
postgres / redis healthy
  |
  v
api healthy
  |
  v
ui healthy
  |
  v
nginx healthy
```

​        这不是生产编排的全部能力，但已经比“盲目按顺序启动”可靠很多。

#### 30.1.6.7 增强 Nginx 配置

​        打开：

```Plain
nginx/default.conf
```

​        在 server 中加入长连接超时：

```Nginx
proxy_read_timeout 3600s;
proxy_send_timeout 3600s;
```

​        在 `/api/` 和 `/sandbox-api/` 中关闭缓冲：

```Nginx
proxy_buffering off;
proxy_cache off;
proxy_set_header X-Accel-Buffering no;
```

​        保留 VNC WebSocket 升级头：

```Nginx
location /sandbox-vnc/ {
    proxy_buffering off;
    proxy_cache off;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_pass http://sandbox:6080/;
}
```

​        新增上传文件静态路径预留：

```Nginx
location /uploads/ {
    alias /var/www/uploads/;
    add_header Cache-Control "private, max-age=3600";
    try_files $uri =404;
}
```

.1.6.7.1 代码讲解

​        SSE 流式事件最怕代理缓冲。

​        如果 Nginx 把后端返回的事件先攒起来，前端就会看到“等待很久后一次性出现”，体验会非常差。

​        所以 `/api/` 需要：

```Nginx
proxy_buffering off;
proxy_set_header X-Accel-Buffering no;
```

​        VNC 使用 WebSocket，所以 `/sandbox-vnc/` 必须保留：

```Nginx
proxy_set_header Upgrade $http_upgrade;
proxy_set_header Connection "upgrade";
```

​        `/uploads/` 当前是静态访问预留。现阶段文件下载仍然主要通过：

```Plain
/api/files/{file_id}/download
```

​        后续如果要让 Nginx 直接服务公开文件，可以把文件 URL 切到 `/uploads/...`。

#### 30.1.6.8 更新环境变量模板

​        打开：

```Plain
.env.example
```

​        加入：

```Plain
RUN_MIGRATIONS=true
```

.1.6.8.1 字段含义

​        `RUN_MIGRATIONS` 控制 API 容器启动时是否自动迁移数据库。

​        默认：

```Plain
true
```

​        这是课程项目更友好的默认值，避免用户忘记迁移。

### 30.1.7 关键理解

​        本节最重要的是理解“启动流程也是产品的一部分”。

​        一个 Agent 项目不只是代码能跑，还要做到：

```Plain
新环境能启动
数据库能迁移
网关能代理
长连接不断
停止和清理有明确命令
```

​        第二个重点是 Nginx 对流式响应的影响。

​        Agent 执行过程依赖 SSE：

```Plain
message_created
plan_created
step_started
tool_called
task_done
```

​        如果 Nginx 缓冲这些事件，前端就无法像实时任务一样展示。

​        第三个重点是健康检查。

​        `depends_on` 不应该只看“容器创建了没有”，还应该看“服务是否真的能响应”。

### 30.1.8 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

#### 30.1.8.1 运行生产配置测试

```Bash
python3 -m unittest tests/test_production_startup_config.py -v
```

​        预期看到：

```Plain
OK
```

#### 30.1.8.2 检查 Compose 配置

```Bash
docker compose config
```

​        预期能看到：

```Plain
RUN_MIGRATIONS: "true"
condition: service_healthy
/var/www/uploads
```

#### 30.1.8.3 构建并启动

​        如果你已经有镜像：

```Bash
./scripts/start.sh
```

​        如果本节改动后需要重新构建：

```Bash
BUILD=true ./scripts/start.sh
```

#### 30.1.8.4 验证网关

```Bash
curl http://localhost:8088/api/status
curl http://localhost:8088/api/status/database
curl http://localhost:8088/sandbox-api/status
curl http://localhost:8088/api/harness/cases
```

​        访问页面：

```Plain
http://localhost:8088
```

#### 30.1.8.5 停止服务

```Bash
./scripts/stop.sh
```

​        如果想清空数据卷：

```Bash
CLEAN_VOLUMES=true ./scripts/stop.sh
```

### 30.1.9 小结

​        本节把项目启动方式向生产化推进了一步：

​        具体来说，第一，新增根目录一键启动脚本；第二，新增根目录停止脚本；第三，API 容器启动时自动执行数据库迁移；第四，Compose 增加健康检查依赖；第五，Nginx 增强 SSE、WebSocket 和上传静态路径配置；第六，新增生产启动配置测试；第七，README、Nginx README 和环境变量模板同步更新。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一阶段开始，项目不再依赖一串容易忘的手动命令，而是有了更稳定的一键启动入口。

## 30.2 测试、调试与可观测性

### 30.2.1 本节目标

​        学完本节后，你将能够：

​        换句话说，第一，理解 Agent 项目为什么需要测试、调试和可观测性一起建设；第二，为 API 请求增加 `X-Request-ID`，让一次请求可以被追踪；第三，新增系统诊断清单接口，集中展示常用排查命令；第四，在设置页展示 API、数据库、沙箱、工具、记忆和 Harness 的诊断入口；第五，使用 `unittest` 验证请求 ID 和诊断清单；第六，建立一套从页面问题追到后端日志、接口和容器状态的排查路径。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 30.2.2 最终效果

​        本节结束后，任意 API 响应都会带上：

```Plain
X-Request-ID
```

​        新增接口：

```Plain
GET /api/observability/checks
```

​        访问：

```Plain
http://localhost:8088
```

​        点击左侧“设置”，页面中会新增：

```Plain
系统诊断
```

​        面板会列出常用排查命令，例如：

```Bash
curl http://localhost:8088/api/status
curl http://localhost:8088/api/status/database
curl http://localhost:8088/sandbox-api/status
curl http://localhost:8088/api/harness/cases
```

### 30.2.3 本节要解决的问题

​        前面章节已经实现了很多能力：

```Plain
会话
消息
事件
文件
长期记忆
Agent Runner
工具调用
Sandbox
Browser
VNC
MCP
A2A
多 Agent
Harness
```

​        功能越多，排查问题越复杂。

​        比如页面上只看到：

```Plain
API 异常
```

​        但真正的问题可能在：

​        从实现顺序看，第一，Nginx 没有代理到 API；第二，API 容器没有启动；第三，API 能启动但数据库没连上；第四，数据库迁移没执行；第五，Redis 任务队列不可用；第六，Sandbox 没有启动；第七，VNC WebSocket 断开；第八，MCP 或 A2A 配置不可用；第九，某条 Agent 事件缺少必要 payload。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本节不只是“多写几个测试”，而是开始建立系统化排查能力。

### 30.2.4 本节技术方案

​        本节新增两类能力。

​        第一类是运行时请求追踪：

```Plain
RequestIdMiddleware
  |
  +-- 读取 X-Request-ID
  +-- 没有则生成 UUID
  +-- 写入 request.state.request_id
  +-- 响应头返回 X-Request-ID
```

​        第二类是诊断清单：

```Plain
ObservabilityService
  |
  v
GET /api/observability/checks
  |
  v
前端系统诊断面板
```

​        本节暂时不做：

​        放到工程语境里看，第一，不接 Prometheus；第二，不接 OpenTelemetry；第三，不接 Grafana；第四，不做集中日志平台；第五，不做真实链路追踪采样。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些能力更偏生产平台化。课程项目先把最关键的“请求可追踪、诊断有入口、测试能覆盖”做稳。

### 30.2.5 新增和修改的文件

```Plain
README.md
backend/api/README.md
frontend/web/README.md
backend/api/app/core/request_id.py
backend/api/app/main.py
backend/api/app/domain/observability/entities.py
backend/api/app/application/observability_service.py
backend/api/app/schemas/observability.py
backend/api/app/presentation/http/routes/observability.py
backend/api/app/presentation/http/router.py
backend/api/tests/test_request_id_middleware.py
backend/api/tests/test_observability_service.py
frontend/web/app/types.ts
frontend/web/app/lib/observability-api.ts
frontend/web/app/components/observability-panel.tsx
frontend/web/app/components/settings-workspace.tsx
docs/course/chapters/47-testing-debugging-observability.md
```

### 30.2.6 实施步骤
#### 30.2.6.1 先写请求 ID 中间件测试

​        创建：

```Plain
backend/api/tests/test_request_id_middleware.py
```

​        完整代码如下：

```Python
import re
import unittest

from fastapi import FastAPI
from fastapi.testclient import TestClient

from app.core.request_id import RequestIdMiddleware

class RequestIdMiddlewareTest(unittest.TestCase):
    # ===================== 第1步：没有传入请求 ID 时，服务端应自动生成一个 =====================
    def test_generates_request_id_response_header(self) -> None:
        app = FastAPI()
        app.add_middleware(RequestIdMiddleware)

        @app.get("/ping")
        async def ping():
            return {"ok": True}

        response = TestClient(app).get("/ping")

        self.assertEqual(response.status_code, 200)
        self.assertRegex(
            response.headers["X-Request-ID"],
            re.compile(r"^[0-9a-f-]{36}$"),
        )

    # ===================== 第2步：调用方传入请求 ID 时，应保持同一个 ID 贯穿响应 =====================
    def test_reuses_incoming_request_id(self) -> None:
        app = FastAPI()
        app.add_middleware(RequestIdMiddleware)

        @app.get("/ping")
        async def ping():
            return {"ok": True}

        response = TestClient(app).get(
            "/ping",
            headers={"X-Request-ID": "debug-request-001"},
        )

        self.assertEqual(response.headers["X-Request-ID"], "debug-request-001")
```

.2.6.1.1 代码讲解

​        这个测试只创建一个最小 FastAPI 应用，不启动完整项目。

​        它验证两个行为：

​        展开来看，第一，浏览器或 curl 没有传 `X-Request-ID` 时，服务端自动生成；第二，调用方已经传了 `X-Request-ID` 时，服务端沿用它。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这样用户在排查问题时，可以主动传一个固定 ID：

```Bash
curl -H "X-Request-ID: debug-001" http://localhost:8088/api/status
```

​        后端响应也会返回同一个 ID。

#### 30.2.6.2 实现 RequestIdMiddleware

​        创建：

```Plain
backend/api/app/core/request_id.py
```

​        完整代码如下：

```Python
import logging
from uuid import uuid4

from starlette.middleware.base import BaseHTTPMiddleware
from starlette.requests import Request
from starlette.responses import Response

logger = logging.getLogger(__name__)

REQUEST_ID_HEADER = "X-Request-ID"

class RequestIdMiddleware(BaseHTTPMiddleware):
    """为每个 HTTP 请求补充可追踪的 request id。"""

    async def dispatch(self, request: Request, call_next) -> Response:
        # ===================== 第1步：读取调用方传入的请求 ID，或生成一个新的 =====================
        request_id = request.headers.get(REQUEST_ID_HEADER) or str(uuid4())
        request.state.request_id = request_id

        # ===================== 第2步：继续处理请求 =====================
        response = await call_next(request)

        # ===================== 第3步：把请求 ID 放回响应头，方便前端和 curl 排查 =====================
        response.headers[REQUEST_ID_HEADER] = request_id
        logger.debug(
            "request completed: method=%s path=%s request_id=%s status=%s",
            request.method,
            request.url.path,
            request_id,
            response.status_code,
        )
        return response
```

​        打开：

```Plain
backend/api/app/main.py
```

​        注册中间件：

```Python
from app.core.request_id import RequestIdMiddleware

app.add_middleware(RequestIdMiddleware)
```

.2.6.2.1 业务流程

​        一次请求进入 API 后：

```Plain
请求进入 FastAPI
  |
  v
RequestIdMiddleware
  |
  +-- 读取或生成 request_id
  +-- 放入 request.state
  |
  v
路由和应用服务继续执行
  |
  v
响应头写入 X-Request-ID
```

.2.6.2.2 为什么这样做

​        请求 ID 是最小但很有用的可观测性能力。

​        它让排查路径变成：

```Plain
用户截图里的 X-Request-ID
  |
  v
Nginx / API 日志搜索同一个 request_id
  |
  v
定位对应接口、状态码和错误
```

#### 30.2.6.3 先写诊断清单测试

​        创建：

```Plain
backend/api/tests/test_observability_service.py
```

​        完整代码如下：

```Python
import unittest

from app.application.observability_service import ObservabilityService

class ObservabilityServiceTest(unittest.TestCase):
    # ===================== 第1步：诊断清单应覆盖核心 Agent 子系统 =====================
    def test_checks_cover_core_agent_subsystems(self) -> None:
        service = ObservabilityService()

        checks = service.list_checks()
        keys = {check.key for check in checks}

        self.assertIn("api_status", keys)
        self.assertIn("database_status", keys)
        self.assertIn("sandbox_status", keys)
        self.assertIn("harness_cases", keys)
        self.assertIn("mcp_tools", keys)
        self.assertIn("a2a_agents", keys)

    # ===================== 第2步：每条诊断项都应包含可执行命令和预期结果 =====================
    def test_every_check_has_command_and_expected_result(self) -> None:
        service = ObservabilityService()

        for check in service.list_checks():
            self.assertTrue(check.command)
            self.assertTrue(check.expected)
            self.assertTrue(check.description)
```

.2.6.3.1 测试重点

​        这个测试不真的执行 curl。

​        它检查诊断清单本身是否完整：

​        具体来说，第一，有没有覆盖核心子系统；第二，每条诊断项有没有命令；第三，每条诊断项有没有预期结果。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 30.2.6.4 定义诊断领域实体

​        创建：

```Plain
backend/api/app/domain/observability/entities.py
```

​        完整代码如下：

```Python
from dataclasses import dataclass

@dataclass(slots=True)
class ObservabilityCheck:
    """一条面向排查的诊断项。"""

    key: str
    name: str
    category: str
    description: str
    command: str
    expected: str
```

.2.6.4.1 字段含义

​        换句话说，第一，`key`：稳定标识，用来给前端列表设置 key；第二，`name`：展示名称；第三，`category`：诊断分类，例如 core、agent、tooling；第四，`description`：说明这条诊断项排查什么；第五，`command`：可以复制执行的命令；第六，`expected`：正常情况下应该看到什么。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 30.2.6.5 实现 ObservabilityService

​        创建：

```Plain
backend/api/app/application/observability_service.py
```

​        核心代码如下：

```Python
class ObservabilityService:
    """提供 Agent 系统调试清单。"""

    def list_checks(self) -> list[ObservabilityCheck]:
        """返回核心子系统的可执行诊断命令。"""

        # ===================== 第1步：API、数据库和会话基础链路 =====================
        base_checks = [
            ObservabilityCheck(
                key="api_status",
                name="API 状态",
                category="core",
                description="确认主 API 服务是否可用。",
                command="curl http://localhost:8088/api/status",
                expected='返回 code=200，data.status 为 "ok"',
            ),
            ObservabilityCheck(
                key="database_status",
                name="数据库状态",
                category="core",
                description="确认 API 是否能连接 PostgreSQL。",
                command="curl http://localhost:8088/api/status/database",
                expected="返回 code=200，数据库状态正常",
            ),
        ]
```

​        继续加入 Agent 和工具相关诊断项：

```Python
        agent_checks = [
            ObservabilityCheck(
                key="session_context",
                name="会话上下文",
                category="agent",
                description="确认短期消息、事件摘要和长期记忆注入是否可读。",
                command="curl http://localhost:8088/api/sessions/{session_id}/context",
                expected="返回 messages、event_summaries、memory_context 和 budget",
            ),
            ObservabilityCheck(
                key="harness_cases",
                name="Harness 任务集",
                category="agent",
                description="确认固定回归任务集和 Harness 接口是否可用。",
                command="curl http://localhost:8088/api/harness/cases",
                expected="返回 browser、memory、multi_agent 等评测用例",
            ),
        ]
```

.2.6.5.1 为什么不在接口里直接探测所有服务

​        诊断接口如果每次都真的访问数据库、Sandbox、MCP、A2A 和浏览器，会有两个问题：

​        从实现顺序看，第一，它本身可能很慢；第二，某个外部服务卡住时，诊断接口也会卡住。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本节先做“诊断清单”，把排查入口结构化返回。

​        真正执行命令由用户或后续自动诊断流程完成。

#### 30.2.6.6 新增诊断 Schema 和路由

​        创建：

```Plain
backend/api/app/schemas/observability.py
```

​        完整代码如下：

```Python
from pydantic import BaseModel

class ObservabilityCheckResponse(BaseModel):
    key: str
    name: str
    category: str
    description: str
    command: str
    expected: str

class ObservabilityCheckListResponse(BaseModel):
    items: list[ObservabilityCheckResponse]
```

​        创建：

```Plain
backend/api/app/presentation/http/routes/observability.py
```

​        核心代码如下：

```Python
@router.get("/checks", response_model=ApiResponse[ObservabilityCheckListResponse])
async def list_observability_checks(
    service: ObservabilityService = Depends(build_observability_service),
) -> ApiResponse[ObservabilityCheckListResponse]:
    # ===================== 第1步：读取可执行诊断清单 =====================
    checks = service.list_checks()

    # ===================== 第2步：转换成统一响应结构 =====================
    return ApiResponse(
        data=ObservabilityCheckListResponse(
            items=[to_check_response(check) for check in checks],
        )
    )
```

​        打开：

```Plain
backend/api/app/presentation/http/router.py
```

​        注册：

```Python
from app.presentation.http.routes import observability

api_router.include_router(observability.router)
```

#### 30.2.6.7 新增前端 API 和类型

​        打开：

```Plain
frontend/web/app/types.ts
```

​        新增：

```TypeScript
export type ObservabilityCheckItem = {
  key: string;
  name: string;
  category: string; // core、agent、tooling 等诊断分类。
  description: string; // 这条诊断项解决什么排查问题。
  command: string; // 可以直接复制执行的 curl 或命令。
  expected: string; // 正常情况下应该看到什么结果。
};

export type ObservabilityCheckListData = {
  items: ObservabilityCheckItem[];
};
```

​        创建：

```Plain
frontend/web/app/lib/observability-api.ts
```

​        完整代码如下：

```TypeScript
import { requestApi } from "./api";
import type { ObservabilityCheckListData } from "../types";

export function fetchObservabilityChecks(): Promise<ObservabilityCheckListData> {
  return requestApi<ObservabilityCheckListData>("/api/observability/checks");
}
```

.2.6.7.1 代码讲解

​        前端组件不直接写接口路径。

​        它只调用：

```Plain
fetchObservabilityChecks()
```

​        这样后续如果接口路径改变，只需要改 `lib` 层。

#### 30.2.6.8 新增系统诊断面板

​        创建：

```Plain
frontend/web/app/components/observability-panel.tsx
```

​        核心代码如下：

```TypeScript
export function ObservabilityPanel() {
  const [checks, setChecks] = useState<
    LoadState<ObservabilityCheckListData>
  >({ type: "loading" });

  async function loadChecks() {
    setChecks({ type: "loading" });
    try {
      const data = await fetchObservabilityChecks();
      setChecks({ type: "ready", data });
    } catch (error) {
      const message = error instanceof Error ? error.message : "unknown error";
      setChecks({ type: "error", message });
    }
  }

  useEffect(() => {
    loadChecks();
  }, []);
```

​        单条诊断项：

```TypeScript
function ObservabilityCheckCard({ item }: { item: ObservabilityCheckItem }) {
  async function copyCommand() {
    await navigator.clipboard.writeText(item.command);
  }

  return (
    <div className="rounded-md border border-slate-200 bg-slate-50 p-4">
      <div className="flex items-start justify-between gap-4">
        <div className="min-w-0">
          <div className="flex items-center gap-2">
            <TerminalSquare className="text-slate-500" size={16} />
            <span className="text-sm font-semibold text-slate-950">
              {item.name}
            </span>
            <span className="rounded bg-white px-2 py-1 text-xs text-slate-500">
              {item.category}
            </span>
          </div>
          <p className="mt-2 text-xs leading-5 text-slate-500">
            {item.description}
          </p>
        </div>
        <button onClick={copyCommand} type="button">
          复制
        </button>
      </div>
    </div>
  );
}
```

.2.6.8.1 组件职责

​        `ObservabilityPanel` 只负责诊断清单：

​        放到工程语境里看，第一，读取诊断项；第二，展示命令；第三，展示预期结果；第四，支持复制命令。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        它不直接执行这些命令，因为浏览器里不能安全地执行本机 shell 命令。

#### 30.2.6.9 接入设置页

​        打开：

```Plain
frontend/web/app/components/settings-workspace.tsx
```

​        导入：

```TypeScript
import { ObservabilityPanel } from "./observability-panel";
```

​        在 Harness 面板后面加入：

```TypeScript
<MemorySettingsPanel />
<HarnessPanel />
<ObservabilityPanel />
```

.2.6.9.1 为什么放在设置页

​        系统诊断是工程运维入口，不是普通对话内容。

​        主工作台负责 Agent 任务执行。

​        设置页负责：

```Plain
配置
记忆管理
Harness
系统诊断
```

### 30.2.7 关键理解

​        本节最重要的是理解“可观测性不是等系统坏了再补”。

​        Agent 系统的链路比普通 CRUD 更长：

```Plain
前端输入
  |
  v
SSE
  |
  v
Planner
  |
  v
Context / Memory
  |
  v
Tool Selection
  |
  v
Sandbox / Browser / MCP / A2A
  |
  v
Event Timeline
```

​        任何一层出问题，用户看到的都可能只是“任务失败”。

​        所以需要：

​        展开来看，第一，请求 ID 串联日志；第二，诊断清单统一排查入口；第三，单元测试覆盖关键服务；第四，Harness 覆盖任务级回归；第五，Docker/Nginx 命令验证运行环境。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 30.2.8 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

#### 30.2.8.1 运行新增后端测试

```Bash
cd backend/api
uv run python -m unittest tests/test_request_id_middleware.py tests/test_observability_service.py -v
```

​        预期看到：

```Plain
OK
```

#### 30.2.8.2 运行全部后端测试

```Bash
uv run python -m unittest discover -s tests -v
uv run python -m compileall app
```

#### 30.2.8.3 检查前端类型和构建

```Bash
cd ../../frontend/web
pnpm typecheck
pnpm build
```

#### 30.2.8.4 Docker 构建和启动

​        最近章节涉及 Docker、Nginx 和生产启动脚本，建议使用新镜像验证：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
BUILD=true ./scripts/start.sh
```

#### 30.2.8.5 验证请求 ID

```Bash
curl -i http://localhost:8088/api/status
```

​        预期响应头中能看到：

```Plain
X-Request-ID: ...
```

​        也可以自己传入：

```Bash
curl -i http://localhost:8088/api/status \
  -H "X-Request-ID: debug-001"
```

​        预期响应头仍然是：

```Plain
X-Request-ID: debug-001
```

#### 30.2.8.6 验证诊断清单

```Bash
curl http://localhost:8088/api/observability/checks
```

​        预期返回中包含：

```Plain
api_status
database_status
sandbox_status
harness_cases
mcp_tools
a2a_agents
```

#### 30.2.8.7 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        操作：

​        具体来说，第一，点击左侧“设置”；第二，找到“系统诊断”面板；第三，确认能看到 API、数据库、Sandbox、Harness、MCP、A2A 等诊断项；第四，点击复制按钮，确认能复制命令。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 30.2.9 小结

​        本节完成了测试、调试与可观测性的第一层建设：

​        换句话说，第一，新增请求 ID 中间件；第二，所有 API 响应返回 `X-Request-ID`；第三，新增系统诊断领域实体和应用服务；第四，新增 `/api/observability/checks` 接口；第五，前端设置页新增系统诊断面板；第六，新增请求 ID 和诊断清单单元测试；第七，补充了从 API、数据库、Sandbox、工具、记忆、Harness 排查问题的入口。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一阶段开始，项目不仅“能跑”，还开始具备“出问题时知道从哪里查”的工程能力。

## 30.3 本章小结

​        完成“生产构建、一键即起与 Nginx”和“测试、调试与可观测性”两个阶段后，这条能力链已经形成闭环。读者仍然可以在每个阶段结束时单独运行验证，但理解上应把两者视作一个连续决策：先建立可靠边界，再让上层能力真正依赖它。

---

[← 第二十九章. 复杂任务复原与 Agent Harness](29-复杂任务复原与%20Agent%20Harness.md) · [返回目录](../README.md) · [第三十一章. 安全加固与沙箱划界 →](31-安全加固与沙箱划界.md)
