# 第三十二章. 产品体验验收与前端交互细琢

## 32.1 成熟 Agent 产品体验点验

### 32.1.1 本节目标

​        学完本节后，你将能够：

​        从实现顺序看，第一，理解为什么最终阶段要做产品体验验收；第二，新增产品体验验收清单接口；第三，把自然对话、计划执行、工具预览、记忆、多 Agent、Harness、浏览器和 VNC 放到同一张验收表里；第四，区分“已有自动化证据”和“必须人工观察”的验收项；第五，在设置页查看最终验收清单；第六，使用 Docker Compose 完成端到端验收。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.1.2 最终效果

​        本节结束后，后端新增接口：

```Plain
GET /api/acceptance/checks
```

​        通过网关访问：

```Bash
curl http://localhost:8088/api/acceptance/checks
```

​        会返回类似结构：

```JSON
{
  "code": 200,
  "message": "success",
  "data": {
    "summary": {
      "total": 11,
      "ready": 6,
      "needs_manual_check": 5
    },
    "items": [
      {
        "key": "natural_conversation",
        "title": "自然对话入口",
        "category": "conversation",
        "status": "needs_manual_check",
        "evidence": "页面发送一条任务消息后，应自动进入规划和执行流程。",
        "verify_steps": ["访问 http://localhost:8088。"],
        "related_routes": ["POST /api/sessions"]
      }
    ]
  }
}
```

​        访问：

```Plain
http://localhost:8088
```

​        点击左侧“设置”，页面中会新增：

```Plain
产品体验验收
```

​        面板会展示：

​        放到工程语境里看，第一，验收项总数；第二，已有自动化证据数量；第三，需要人工确认数量；第四，每条验收项的证据、验证步骤和相关接口。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.1.3 本节要解决的问题

​        前面章节已经逐步实现了很多能力：

```Plain
会话
消息
SSE
计划
工具调用
文件
Shell
浏览器
VNC
MCP
A2A
多 Agent
长期记忆
Harness
诊断
安全边界
```

​        但是能力都存在，不等于产品体验已经完整。

​        最终阶段最容易出现的问题是：

​        展开来看，第一，接口都能调通，但页面流程不顺；第二，单个工具能用，但没有进入统一时间线；第三，Harness 能跑，但没有和产品体验验收关联；第四，记忆接口存在，但没有确认是否真的进入上下文；第五，浏览器截图能返回，但 VNC 观察没有被纳入最终检查；第六，失败、停止、重试接口都在，但没有形成验收路径。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本节做一件事：

```Plain
把成熟 Agent 产品的核心体验整理成一张最终验收清单。
```

​        这张清单不是替代真实测试，也不是替代人工体验。

​        它的作用是让项目最后阶段有一个统一入口，明确：

```Plain
哪些能力已经具备代码和接口证据
哪些体验必须打开页面人工确认
每一项应该怎么验证
出了问题应该看哪些接口
```

### 32.1.4 本节技术方案

​        本节新增 `ProductAcceptanceService`。

​        服务返回：

```Plain
ProductAcceptanceChecklist
  |
  +-- summary
  |     +-- total
  |     +-- ready
  |     +-- needs_manual_check
  |
  +-- items
        +-- natural_conversation
        +-- agent_plan_execution
        +-- streaming_timeline
        +-- tool_preview_surface
        +-- context_and_memory
        +-- multi_agent_collaboration
        +-- harness_regression
        +-- browser_vnc_observation
        +-- mcp_a2a_events
        +-- failure_recovery
        +-- compose_startup
```

​        每条验收项包含：

```Plain
key             稳定标识
title           展示标题
category        分类
status          ready 或 needs_manual_check
evidence        当前项目已有证据
verify_steps    验证步骤
related_routes  相关接口
```

​        本节新增和修改文件：

```Plain
README.md
backend/api/README.md
frontend/web/README.md
backend/api/app/domain/acceptance/__init__.py
backend/api/app/domain/acceptance/entities.py
backend/api/app/application/product_acceptance_service.py
backend/api/app/schemas/acceptance.py
backend/api/app/presentation/http/routes/acceptance.py
backend/api/app/presentation/http/router.py
backend/api/tests/test_product_acceptance_service.py
frontend/web/app/types.ts
frontend/web/app/lib/acceptance-api.ts
frontend/web/app/components/product-acceptance-panel.tsx
frontend/web/app/components/settings-workspace.tsx
docs/course/chapters/49-product-experience-acceptance.md
```

### 32.1.5 实施步骤
#### 32.1.5.1 先写产品验收测试

​        创建 `backend/api/tests/test_product_acceptance_service.py`：

```Python
import unittest

from app.application.product_acceptance_service import ProductAcceptanceService

class ProductAcceptanceServiceTest(unittest.TestCase):
    # ===================== 第1步：最终验收清单必须覆盖成熟 Agent 产品核心体验 =====================
    def test_acceptance_items_cover_mature_agent_experience(self) -> None:
        service = ProductAcceptanceService()

        checklist = service.get_checklist()
        item_ids = {item.key for item in checklist.items}

        self.assertIn("natural_conversation", item_ids)
        self.assertIn("agent_plan_execution", item_ids)
        self.assertIn("streaming_timeline", item_ids)
        self.assertIn("tool_preview_surface", item_ids)
        self.assertIn("context_and_memory", item_ids)
        self.assertIn("multi_agent_collaboration", item_ids)
        self.assertIn("harness_regression", item_ids)
        self.assertIn("browser_vnc_observation", item_ids)
        self.assertIn("failure_recovery", item_ids)
        self.assertIn("compose_startup", item_ids)

    # ===================== 第2步：每个验收项都要给出证据、验证步骤和相关接口 =====================
    def test_every_acceptance_item_has_evidence_steps_and_routes(self) -> None:
        service = ProductAcceptanceService()

        checklist = service.get_checklist()

        for item in checklist.items:
            self.assertTrue(item.evidence)
            self.assertTrue(item.verify_steps)
            self.assertTrue(item.related_routes)
            self.assertIn(item.status, {"ready", "needs_manual_check"})

    # ===================== 第3步：汇总信息应能告诉前端有多少项已经具备自动验收证据 =====================
    def test_summary_counts_ready_and_manual_items(self) -> None:
        service = ProductAcceptanceService()

        checklist = service.get_checklist()

        self.assertEqual(checklist.summary.total, len(checklist.items))
        self.assertEqual(
            checklist.summary.ready + checklist.summary.needs_manual_check,
            checklist.summary.total,
        )
        self.assertGreater(checklist.summary.ready, 0)

if __name__ == "__main__":
    unittest.main()
```

.1.5.1.1 代码讲解

​        这组测试关注三件事。

​        第一，验收清单不能漏掉关键体验。

​        比如：

```Plain
自然对话
计划执行
流式时间线
工具预览
长期记忆
多 Agent
Harness
浏览器和 VNC
失败恢复
Compose 启动
```

​        第二，每条验收项必须能指导用户操作，所以必须有：

```Plain
evidence
verify_steps
related_routes
```

​        第三，前端需要展示汇总数据，所以要保证：

```Plain
ready + needs_manual_check = total
```

#### 32.1.5.2 定义产品验收领域实体

​        创建 `backend/api/app/domain/acceptance/__init__.py`：

```Python
"""Product acceptance domain package."""
```

​        创建 `backend/api/app/domain/acceptance/entities.py`：

```Python
from dataclasses import dataclass

@dataclass(slots=True)
class ProductAcceptanceItem:
    """一条最终产品体验验收项。"""

    key: str
    title: str
    category: str
    status: str
    evidence: str
    verify_steps: list[str]
    related_routes: list[str]

@dataclass(slots=True)
class ProductAcceptanceSummary:
    """最终验收清单的数量汇总。"""

    total: int
    ready: int
    needs_manual_check: int

@dataclass(slots=True)
class ProductAcceptanceChecklist:
    """最终产品体验验收清单。"""

    summary: ProductAcceptanceSummary
    items: list[ProductAcceptanceItem]
```

.1.5.2.1 字段讲解

​        `ProductAcceptanceItem` 是单条验收项。

​        `status` 不是任务执行状态，而是验收证据状态：

```Plain
ready               已经有接口、测试或服务能力作为证据
needs_manual_check  必须打开页面观察交互体验
```

​        为什么需要 `needs_manual_check`？

​        因为 UI 体验不能全部靠接口证明。

​        例如“发送消息后页面是否自然地流式更新”，最终还是要在浏览器里观察。

#### 32.1.5.3 实现 ProductAcceptanceService

​        创建 `backend/api/app/application/product_acceptance_service.py`。

​        文件核心结构如下：

```Python
from app.domain.acceptance.entities import (
    ProductAcceptanceChecklist,
    ProductAcceptanceItem,
    ProductAcceptanceSummary,
)

class ProductAcceptanceService:
    """汇总最终 Agent 产品体验验收清单。

    第 49 章不再新增大型业务模块，而是把前面章节已经实现的对话、计划、
    工具、记忆、多 Agent、Harness 和沙箱能力整理成一组可执行验收项。
    """

    def get_checklist(self) -> ProductAcceptanceChecklist:
        """返回成熟 Agent 产品体验验收清单。"""

        # ===================== 第1步：整理端到端对话与执行体验 =====================
        conversation_items = [
            ProductAcceptanceItem(
                key="natural_conversation",
                title="自然对话入口",
                category="conversation",
                status="needs_manual_check",
                evidence="页面发送一条任务消息后，应自动进入规划和执行流程。",
                verify_steps=[
                    "访问 http://localhost:8088。",
                    "创建或选择一个会话。",
                    "直接输入任务并发送，不需要额外点击生成计划或执行按钮。",
                    "确认对话区出现用户消息和 Agent 运行过程。",
                ],
                related_routes=[
                    "POST /api/sessions",
                    "POST /api/sessions/{session_id}/messages/stream",
                ],
            ),
            ProductAcceptanceItem(
                key="agent_plan_execution",
                title="计划生成和步骤执行",
                category="conversation",
                status="ready",
                evidence="Agent Runner 已统一 SSE、同步执行和后台任务链路，并产生 plan_created、step_started、tool_called 等事件。",
                verify_steps=[
                    "发送一个需要多步骤执行的任务。",
                    "确认时间线出现计划步骤。",
                    "确认步骤状态从 pending/running 变为 completed 或 failed。",
                ],
                related_routes=[
                    "POST /api/sessions/{session_id}/plan",
                    "POST /api/sessions/{session_id}/plan/tasks",
                    "GET /api/sessions/{session_id}/events",
                ],
            ),
        ]
```

.1.5.3.1 业务讲解

​        这里不是让后端真的去自动打开浏览器验收 UI。

​        后端提供的是“验收说明书的数据结构”。

​        前端拿到这份结构后，可以展示：

​        具体来说，第一，当前要验收什么；第二，为什么认为这个能力已经具备；第三，具体怎么验证；第四，出问题时优先看哪些接口。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        继续在 `get_checklist()` 中整理工具、记忆、多 Agent、Harness 和部署项：

```Python
        tool_items = [
            ProductAcceptanceItem(
                key="tool_preview_surface",
                title="统一工具预览",
                category="tooling",
                status="needs_manual_check",
                evidence="右侧工具预览已收敛文件、Shell、浏览器、搜索、MCP、A2A 和多 Agent 结果。",
                verify_steps=[
                    "发送包含文件、搜索或浏览器动作的任务。",
                    "点击时间线中的工具调用卡片。",
                    "确认右侧抽屉或弹层展示调用参数和结果。",
                ],
                related_routes=[
                    "GET /api/sessions/{session_id}/events",
                    "GET /api/files/{file_id}/preview",
                    "GET /sandbox-api/browser/page",
                ],
            ),
            ProductAcceptanceItem(
                key="browser_vnc_observation",
                title="浏览器截图和 VNC 观察",
                category="tooling",
                status="needs_manual_check",
                evidence="BrowserTool 可打开页面和截图，VNC 面板可以观察沙箱浏览器实时画面。",
                verify_steps=[
                    "发送一个访问网页并截图的任务。",
                    "确认事件中出现 browser_open 或 browser_screenshot。",
                    "确认右侧远程桌面能看到浏览器画面。",
                ],
                related_routes=[
                    "GET /sandbox-api/browser/status",
                    "POST /sandbox-api/browser/page/navigate",
                    "GET /sandbox-api/vnc/status",
                ],
            ),
        ]
```

​        最后计算汇总：

```Python
        items = conversation_items + tool_items + reliability_items
        ready = len([item for item in items if item.status == "ready"])
        needs_manual_check = len(
            [item for item in items if item.status == "needs_manual_check"]
        )
        summary = ProductAcceptanceSummary(
            total=len(items),
            ready=ready,
            needs_manual_check=needs_manual_check,
        )
        return ProductAcceptanceChecklist(summary=summary, items=items)
```

.1.5.3.2 为什么不全部标记为 ready

​        有些能力可以通过接口和测试证明。

​        例如：

```Plain
Harness 用例接口
长期记忆上下文接口
MCP/A2A 工具列表接口
```

​        但有些体验必须人工确认。

​        例如：

```Plain
页面是不是自然对话
时间线是不是持续更新
VNC 画面是不是能观察浏览器
工具详情是不是好理解
```

​        所以本节保留两种状态。

#### 32.1.5.4 新增 API Schema 和路由

​        创建 `backend/api/app/schemas/acceptance.py`：

```Python
from pydantic import BaseModel

class ProductAcceptanceItemResponse(BaseModel):
    key: str
    title: str
    category: str
    status: str
    evidence: str
    verify_steps: list[str]
    related_routes: list[str]

class ProductAcceptanceSummaryResponse(BaseModel):
    total: int
    ready: int
    needs_manual_check: int

class ProductAcceptanceChecklistResponse(BaseModel):
    summary: ProductAcceptanceSummaryResponse
    items: list[ProductAcceptanceItemResponse]
```

​        创建 `backend/api/app/presentation/http/routes/acceptance.py`：

```Python
from fastapi import APIRouter, Depends

from app.application.product_acceptance_service import ProductAcceptanceService
from app.domain.acceptance.entities import (
    ProductAcceptanceChecklist,
    ProductAcceptanceItem,
)
from app.schemas.acceptance import (
    ProductAcceptanceChecklistResponse,
    ProductAcceptanceItemResponse,
    ProductAcceptanceSummaryResponse,
)
from app.schemas.common import ApiResponse

router = APIRouter(prefix="/acceptance", tags=["acceptance"])

def build_product_acceptance_service() -> ProductAcceptanceService:
    return ProductAcceptanceService()

def to_item_response(item: ProductAcceptanceItem) -> ProductAcceptanceItemResponse:
    return ProductAcceptanceItemResponse(
        key=item.key,
        title=item.title,
        category=item.category,
        status=item.status,
        evidence=item.evidence,
        verify_steps=item.verify_steps,
        related_routes=item.related_routes,
    )

def to_checklist_response(
    checklist: ProductAcceptanceChecklist,
) -> ProductAcceptanceChecklistResponse:
    return ProductAcceptanceChecklistResponse(
        summary=ProductAcceptanceSummaryResponse(
            total=checklist.summary.total,
            ready=checklist.summary.ready,
            needs_manual_check=checklist.summary.needs_manual_check,
        ),
        items=[to_item_response(item) for item in checklist.items],
    )

@router.get("/checks", response_model=ApiResponse[ProductAcceptanceChecklistResponse])
async def get_product_acceptance_checks(
    service: ProductAcceptanceService = Depends(build_product_acceptance_service),
) -> ApiResponse[ProductAcceptanceChecklistResponse]:
    # ===================== 第1步：读取最终产品体验验收清单 =====================
    checklist = service.get_checklist()

    # ===================== 第2步：转换为前端可以直接渲染的响应结构 =====================
    return ApiResponse(data=to_checklist_response(checklist))
```

​        打开 `backend/api/app/presentation/http/router.py` 注册：

```Python
from app.presentation.http.routes import (
    a2a,
    acceptance,
    agent_core,
    ...
)

api_router.include_router(acceptance.router)
```

#### 32.1.5.5 前端新增验收类型和 API

​        打开 `frontend/web/app/types.ts`，新增：

```TypeScript
export type ProductAcceptanceItem = {
  key: string;
  title: string;
  category: string;
  status: "ready" | "needs_manual_check";
  evidence: string;
  verify_steps: string[];
  related_routes: string[];
};

export type ProductAcceptanceSummary = {
  total: number;
  ready: number;
  needs_manual_check: number;
};

export type ProductAcceptanceChecklistData = {
  summary: ProductAcceptanceSummary;
  items: ProductAcceptanceItem[];
};
```

​        创建 `frontend/web/app/lib/acceptance-api.ts`：

```TypeScript
import { requestApi } from "./api";
import type { ProductAcceptanceChecklistData } from "../types";

export function fetchProductAcceptanceChecks(): Promise<ProductAcceptanceChecklistData> {
  return requestApi<ProductAcceptanceChecklistData>("/api/acceptance/checks");
}
```

#### 32.1.5.6 新增产品体验验收面板

​        创建 `frontend/web/app/components/product-acceptance-panel.tsx`。

​        核心代码如下：

```TypeScript
"use client";

import { CheckCircle2, ClipboardCheck, Eye, RefreshCcw } from "lucide-react";
import { useEffect, useState } from "react";

import { fetchProductAcceptanceChecks } from "../lib/acceptance-api";
import type {
  LoadState,
  ProductAcceptanceChecklistData,
  ProductAcceptanceItem,
} from "../types";

// ===================== 第1步：展示最终产品体验验收入口 =====================
export function ProductAcceptancePanel() {
  const [checklist, setChecklist] = useState<
    LoadState<ProductAcceptanceChecklistData>
  >({ type: "loading" });

  async function loadChecklist() {
    // 1. 刷新时先进入 loading，让用户知道正在读取最新验收清单。
    setChecklist({ type: "loading" });
    try {
      // 2. 从主 API 读取最终产品体验验收项。
      const data = await fetchProductAcceptanceChecks();
      setChecklist({ type: "ready", data });
    } catch (error) {
      // 3. 设置页不能因为验收接口失败而整体不可用，所以错误只显示在本面板内。
      const message = error instanceof Error ? error.message : "unknown error";
      setChecklist({ type: "error", message });
    }
  }

  useEffect(() => {
    loadChecklist();
  }, []);

  return (
    <section className="rounded-md border border-slate-200 bg-white p-5">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h3 className="flex items-center gap-2 text-base font-semibold text-slate-950">
            <ClipboardCheck size={18} aria-hidden="true" />
            产品体验验收
          </h3>
          <p className="mt-1 text-sm leading-6 text-slate-500">
            汇总自然对话、工具预览、记忆、多 Agent、Harness 和沙箱观察的最终验收项
          </p>
        </div>
        <button
          className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-slate-200 bg-white text-slate-600 hover:bg-slate-50"
          onClick={loadChecklist}
          title="刷新验收清单"
          type="button"
        >
          <RefreshCcw size={16} aria-hidden="true" />
        </button>
      </div>

      {checklist.type === "loading" ? (
        <div className="mt-4 rounded-md border border-slate-200 bg-slate-50 p-3 text-sm text-slate-500">
          正在读取产品体验验收清单...
        </div>
      ) : null}

      {checklist.type === "error" ? (
        <div className="mt-4 rounded-md border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">
          {checklist.message}
        </div>
      ) : null}

      {checklist.type === "ready" ? (
        <ProductAcceptanceReadyView data={checklist.data} />
      ) : null}
    </section>
  );
}
```

.1.5.6.1 面板逻辑讲解

​        `ProductAcceptancePanel` 和第 47、48 章的诊断、安全面板一样，都使用：

```Plain
loading
error
ready
```

​        三种状态。

​        这样设置页的每个模块互不影响。

​        如果验收接口失败，只会显示本面板错误，不会让设置页其他模块一起崩掉。

#### 32.1.5.7 接入设置页

​        打开 `frontend/web/app/components/settings-workspace.tsx`，导入：

```TypeScript
import { ProductAcceptancePanel } from "./product-acceptance-panel";
```

​        在 `SettingsReadyView` 中加入：

```TypeScript
<MemorySettingsPanel />
<HarnessPanel />
<ProductAcceptancePanel />
<ObservabilityPanel />
<SecurityPanel />
```

.1.5.7.1 为什么放在设置页

​        产品体验验收不是单次对话内容。

​        它是项目最终检查入口，所以和这些面板放在一起更合适：

```Plain
Harness
产品体验验收
系统诊断
安全边界
```

### 32.1.6 关键理解

​        本节最重要的是理解“最终验收不是再写一堆新功能”。

​        最终验收做的是：

```Plain
确认前面功能能不能组成一个完整产品体验
```

​        第二个重点是理解自动证据和人工观察的区别。

​        接口可以证明：

```Plain
某个能力存在
某个数据能返回
某个服务能调用
```

​        但页面体验还要确认：

```Plain
用户是否只需要自然发送消息
时间线是否好理解
工具详情是否能看懂
VNC 是否真的能观察浏览器
失败和重试是否有清晰反馈
```

​        第三个重点是最终验收要可重复。

​        不要只说：

```Plain
项目看起来差不多了
```

​        而是要有：

```Plain
验收项
证据
步骤
接口
```

### 32.1.7 技术难点与亮点

​        技术难点：

​        换句话说，第一，很多体验无法只靠接口自动证明；第二，最终验收需要同时覆盖后端、前端、沙箱、工具、记忆和部署；第三，验收清单要能被测试，不能只是文档里的文字。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        从实现顺序看，第一，最终体验验收进入 API 和设置页；第二，验收项覆盖对话、计划、工具、记忆、多 Agent、Harness、浏览器和部署；第三，每项都提供证据、验证步骤和相关接口；第四，为本章后文最终交付和二次开发指南打基础。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.1.8 面试考点

​        放到工程语境里看，第一，为什么最终验收不能只看单个接口？；第二，Agent 产品体验验收应该覆盖哪些链路？；第三，哪些能力可以自动测试，哪些必须人工观察？；第四，为什么 Harness 不能完全替代产品体验验收？；第五，为什么工具调用需要进入统一事件流和统一预览？。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.1.9 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

#### 32.1.9.1 运行本节后端测试

```Bash
cd backend/api
uv run python -m unittest tests/test_product_acceptance_service.py -v
```

​        预期看到：

```Plain
Ran 3 tests
OK
```

#### 32.1.9.2 编译后端代码

```Bash
uv run python -m compileall app
```

​        预期没有 Python 语法错误。

#### 32.1.9.3 检查前端类型和构建

```Bash
cd ../../frontend/web
pnpm typecheck
pnpm build
```

​        预期 TypeScript 和 Next.js 构建都通过。

#### 32.1.9.4 Docker 构建和启动

​        本节改了 API 和 UI，建议重新构建镜像：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
BUILD=true ./scripts/start.sh
```

#### 32.1.9.5 验收接口验证

```Bash
curl http://localhost:8088/api/acceptance/checks
```

​        预期能看到：

```Plain
natural_conversation
agent_plan_execution
streaming_timeline
tool_preview_surface
context_and_memory
multi_agent_collaboration
harness_regression
browser_vnc_observation
failure_recovery
compose_startup
```

#### 32.1.9.6 页面验收

​        访问：

```Plain
http://localhost:8088
```

​        操作：

​        展开来看，第一，点击左侧“设置”；第二，确认出现“产品体验验收”面板；第三，确认面板顶部显示验收项总数、已有证据和人工确认数量；第四，查看每条验收项是否包含证据、验证步骤和相关接口；第五，回到“工作台”，发送一个真实任务；第六，确认对话区出现用户消息、计划、步骤、工具调用和最终结果；第七，点击工具调用卡片，确认右侧预览或弹层可以看到详情。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.1.10 小结

​        本节完成了成熟 Agent 产品体验验收入口：

​        具体来说，第一，新增产品验收领域实体；第二，新增 `ProductAcceptanceService`；第三，新增 `/api/acceptance/checks` 接口；第四，新增本节后端测试；第五，前端设置页新增“产品体验验收”面板；第六，验收清单覆盖对话、计划、工具、记忆、多 Agent、Harness、浏览器、VNC、失败恢复和 Compose 启动。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一阶段开始，项目具备了一张最终体验验收表。后续做二次开发时，可以先看这张表，确认改动有没有破坏核心 Agent 产品链路。

## 32.2 前端产品化交互与视觉细琢

### 32.2.1 本节目标

​        学完本节后，你将能够：

​        换句话说，第一，理解为什么最终阶段要集中打磨前端体验；第二，把主工作台从“功能可用”升级为“任务执行工作台”；第三，优化对话区、任务时间线、步骤节点、工具调用卡片和最终结果展示；第四，优化底部任务输入台，让用户只需要自然发送任务；第五，优化右侧工具预览区，让它更像 Agent 的远程电脑；第六，移除主工作台的演示感，保留设置页中的配置、诊断、安全和验收面板；第七，使用类型检查、构建和浏览器页面验证检查前端改动。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.2 最终效果

​        本节结束后，访问：

```Plain
http://localhost:8088
```

​        工作台会更接近成熟 Agent 产品体验：

​        从实现顺序看，第一，中间区域聚焦 AI 对话和任务执行过程；第二，用户发送任务后，不需要额外点击“生成计划”或“执行计划”；第三，时间线会展示任务理解、计划、步骤、工具调用和最终结果；第四，页面整体不再滚动，左侧会话列表和中间执行流各自滚动；第五，步骤节点以思维链式执行链展示，不再像后台管理卡片；第六，点击任意步骤会打开步骤详情弹窗，展示目标、状态、摘要和工具输出；第七，步骤节点更清晰地区分 `pending`、`running`、`completed`、`failed`；第八，工具调用可以点击查看右侧预览；第九，右侧预览区以“AtlasAgent 的电脑”为概念，集中展示工具输出、截图、搜索、MCP、A2A、多 Agent 等结果；第十，输入框更像任务分配台，而不是普通表单。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.3 本节要解决的问题

​        前文已经把最终产品体验整理成验收清单。

​        但是验收清单会暴露一个很关键的问题：

```Plain
后端能力已经很多，前端体验还需要最后收口。
```

​        前面章节为了教学清晰，逐步加入了很多面板和功能：

```Plain
会话
消息
计划
事件
工具
文件
上下文
VNC
MCP
A2A
多 Agent
Harness
诊断
安全
验收
```

​        这些能力如果都平铺在主页面里，用户会觉得像“功能集合”，而不是“AI Agent 工作台”。

​        成熟 Agent 产品的主工作台应该优先回答三个问题：

```Plain
我发了什么任务？
Agent 正在做什么？
我在哪里看工具结果？
```

​        所以本节集中优化主工作台：

​        放到工程语境里看，第一，主区域只服务 AI 对话和任务过程；第二，工具详情按需出现在右侧；第三，设置、诊断、安全、验收继续留在设置页；第四，输入区只保留任务分配和附件入口。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.4 本节技术方案

​        本节不大改后端，不新增数据库表，也不重写 Agent Runner。

​        本节只改前端组件和教程：

```Plain
frontend/web/app/page.tsx
frontend/web/app/globals.css
frontend/web/app/components/app-sidebar.tsx
frontend/web/app/components/session-list.tsx
frontend/web/app/components/chat-workspace.tsx
frontend/web/app/components/session-control-bar.tsx
frontend/web/app/components/conversation-timeline.tsx
frontend/web/app/components/chat-input.tsx
frontend/web/app/components/tool-preview-panel.tsx
frontend/web/Dockerfile
docker-compose.yml
frontend/web/README.md
README.md
docs/course/outline.md
docs/course/chapters/50-frontend-product-polish.md
```

​        核心改造思路：

```Plain
page.tsx
  |
  +-- 提供固定高度页面外壳
  +-- 工作台不显示顶部大标题
  +-- 设置页才显示控制中心和状态

ChatWorkspace
  |
  +-- 固定高度任务工作区
  +-- 中间独立滚动的对话时间线
  +-- 底部固定任务输入台
  +-- 右侧按需预览

ConversationTimeline
  |
  +-- 空状态
  +-- 用户消息
  +-- Agent 计划块
  +-- 思维链式步骤节点
  +-- 步骤详情弹窗
  +-- 工具调用入口
  +-- 最终结果

ToolPreviewPanel
  |
  +-- 当前工具结果
  +-- 最近工具调用
  +-- 参数和输出
```

### 32.2.5 实施步骤
#### 32.2.5.1 优化主页面外层

​        打开 `frontend/web/app/page.tsx`。

​        把主页面外层从普通容器改成固定高度应用外壳：

```TypeScript
return (
  <main className="h-screen overflow-hidden bg-[#050506] text-zinc-50">
    <div className="grid h-full min-h-0 grid-cols-[300px_1fr] max-lg:grid-cols-1">
```

​      <AppSidebar *...* />

​      <section className="agent-grid-bg flex min-h-0 min-w-0 flex-col overflow-hidden bg-[#05060a]">

​        {activeView === "settings" ? (

​          <header className="flex min-h-24 items-center justify-between border-b border-white/10 bg-[#05060a]/80 px-8 backdrop-blur-2xl">

​        ...

​          </header>

​        ) : null}

        <div className={activeView === "workspace" ? "min-h-0 flex-1 overflow-hidden p-0" : "min-h-0 flex-1 overflow-hidden p-5"}>

​        {activeView === "workspace" ? <ChatWorkspace *...* /> : <SettingsWorkspace *...* />}

​        </div>

​      </section>

​    </div>

  </main>

​        );

```Plain

```

.2.5.1.1 代码讲解

​        这里主要做了两件事。

​        第一，工作台页不再显示顶部大标题和状态徽标。

​        AI 对话产品的主界面应该把第一屏留给：

```Plain
用户任务
Agent 回复
执行步骤
工具详情
```

​        API 状态、数据库状态、配置项仍然重要，但它们放在设置页中，不再挤占对话页面。

​        第二，工作台内容区使用 `p-0`，让对话执行流完整占满右侧区域。

#### 32.2.5.2 锁定页面滚动边界

​        打开 `frontend/web/app/globals.css`，把页面高度固定住：

```CSS
html,
body {
  height: 100%;
  min-height: 100%;
  overflow: hidden;
}
```

​        打开 `frontend/web/app/page.tsx`，把主页面外层改成固定高度：

```TypeScript
<main className="h-screen overflow-hidden bg-[#050506] text-zinc-50">
  <div className="grid h-full min-h-0 grid-cols-[300px_1fr] max-lg:grid-cols-1">
    ...
  </div>
</main>
```

​        打开 `frontend/web/app/components/app-sidebar.tsx`，让左侧栏自己占满一屏：

```TypeScript
<aside className="flex h-screen min-h-0 flex-col overflow-hidden border-r border-white/10 bg-black px-4 py-5">
  ...
</aside>
```

​        打开 `frontend/web/app/components/session-list.tsx`，只让会话列表内部滚动：

```TypeScript
<div className="mt-3 min-h-0 flex-1 overflow-y-auto pr-1">
  <div className="grid gap-2">
    {state.data.map(...)}
  </div>
</div>
```

.2.5.2.1 代码讲解

​        这一步解决的是产品界面里非常关键的滚动问题。

​        如果让整个页面滚动，会出现两个不好的体验：

```Plain
左侧会话列表滚走
底部输入框跟着页面移动
```

​        Agent 工作台更适合使用固定外壳：

```Plain
浏览器窗口
  |
  +-- 左侧：会话列表内部滚动
  +-- 中间：对话执行流内部滚动
  +-- 底部：输入框固定
  +-- 右侧：详情抽屉按需出现
```

​        这样用户始终知道在哪里切换会话、在哪里输入任务、在哪里查看执行过程。

#### 32.2.5.3 确认 UI 容器监听地址

​        打开 `frontend/web/Dockerfile`，确认运行镜像中显式设置：

```Dockerfile
ENV HOSTNAME=0.0.0.0
ENV PORT=3000
```

.2.5.3.1 代码讲解

​        Next.js standalone 运行时会读取 `HOSTNAME` 和 `PORT`。

​        Docker 容器默认也有一个 `HOSTNAME` 环境变量，值通常是容器 ID。如果不显式设置，Next.js 可能只监听容器主机名，而不是监听所有网卡。

​        这会造成一个很隐蔽的问题：

```Plain
UI 进程已经启动
  |
  v
容器健康检查访问 localhost:3000 失败
  |
  v
UI 容器变成 unhealthy
  |
  v
Nginx 没有可用的 UI 上游
  |
  v
访问 http://localhost:8088 返回 502
```

​        所以这里明确绑定：

```Plain
0.0.0.0:3000
```

​        这样容器内健康检查、Nginx 和同一个 Docker 网络里的其他服务都能访问 UI。

#### 32.2.5.4 优化 ChatWorkspace 容器

​        打开 `frontend/web/app/components/chat-workspace.tsx`。

​        把工作台容器改成固定高度任务空间：

```TypeScript
return (
  <section className="relative flex h-full min-h-0 gap-0 overflow-hidden bg-transparent max-xl:flex-col">
    <div className="pointer-events-none absolute inset-0 bg-[linear-gradient(rgba(255,255,255,0.035)_1px,transparent_1px),linear-gradient(90deg,rgba(255,255,255,0.035)_1px,transparent_1px)] bg-[size:64px_64px]" />
    <div className="pointer-events-none absolute inset-0 bg-[radial-gradient(circle_at_50%_0%,rgba(59,130,246,0.14),transparent_34%),linear-gradient(180deg,rgba(5,6,10,0.25),#05060a_78%)]" />

    <div className="relative z-10 mx-auto flex min-h-0 w-full max-w-[980px] flex-1 flex-col overflow-hidden">
      ...
    </div>
  </section>
);
```

.2.5.4.1 代码讲解

​        这一段的两个绝对定位 `div` 是背景层：

​        展开来看，第一，第一个画网格；第二，第二个画顶部蓝色光感和底部暗色过渡。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        它们都使用：

```TypeScript
pointer-events-none
```

​        这样背景不会挡住按钮和输入框点击。

​        真正的内容区域加：

```TypeScript
relative z-10
```

​        保证内容在背景之上。

.2.5.4.2 为什么这样改

​        Agent 产品的工作台通常不是普通后台卡片。

​        这里取消旧版本的大圆角外框，把中间内容收敛成窄列执行流。这样页面更像一个 AI 任务过程，而不是多个模块拼在一起：

```Plain
任务进入系统
Agent 规划
步骤推进
工具调用
结果输出
```

​        右侧详情抽屉出现时，中间区域会让出空间；没有详情时，中间执行流保持居中。

#### 32.2.5.5 优化顶部会话控制栏

​        打开 `frontend/web/app/components/session-control-bar.tsx`。

​        新增 `CircleDot` 图标，并改造标题区域：

```TypeScript
<div className="min-w-0">
  <div className="mb-1 flex items-center gap-2 text-xs font-medium uppercase tracking-[0.18em] text-zinc-600">
    <CircleDot
      className={isRunning ? "text-blue-400" : "text-zinc-600"}
      size={14}
      aria-hidden="true"
    />
    AtlasAgent Workspace
  </div>
  <div className="truncate text-lg font-semibold text-zinc-100">
    {selectedSession ? selectedSession.title : "未选择会话"}
  </div>
  <div className="mt-1 flex flex-wrap items-center gap-2 text-xs text-zinc-500">
    <span className="rounded-full border border-white/10 bg-white/[0.04] px-2 py-1">
      {selectedSession?.status ?? "idle"}
    </span>
    <span>未读 {selectedSession?.unread_count ?? 0}</span>
  </div>
</div>
```

.2.5.5.1 代码讲解

​        控制栏不再只是显示：

```Plain
状态：running · 未读：0
```

​        而是把当前会话当成一个任务工作区来展示：

```Plain
AtlasAgent Workspace
任务标题
状态标签
未读数
```

​        右侧仍然保留：

​        具体来说，第一，上下文；第二，清未读；第三，停止。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        但按钮改成圆角胶囊，视觉上更轻。

#### 32.2.5.6 优化 ConversationTimeline

​        打开 `frontend/web/app/components/conversation-timeline.tsx`。

​        空状态改成更明确的任务入口：

```TypeScript
if (items.length === 0) {
  return (
    <div className="flex flex-1 items-center justify-center px-8 py-10 text-center">
      <div className="max-w-xl">
        <div className="mx-auto flex h-14 w-14 items-center justify-center rounded-3xl border border-blue-500/30 bg-blue-500/10 text-blue-400 shadow-2xl shadow-blue-950/30">
          <Sparkles size={22} aria-hidden="true" />
        </div>
        <h2 className="mt-5 text-2xl font-semibold text-zinc-100">
          直接描述任务，Agent 会开始规划和执行
        </h2>
        <p className="mt-3 text-base leading-7 text-zinc-500">
          对话区会连续展示任务理解、执行计划、步骤进度、工具调用和最终结果；需要查看细节时，点击工具节点打开右侧工作台。
        </p>
      </div>
    </div>
  );
}
```

.2.5.6.1 为什么改空状态

​        空状态是用户第一次进入产品时最重要的引导。

​        旧版本只告诉用户“开始让 Agent 工作”。

​        新版本明确告诉用户：

```Plain
直接描述任务
Agent 会自动规划和执行
工具细节在右侧打开
```

​        这能减少用户对“我下一步该点哪里”的困惑。

#### 32.2.5.7 优化计划块、执行链和步骤详情

​        在 `AgentRunBlock` 中，把说明文案改为：

```TypeScript
<p className="mt-3 text-base leading-8 text-zinc-400">
  这是一个任务执行过程，已拆成 {plan.steps.length} 个步骤。下面会按顺序规划、调用工具、记录证据并汇总结果。
</p>
```

​        步骤节点改成轻量执行链：

```TypeScript
<button
  className="flex w-full items-start justify-between gap-4 rounded-xl px-3 py-2 text-left outline-none transition focus-visible:ring-2 focus-visible:ring-blue-500/60"
  onClick={() => onOpenStep(step)}
  type="button"
>
  <div className="min-w-0">
    <div className="flex items-center gap-2">
      <span className="text-xs font-semibold text-blue-400">
        {String(index + 1).padStart(2, "0")}
      </span>
      <h4 className="truncate text-base font-semibold leading-7 text-zinc-50">
        {step.title}
      </h4>
      <Maximize2 className="shrink-0 text-zinc-600" size={14} aria-hidden="true" />
    </div>
    <p className="mt-2 line-clamp-2 text-sm leading-6 text-zinc-400">
      {running ? getRunningCopy(step) : completed ? step.summary : step.description}
    </p>
  </div>
  <span className={getStatusClass(step.status)}>
    {getStatusLabel(step.status)}
  </span>
</button>
```

​        步骤内部的工具调用继续作为胶囊按钮展示：

```TypeScript
<div className="ml-3 grid max-w-3xl gap-3 border-l border-dashed border-zinc-800/90 pl-4">
  <div className="inline-flex w-fit items-center gap-2 rounded-full border border-blue-500/20 bg-blue-500/10 px-3 py-1.5 text-sm font-semibold text-blue-200">
  <Wrench className="text-blue-400" size={18} aria-hidden="true" />
  {running ? "正在使用工具" : "工具调用完成"}
  </div>
</div>
```

​        点击步骤时，弹出 `StepDetailDialog`：

```TypeScript
{detailStep ? (
  <StepDetailDialog
    onClose={() => setDetailStep(null)}
    onSelectToolEvent={onSelectToolEvent}
    step={detailStep}
  />
) : null}
```

.2.5.7.1 业务讲解

​        步骤节点是 Agent 产品最核心的展示。

​        用户关心的不是事件表里有多少条事件，而是：

```Plain
这一步在做什么？
现在是否正在跑？
有没有调用工具？
结果在哪里看？
```

​        所以本节把步骤节点做成“执行链”：

​        换句话说，第一，左侧圆点表示状态；第二，标题表示任务拆解；第三，正文表示当前动作或结果；第四，工具胶囊表示调用过程；第五，点击步骤打开详情弹窗；第六，点击工具打开右侧工作区。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这样用户既能快速扫完整个任务，也能按需查看某一步的输入、输出和工具结果。

#### 32.2.5.8 优化底部任务输入台

​        打开 `frontend/web/app/components/chat-input.tsx`。

​        输入框改成任务分配台。

​        本节把输入区上方的“自动规划、工具调用、结果汇总”等标签去掉。原因是这些能力应该在任务执行流里自然出现，而不是作为静态说明占住输入区。输入区只保留两件事：

​        从实现顺序看，第一，用户要交给 Agent 的任务；第二，发送和附件入口。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

```TypeScript
<div className="rounded-[28px] border border-white/10 bg-[#07080d]/95 p-4 shadow-2xl shadow-black/40 ring-1 ring-blue-500/15">
  <textarea
    className="max-h-40 min-h-24 w-full resize-none border-0 bg-transparent px-2 py-2 text-lg font-medium leading-8 text-zinc-100 outline-none placeholder:text-zinc-600 disabled:bg-transparent"
    disabled={disabled || sending}
    onChange={(event) => onDraftChange(event.target.value)}
    placeholder={disabled ? "先创建或选择一个会话" : "分配一个任务或提问任何问题..."}
    value={draft}
  />
  <div className="mt-2 flex items-center justify-end">
    ...
  </div>
</div>
```

.2.5.8.1 代码讲解

​        这里故意不写：

```Plain
Enter 发送
```

​        因为当前是 `textarea`，Enter 更适合换行。真正发送由右侧按钮完成。

​        这比误导用户按 Enter 更稳。

​        输入区也不再写“自动规划”“工具调用”这种功能标签。任务发出后，计划、步骤、工具和最终结果会在中间执行流里出现，这样主界面更像一个真实对话工作台，而不是功能说明页。

#### 32.2.5.9 优化工具预览面板

​        打开 `frontend/web/app/components/tool-preview-panel.tsx`。

​        顶部改成工具工作区：

```TypeScript
<div className="border-b border-white/10 bg-[#0b0d14]/95 p-5">
  <div className="flex items-start justify-between gap-3">
    <div>
      <div className="mb-2 text-xs font-medium uppercase tracking-[0.18em] text-zinc-600">
        Tool Workspace
      </div>
      <h2 className="flex items-center gap-2 text-lg font-semibold text-zinc-50">
        <Monitor size={18} aria-hidden="true" />
        AtlasAgent 的电脑
      </h2>
      <p className="mt-1 text-sm leading-5 text-zinc-500">
        查看工具调用、文件、浏览器、远程 Agent 和协作结果
      </p>
    </div>
    ...
  </div>
</div>
```

.2.5.9.1 为什么叫“AtlasAgent 的电脑”

​        用户不需要理解所有底层工具名。

​        右侧面板可以被理解成：

```Plain
Agent 正在使用的远程工作区
```

​        里面可能展示：

​        放到工程语境里看，第一，文件；第二，Shell 输出；第三，浏览器截图；第四，搜索结果；第五，MCP 返回；第六，A2A 返回；第七，多 Agent 协作结果。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这个心智模型比“工具详情面板”更接近真实 Agent 产品体验。

### 32.2.6 关键理解

​        本节最重要的是理解“产品化前端不是换颜色”。

​        产品化前端要解决：

```Plain
用户下一步该做什么
系统正在发生什么
结果在哪里看
出错后怎么恢复
```

​        第二个重点是不要把设置页能力塞回工作台。

​        这些内容应该留在设置页：

​        展开来看，第一，Harness；第二，诊断；第三，安全；第四，验收；第五，模块配置。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        工作台只保留：

​        具体来说，第一，对话；第二，执行过程；第三，工具预览；第四，上下文和文件按需抽屉。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        第三个重点是保持数据流不变。

​        本节主要改组件表现，不改：

​        换句话说，第一，Zustand store；第二，SSE 数据流；第三，后端事件结构；第四，Agent Runner。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这样改动风险更低。

### 32.2.7 技术难点与亮点

​        技术难点：

​        从实现顺序看，第一，前端视觉升级不能破坏已有交互；第二，暗色界面容易出现文字层级混乱；第三，工具预览、文件预览、上下文预览都要共用右侧空间；第四，移动端不能因为固定宽度出现文字溢出。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        放到工程语境里看，第一，主工作台从功能集合升级为任务执行空间；第二，输入框表达“发送任务后自动规划和执行”；第三，计划、步骤和工具调用形成更连贯的任务流；第四，右侧预览更像 Agent 的远程工作区；第五，设置页继续承担配置、诊断、安全和验收职责。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.8 面试考点

​        展开来看，第一，为什么 Agent 产品主界面应该围绕任务流设计？；第二，为什么工具预览适合按需打开，而不是一直平铺？；第三，为什么本节只改表现层，不改状态管理和后端数据结构？；第四，如何避免前端功能越加越像后台管理系统？；第五，暗色工作台中如何处理文字层级和状态反馈？。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.9 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

#### 32.2.9.1 检查前端类型

```Bash
cd frontend/web
pnpm typecheck
```

​        预期没有 TypeScript 报错。

#### 32.2.9.2 构建前端

```Bash
pnpm build
```

​        预期 Next.js 构建成功。

#### 32.2.9.3 Docker 构建和启动

​        本节改了 UI，建议重新构建 UI 镜像：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
docker compose build ui
docker compose up -d --force-recreate ui nginx
```

​        如果想完整验证所有服务：

```Bash
BUILD=true ./scripts/start.sh
```

#### 32.2.9.4 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        检查：

​        具体来说，第一，浏览器页面本身不出现整页滚动条；第二，左侧会话列表较多时，只在会话列表内部滚动；第三，中间对话内容较多时，只在对话执行流内部滚动，底部输入框保持固定；第四，主工作台是暗色网格背景和居中的任务执行流，不再像功能卡片集合；第五，工作台页不再显示顶部大标题、API 状态和数据库状态；第六，当前会话标题只在对话执行区顶部轻量展示；第七，空状态提示“直接描述任务，Agent 会开始规划和执行”；第八，输入框提示“分配一个任务或提问任何问题”；第九，发送任务后，时间线展示用户消息、Agent 计划、思维链式步骤节点和工具调用入口；第十，点击任意步骤节点，页面弹出步骤详情；第十一，点击工具调用后，右侧出现“AtlasAgent 的电脑”预览区；第十二，点击“上下文”后，右侧出现上下文抽屉；第十三，切换到“设置”页，Harness、产品体验验收、系统诊断、安全边界仍然可用。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 32.2.10 小结

​        本节完成了前端产品化交互与视觉打磨的第一轮：

​        换句话说，第一，优化主页面外层和标题区；第二，优化 ChatWorkspace 工作台容器；第三，优化会话控制栏；第四，优化对话空状态、计划块、步骤节点和最终结果卡片；第五，优化底部任务输入台；第六，优化右侧工具预览工作区；第七，更新 UI README 和课程大纲。这些点放在一起看，构成了本节叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一阶段开始，项目不只是“功能都能用”，而是更像一个真正围绕 AI 任务执行设计的 Agent 工作台。

​        但本节不是最终 UI 封版。后续还会继续拆章节处理组件体系、流式动效、工具工作区、响应式和可访问性，避免把所有前端优化都塞进一章里。

## 32.3 本章小结

​        完成“成熟 Agent 产品体验点验”和“前端产品化交互与视觉细琢”两个阶段后，这条能力链已经形成闭环。读者仍然可以在每个阶段结束时单独运行验证，但理解上应把两者视作一个连续决策：先建立可靠边界，再让上层能力真正依赖它。

---

[← 第三十一章. 安全加固与沙箱划界](31-安全加固与沙箱划界.md) · [返回目录](../README.md) · [第三十三章. 组件规范与流式执行体验 →](33-组件规范与流式执行体验.md)
