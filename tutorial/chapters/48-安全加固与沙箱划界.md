# 第四十八章. 安全加固与沙箱划界

## 48.1 本章目标

​        学完本章后，你将能够：

​        从实现顺序看，第一，理解 Agent 项目为什么需要从一开始就明确安全边界；第二，新增安全检查清单接口；第三，检查 CORS、数据库默认密码、上传文件、Sandbox、外部工具和长期记忆风险；第四，在设置页展示安全边界检查结果；第五，理解 Sandbox 文件、Shell、Browser 和 VNC 为什么不能直接暴露到公网；第六，理解长期记忆为什么必须考虑敏感信息过滤、禁用和删除；第七，使用测试保证安全检查清单覆盖关键边界。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.2 最终效果

​        本章结束后，后端新增接口：

```Plain
GET /api/security/checks
```

​        通过网关访问：

```Bash
curl http://localhost:8088/api/security/checks
```

​        会返回类似结构：

```JSON
{
  "code": 200,
  "message": "success",
  "data": {
    "items": [
      {
        "key": "cors_origin_policy",
        "name": "CORS 来源策略",
        "category": "configuration",
        "severity": "warning",
        "risk": "CORS 过于开放会让任意网页调用 API，生产环境尤其危险。",
        "recommendation": "生产环境只保留正式前端域名；本地开发可以保留 localhost。",
        "verify_command": "curl http://localhost:8088/api/security/checks"
      }
    ]
  }
}
```

​        访问：

```Plain
http://localhost:8088
```

​        点击左侧“设置”，页面中会新增：

```Plain
安全边界
```

​        面板会展示：

​        放到工程语境里看，第一，配置风险；第二，上传风险；第三，Sandbox 风险；第四，外部集成风险；第五，长期记忆风险；第六，每条风险的验证命令。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.3 本章要解决的问题

​        到第 47 章，项目已经有了：

```Plain
AI 对话
计划和执行
文件上传和预览
Shell 工具
浏览器工具
VNC 远程桌面
MCP / A2A / 多 Agent
长期记忆
Harness 和系统诊断
```

​        这些能力越接近真实 Agent 产品，安全边界就越重要。

​        一个普通 CRUD 系统通常主要关注：

```Plain
登录
权限
数据库
接口参数
```

​        但 Agent 系统还会多出几类风险：

```Plain
LLM 会读取用户输入
工具会调用外部系统
Shell 会执行命令
浏览器会访问网页
长期记忆会保存信息
多 Agent 会互相调用
Sandbox 会读写文件
```

​        如果没有统一安全清单，很容易出现这些问题：

​        展开来看，第一，本地开发能跑，生产环境却还在用默认数据库密码；第二，CORS 为了调试设置成 `*`，上线后没有改回来；第三，上传文件没有大小限制或路径边界；第四，Sandbox API 被直接暴露到公网；第五，Shell 命令没有超时、目录和输出限制；第六，长期记忆误保存 API Key、密码或隐私内容；第七，MCP/A2A 外部调用没有超时和禁用开关。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本章先做一个“上线前安全边界清单”。

​        注意，本章不是完整安全体系。

​        本章不做：

​        具体来说，第一，登录；第二，用户权限；第三，多租户隔离；第四，HTTPS 证书申请；第五，生产密钥管理系统；第六，完整文件杀毒。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些会出现在真实生产部署和后续扩展中。本章先把边界、风险和验证入口结构化，让后续优化有抓手。

## 48.4 本章技术方案

​        本章新增一个 `SecurityAuditService`。

​        它负责返回安全检查清单：

```Plain
SecurityAuditService
  |
  +-- configuration 配置检查
  +-- uploads 上传边界检查
  +-- sandbox 沙箱边界检查
  +-- integrations 外部集成检查
  +-- memory 长期记忆检查
```

​        每条检查包含：

```Plain
key              稳定标识
name             展示名称
category         分类
severity         info / warning / risk
risk             风险说明
recommendation   处理建议
verify_command   验证命令
```

​        为什么先做检查清单，而不是直接做完整鉴权？

​        因为本项目是课程项目，当前阶段仍然以本地和单机 Compose 为主。贸然加入完整登录、权限、多租户，会把主线从 Agent 工程变成认证系统。

​        更合适的节奏是：

```Plain
先明确边界 -> 再提供检查接口 -> 再在生产章节逐步收紧
```

​        本章新增和修改：

```Plain
README.md
api/README.md
ui/README.md
api/app/domain/security/__init__.py
api/app/domain/security/entities.py
api/app/application/security_audit_service.py
api/app/schemas/security.py
api/app/presentation/http/routes/security.py
api/app/presentation/http/router.py
api/tests/test_security_audit_service.py
ui/app/types.ts
ui/app/lib/security-api.ts
ui/app/components/security-panel.tsx
ui/app/components/settings-workspace.tsx
docs/course/chapters/48-security-sandbox-boundary.md
```

## 48.5 实施步骤
### 48.5.1 先写安全检查测试

​        创建 `api/tests/test_security_audit_service.py`：

```Python
import unittest
from types import SimpleNamespace

from app.application.security_audit_service import SecurityAuditService


class SecurityAuditServiceTest(unittest.TestCase):
    # ===================== 第1步：安全清单应覆盖 Agent 系统的主要边界 =====================
    def test_checks_cover_security_boundaries(self) -> None:
        settings = SimpleNamespace(
            api_env="development",
            cors_allow_origins=["http://localhost:8088"],
            database_url="postgresql+asyncpg://postgres:postgres@localhost:5432/atlas_agents",
            upload_dir="uploads",
            max_upload_size=10 * 1024 * 1024,
            max_file_preview_size=64 * 1024,
            sandbox_api_base_url="http://sandbox:8100/api",
            sandbox_api_timeout_seconds=30.0,
            sandbox_shell_wait_timeout_seconds=10.0,
            search_timeout_seconds=10.0,
            bing_search_api_key="",
            context_memory_limit=6,
            context_memory_candidate_limit=100,
        )
        service = SecurityAuditService(settings=settings)

        checks = service.list_checks()
        categories = {check.category for check in checks}

        self.assertIn("configuration", categories)
        self.assertIn("uploads", categories)
        self.assertIn("sandbox", categories)
        self.assertIn("integrations", categories)
        self.assertIn("memory", categories)

    # ===================== 第2步：每条安全检查都要说明风险、建议和验证命令 =====================
    def test_every_check_explains_risk_recommendation_and_verification(self) -> None:
        settings = SimpleNamespace(
            api_env="production",
            cors_allow_origins=["*"],
            database_url="postgresql+asyncpg://postgres:postgres@postgres:5432/atlas_agents",
            upload_dir="/tmp/atlas-uploads",
            max_upload_size=10 * 1024 * 1024,
            max_file_preview_size=64 * 1024,
            sandbox_api_base_url="http://sandbox:8100/api",
            sandbox_api_timeout_seconds=30.0,
            sandbox_shell_wait_timeout_seconds=10.0,
            search_timeout_seconds=10.0,
            bing_search_api_key="",
            context_memory_limit=6,
            context_memory_candidate_limit=100,
        )
        service = SecurityAuditService(settings=settings)

        for check in service.list_checks():
            self.assertTrue(check.risk)
            self.assertTrue(check.recommendation)
            self.assertTrue(check.verify_command)
            self.assertIn(check.severity, {"info", "warning", "risk"})

    # ===================== 第3步：生产环境默认密码和通配 CORS 应被标记为高风险 =====================
    def test_production_defaults_are_reported_as_risks(self) -> None:
        settings = SimpleNamespace(
            api_env="production",
            cors_allow_origins=["*"],
            database_url="postgresql+asyncpg://postgres:postgres@postgres:5432/atlas_agents",
            upload_dir="uploads",
            max_upload_size=10 * 1024 * 1024,
            max_file_preview_size=64 * 1024,
            sandbox_api_base_url="http://sandbox:8100/api",
            sandbox_api_timeout_seconds=30.0,
            sandbox_shell_wait_timeout_seconds=10.0,
            search_timeout_seconds=10.0,
            bing_search_api_key="",
            context_memory_limit=6,
            context_memory_candidate_limit=100,
        )
        service = SecurityAuditService(settings=settings)

        risk_keys = {
            check.key for check in service.list_checks() if check.severity == "risk"
        }

        self.assertIn("cors_origin_policy", risk_keys)
        self.assertIn("database_default_password", risk_keys)


if __name__ == "__main__":
    unittest.main()
```

​        先运行：

```Bash
cd api
uv run python -m unittest tests/test_security_audit_service.py -v
```

​        此时会失败，因为 `SecurityAuditService` 还没有创建。这是正常的 TDD 红灯阶段。

#### 48.5.1.1 代码讲解

​        这里没有直接使用真实 `.env`。

​        测试用 `SimpleNamespace` 构造了一个轻量配置对象，这样可以模拟：

​        换句话说，第一，development 环境；第二，production 环境；第三，CORS 为 `*`；第四，数据库默认密码仍然是 `postgres`。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这样测试不会依赖你本机真实配置，也不会误读真实密钥。

### 48.5.2 定义安全检查领域实体

​        创建 `api/app/domain/security/__init__.py`：

```Bash
touch api/app/domain/security/__init__.py
```

​        创建 `api/app/domain/security/entities.py`：

```Python
from dataclasses import dataclass


@dataclass(slots=True)
class SecurityCheck:
    """一条面向安全边界的检查项。"""

    key: str
    name: str
    category: str
    severity: str
    risk: str
    recommendation: str
    verify_command: str
```

#### 48.5.2.1 字段含义

​        从实现顺序看，第一，`key`：程序使用的稳定标识，前端渲染列表时也可以作为 `key`；第二，`name`：展示名称；第三，`category`：分类，例如 `configuration`、`sandbox`、`memory`；第四，`severity`：风险等级，取值为 `info`、`warning`、`risk`；第五，`risk`：这条边界如果不处理，会发生什么问题；第六，`recommendation`：推荐怎么修复或收紧；第七，`verify_command`：可以复制执行的验证命令。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 48.5.2.2 为什么放在 domain

​        `SecurityCheck` 不是数据库模型，也不是 HTTP 响应模型。

​        它描述的是项目内部对安全检查项的业务理解，所以放在 `domain/security/entities.py`。

### 48.5.3 实现 SecurityAuditService

​        创建 `api/app/application/security_audit_service.py`：

```Python
from app.core.config import Settings, settings
from app.domain.security.entities import SecurityCheck


class SecurityAuditService:
    """生成 API、Sandbox、上传、记忆和外部集成的安全边界清单。

    这个服务不负责“自动修复”安全问题。它的职责是把当前项目最容易被忽略
    的安全边界结构化返回给前端、curl 和教程验证步骤，方便上线前逐项排查。
    """

    def __init__(self, settings: Settings = settings) -> None:
        # ===================== 第1步：保存配置对象，便于测试注入不同环境 =====================
        self.settings = settings

    def list_checks(self) -> list[SecurityCheck]:
        """返回当前项目需要关注的安全检查清单。"""

        # ===================== 第2步：按边界类型聚合检查项 =====================
        checks: list[SecurityCheck] = []
        checks.extend(self._configuration_checks())
        checks.extend(self._upload_checks())
        checks.extend(self._sandbox_checks())
        checks.extend(self._integration_checks())
        checks.extend(self._memory_checks())
        return checks
```

​        继续在同一个文件中加入配置检查：

```Python
    def _configuration_checks(self) -> list[SecurityCheck]:
        # ===================== 第3步：检查 CORS 是否在生产环境过于开放 =====================
        cors_is_wildcard = "*" in self.settings.cors_allow_origins
        production = self.settings.api_env.lower() == "production"
        cors_severity = "risk" if production and cors_is_wildcard else "warning"

        checks = [
            SecurityCheck(
                key="cors_origin_policy",
                name="CORS 来源策略",
                category="configuration",
                severity=cors_severity,
                risk="CORS 过于开放会让任意网页调用 API，生产环境尤其危险。",
                recommendation=(
                    "生产环境只保留正式前端域名；本地开发可以保留 localhost。"
                ),
                verify_command="curl http://localhost:8088/api/security/checks",
            )
        ]

        # ===================== 第4步：检查数据库默认密码是否仍然存在 =====================
        database_url = self.settings.database_url
        uses_default_password = ":postgres@" in database_url
        checks.append(
            SecurityCheck(
                key="database_default_password",
                name="数据库默认密码",
                category="configuration",
                severity="risk" if production and uses_default_password else "warning",
                risk="默认数据库密码容易被猜到，不适合生产或公网环境。",
                recommendation=(
                    "在 .env 中替换 PostgreSQL 用户、密码和连接地址，并避免提交真实密钥。"
                ),
                verify_command="docker compose config | sed -n '/DATABASE_URL/p'",
            )
        )
        return checks
```

#### 48.5.3.1 业务讲解

​        安全检查不是只有“通过/不通过”。

​        同一个配置在本地开发和生产环境里的风险不一样：

```Plain
本地开发 CORS 允许 localhost    可以接受
生产环境 CORS 使用 *            高风险
本地数据库默认密码 postgres      可以短期接受
生产数据库默认密码 postgres      高风险
```

​        所以服务里先判断：

```Python
production = self.settings.api_env.lower() == "production"
```

​        再决定 `severity`。

​        继续在同一个文件中加入上传、Sandbox、外部集成和记忆检查：

```Python
    def _upload_checks(self) -> list[SecurityCheck]:
        # ===================== 第5步：检查上传目录和大小限制是否明确 =====================
        return [
            SecurityCheck(
                key="upload_size_limit",
                name="上传大小限制",
                category="uploads",
                severity="info",
                risk="没有大小限制时，恶意上传可能占满磁盘或拖慢文件预览。",
                recommendation=(
                    f"当前 max_upload_size={self.settings.max_upload_size} 字节；"
                    "生产环境应按业务场景设置更严格的限制。"
                ),
                verify_command="curl http://localhost:8088/api/config/app",
            ),
            SecurityCheck(
                key="upload_path_boundary",
                name="上传路径边界",
                category="uploads",
                severity="warning",
                risk="如果直接信任文件名或相对路径，可能出现目录穿越。",
                recommendation=(
                    f"上传根目录为 {self.settings.upload_dir}；保存文件时应生成服务端文件名，"
                    "并确保最终路径仍在上传根目录内。"
                ),
                verify_command="curl http://localhost:8088/api/security/checks",
            ),
        ]

    def _sandbox_checks(self) -> list[SecurityCheck]:
        # ===================== 第6步：检查 Sandbox API、Shell 和浏览器边界 =====================
        timeout_severity = (
            "risk" if self.settings.sandbox_api_timeout_seconds > 60 else "info"
        )
        return [
            SecurityCheck(
                key="sandbox_api_boundary",
                name="Sandbox API 边界",
                category="sandbox",
                severity="warning",
                risk="Sandbox 拥有文件、Shell、浏览器和 VNC 能力，不应直接暴露到公网。",
                recommendation=(
                    "只通过 Nginx 网关和主 API 访问 Sandbox；生产环境要叠加鉴权和网络隔离。"
                ),
                verify_command="curl http://localhost:8088/sandbox-api/status",
            ),
            SecurityCheck(
                key="sandbox_shell_timeout",
                name="Shell 命令超时",
                category="sandbox",
                severity=timeout_severity,
                risk="Shell 工具如果没有超时和输出限制，可能被长时间运行或输出洪泛拖垮。",
                recommendation=(
                    f"当前等待超时为 {self.settings.sandbox_shell_wait_timeout_seconds} 秒；"
                    "后续工具执行应继续限制命令白名单、工作目录和输出长度。"
                ),
                verify_command="curl http://localhost:8088/sandbox-api/shell/sessions",
            ),
        ]

    def _integration_checks(self) -> list[SecurityCheck]:
        # ===================== 第7步：检查外部搜索、MCP 和 A2A 调用的安全边界 =====================
        search_has_key = bool(self.settings.bing_search_api_key.strip())
        return [
            SecurityCheck(
                key="external_search_key",
                name="外部搜索密钥",
                category="integrations",
                severity="info" if search_has_key else "warning",
                risk="外部搜索密钥缺失时搜索工具不可用；密钥泄露时会带来额度和数据风险。",
                recommendation=(
                    "把搜索密钥放在 .env 或密钥管理系统中，不要写入代码和教程示例。"
                ),
                verify_command="curl http://localhost:8088/api/config/llm",
            ),
            SecurityCheck(
                key="mcp_a2a_timeout_and_disable",
                name="MCP/A2A 超时和禁用开关",
                category="integrations",
                severity="warning",
                risk="外部 Agent 或 MCP Server 如果无超时、无禁用开关，可能拖慢主任务。",
                recommendation=(
                    f"当前搜索超时为 {self.settings.search_timeout_seconds} 秒；"
                    "外部工具应统一设置超时、错误事件和启用开关。"
                ),
                verify_command="curl http://localhost:8088/api/config/app",
            ),
        ]

    def _memory_checks(self) -> list[SecurityCheck]:
        # ===================== 第8步：检查长期记忆的敏感信息处理边界 =====================
        return [
            SecurityCheck(
                key="memory_sensitive_filter",
                name="长期记忆敏感信息过滤",
                category="memory",
                severity="warning",
                risk="长期记忆可能保存用户偏好、项目事实和任务经验，也可能误收集密钥或隐私。",
                recommendation=(
                    "写入长期记忆前应过滤 API Key、密码、Token、身份证号等敏感信息。"
                ),
                verify_command="curl http://localhost:8088/api/memories",
            ),
            SecurityCheck(
                key="memory_disable_and_delete",
                name="记忆禁用和删除机制",
                category="memory",
                severity="info",
                risk="用户需要能禁用、删除或过期长期记忆，否则错误记忆会持续影响 Agent。",
                recommendation=(
                    f"当前最多注入 {self.settings.context_memory_limit} 条记忆；"
                    "保留 enabled、delete 和 expires_at 机制。"
                ),
                verify_command="curl http://localhost:8088/api/memories",
            ),
        ]
```

#### 48.5.3.2 上传边界讲解

​        上传文件至少要关注三件事：

```Plain
文件大小
文件类型
保存路径
```

​        本项目当前已经有大小限制配置：

```Python
max_upload_size
max_file_preview_size
```

​        但路径边界仍然要在教程里强调。

​        不要直接相信用户传来的文件名。例如：

```Plain
../../.env
```

​        这类路径如果没有检查，可能写到上传目录外面。

​        生产系统通常会做：

​        放到工程语境里看，第一，服务端生成文件 ID；第二，文件名只作为展示字段；第三，保存前解析最终路径；第四，确认最终路径仍然位于上传根目录内。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 48.5.4 新增 API Schema 和路由

​        创建 `api/app/schemas/security.py`：

```Python
from pydantic import BaseModel


class SecurityCheckResponse(BaseModel):
    key: str
    name: str
    category: str
    severity: str
    risk: str
    recommendation: str
    verify_command: str


class SecurityCheckListResponse(BaseModel):
    items: list[SecurityCheckResponse]
```

​        创建 `api/app/presentation/http/routes/security.py`：

```Python
from fastapi import APIRouter, Depends

from app.application.security_audit_service import SecurityAuditService
from app.domain.security.entities import SecurityCheck
from app.schemas.common import ApiResponse
from app.schemas.security import SecurityCheckListResponse, SecurityCheckResponse

router = APIRouter(prefix="/security", tags=["security"])


def build_security_audit_service() -> SecurityAuditService:
    return SecurityAuditService()


def to_security_check_response(check: SecurityCheck) -> SecurityCheckResponse:
    return SecurityCheckResponse(
        key=check.key,
        name=check.name,
        category=check.category,
        severity=check.severity,
        risk=check.risk,
        recommendation=check.recommendation,
        verify_command=check.verify_command,
    )


@router.get("/checks", response_model=ApiResponse[SecurityCheckListResponse])
async def list_security_checks(
    service: SecurityAuditService = Depends(build_security_audit_service),
) -> ApiResponse[SecurityCheckListResponse]:
    # ===================== 第1步：读取安全边界检查清单 =====================
    checks = service.list_checks()

    # ===================== 第2步：转换成统一响应结构，交给前端和 curl 使用 =====================
    return ApiResponse(
        data=SecurityCheckListResponse(
            items=[to_security_check_response(check) for check in checks],
        )
    )
```

​        打开 `api/app/presentation/http/router.py`，注册路由：

```Python
from app.presentation.http.routes import (
    a2a,
    agent_core,
    agent_thinking,
    config,
    files,
    harness,
    llm,
    mcp,
    memories,
    multi_agent,
    observability,
    sandboxes,
    security,
    sessions,
    status,
)

api_router.include_router(security.router)
```

#### 48.5.4.1 为什么路由层不做判断

​        路由层应该很薄。

​        它只负责：

```Plain
接收请求
调用服务
转换响应
返回结果
```

​        如果把 CORS、数据库密码、Sandbox 边界判断写在路由里，后续测试和复用会变困难。

### 48.5.5 前端新增安全类型和请求函数

​        打开 `ui/app/types.ts`，新增：

```TypeScript
export type SecurityCheckItem = {
  key: string;
  name: string;
  category: string; // configuration、uploads、sandbox、integrations、memory。
  severity: "info" | "warning" | "risk"; // 风险等级，用于决定前端徽标颜色。
  risk: string; // 不处理这条边界会带来什么风险。
  recommendation: string; // 推荐的配置、架构或上线前处理方式。
  verify_command: string; // 可复制执行的验证命令。
};

export type SecurityCheckListData = {
  items: SecurityCheckItem[];
};
```

​        创建 `ui/app/lib/security-api.ts`：

```TypeScript
import { requestApi } from "./api";
import type { SecurityCheckListData } from "../types";


export function fetchSecurityChecks(): Promise<SecurityCheckListData> {
  return requestApi<SecurityCheckListData>("/api/security/checks");
}
```

#### 48.5.5.1 代码讲解

​        前端组件不直接写：

```TypeScript
fetch("/api/security/checks")
```

​        而是通过 `security-api.ts` 封装。

​        这样做有两个好处：

​        展开来看，第一，组件只关心展示，不关心接口路径；第二，后续如果接口从 `/api/security/checks` 改成其他路径，只改一个文件。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

### 48.5.6 新增安全边界面板

​        创建 `ui/app/components/security-panel.tsx`。

​        这个组件负责：

​        具体来说，第一，加载安全检查清单；第二，展示 loading、error、ready 三种状态；第三，按风险等级展示徽标；第四，支持复制验证命令。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        核心代码如下：

```TypeScript
"use client";

import { Copy, RefreshCcw, ShieldAlert, ShieldCheck, TriangleAlert } from "lucide-react";
import { useEffect, useState } from "react";

import { fetchSecurityChecks } from "../lib/security-api";
import type {
  LoadState,
  SecurityCheckItem,
  SecurityCheckListData,
} from "../types";


// ===================== 第1步：展示安全边界检查入口 =====================
export function SecurityPanel() {
  const [checks, setChecks] = useState<LoadState<SecurityCheckListData>>({
    type: "loading",
  });

  async function loadChecks() {
    // 1. 每次刷新先进入 loading，避免用户看到旧数据还以为已经更新。
    setChecks({ type: "loading" });
    try {
      // 2. 从主 API 读取结构化安全检查清单。
      const data = await fetchSecurityChecks();
      setChecks({ type: "ready", data });
    } catch (error) {
      // 3. 请求失败时只展示错误文案，不让整个设置页崩溃。
      const message = error instanceof Error ? error.message : "unknown error";
      setChecks({ type: "error", message });
    }
  }

  useEffect(() => {
    loadChecks();
  }, []);

  return (
    <section className="rounded-md border border-slate-200 bg-white p-5">
      <div className="flex items-start justify-between gap-4">
        <div>
          <h3 className="flex items-center gap-2 text-base font-semibold text-slate-950">
            <ShieldAlert size={18} aria-hidden="true" />
            安全边界
          </h3>
          <p className="mt-1 text-sm leading-6 text-slate-500">
            检查配置、上传、沙箱、外部集成和长期记忆的上线风险
          </p>
        </div>
        <button
          className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-slate-200 bg-white text-slate-600 hover:bg-slate-50"
          onClick={loadChecks}
          title="刷新安全清单"
          type="button"
        >
          <RefreshCcw size={16} aria-hidden="true" />
        </button>
      </div>

      {checks.type === "loading" ? (
        <div className="mt-4 rounded-md border border-slate-200 bg-slate-50 p-3 text-sm text-slate-500">
          正在读取安全检查清单...
        </div>
      ) : null}

      {checks.type === "error" ? (
        <div className="mt-4 rounded-md border border-rose-200 bg-rose-50 p-3 text-sm text-rose-700">
          {checks.message}
        </div>
      ) : null}

      {checks.type === "ready" ? (
        <div className="mt-4 grid gap-3">
          {checks.data.items.map((item) => (
            <SecurityCheckCard item={item} key={item.key} />
          ))}
        </div>
      ) : null}
    </section>
  );
}
```

#### 48.5.6.1 业务讲解

​        这个面板和“系统诊断”面板很像，但关注点不同。

```Plain
系统诊断：系统坏了怎么排查
安全边界：上线前哪些风险要收紧
```

​        安全检查结果不一定都是错误。

​        例如 `info` 只是提示当前配置值得关注；`warning` 表示需要确认；`risk` 表示生产环境必须处理。

### 48.5.7 接入设置页

​        打开 `ui/app/components/settings-workspace.tsx`，导入：

```TypeScript
import { SecurityPanel } from "./security-panel";
```

​        在 `SettingsReadyView` 中，把安全面板放在系统诊断后面：

```TypeScript
<MemorySettingsPanel />
<HarnessPanel />
<ObservabilityPanel />
<SecurityPanel />
```

#### 48.5.7.1 为什么放在设置页

​        安全边界不是一次聊天里的内容。

​        它更像项目级配置和上线检查，所以放在“设置”页，比放在聊天工作台更合适。

## 48.6 关键理解

​        本章最重要的是理解“安全边界不是某一个功能点”。

​        Agent 项目有很多可执行能力：

```Plain
Shell
Browser
VNC
MCP
A2A
Search
Memory
File Upload
```

​        每一项都要问三个问题：

```Plain
谁能调用？
调用范围在哪里？
失败或滥用时如何停止？
```

​        第二个重点是理解 Sandbox 边界。

​        Sandbox 的职责是隔离执行环境，但 Sandbox 不是天然安全。

​        如果把 Sandbox API、VNC 或 Shell 端口直接暴露到公网，隔离意义会大幅下降。

​        正确方向是：

```Plain
浏览器 -> Nginx -> 主 API -> 受控调用 Sandbox
```

​        第三个重点是长期记忆。

​        长期记忆提高 Agent 连续使用体验，但也会带来持久化风险。

​        所以必须保留：

​        换句话说，第一，禁用；第二，删除；第三，过期；第四，敏感信息过滤；第五，来源追溯。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.7 技术难点与亮点

​        技术难点：

​        从实现顺序看，第一，安全检查要能被测试，不能只写成文档说明；第二，同一个配置在本地和生产环境风险不同；第三，Sandbox 既要提供强能力，又要避免直接暴露；第四，长期记忆需要在“有用”和“敏感”之间做边界管理。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        放到工程语境里看，第一，安全边界进入真实 API 和设置页，不再停留在口头提醒；第二，每条检查都包含风险、建议和验证命令；第三，使用测试确保安全清单覆盖关键边界；第四，为后续生产鉴权、HTTPS、密钥管理和多用户隔离预留入口。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.8 面试考点

​        展开来看，第一，Agent 项目和普通 Web 项目的安全风险有什么不同？；第二，为什么 Sandbox 不应该直接暴露给公网？；第三，为什么 Shell 工具必须有超时和输出限制？；第四，上传文件为什么要检查路径边界？；第五，长期记忆为什么需要禁用、删除和敏感信息过滤？；第六，为什么安全检查服务要支持测试注入配置？。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.9 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

### 48.9.1 运行第 48 章后端测试

```Bash
cd api
uv run python -m unittest tests/test_security_audit_service.py -v
```

​        预期看到：

```Plain
Ran 3 tests
OK
```

### 48.9.2 编译后端代码

```Bash
uv run python -m compileall app
```

​        预期没有 Python 语法错误。

### 48.9.3 检查前端类型

```Bash
cd ../ui
pnpm typecheck
```

​        预期没有 TypeScript 报错。

### 48.9.4 Docker 构建和启动

​        本章改了 API 和 UI，建议重新构建镜像验证：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
BUILD=true ./scripts/start.sh
```

​        如果镜像已经是最新，也可以只启动：

```Bash
./scripts/start.sh
```

### 48.9.5 验证安全检查接口

```Bash
curl http://localhost:8088/api/security/checks
```

​        预期返回：

```Plain
cors_origin_policy
database_default_password
upload_size_limit
upload_path_boundary
sandbox_api_boundary
sandbox_shell_timeout
external_search_key
mcp_a2a_timeout_and_disable
memory_sensitive_filter
memory_disable_and_delete
```

### 48.9.6 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        操作：

​        具体来说，第一，点击左侧“设置”；第二，确认页面中出现“安全边界”；第三，确认能看到 `高风险`、`注意` 或 `提示` 徽标；第四，点击刷新按钮；第五，点击某条检查的复制按钮，再粘贴到终端确认命令可用。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 48.10 常见问题

- 问题：`/api/security/checks` 返回 404 怎么办？

​        解释：通常是 API 镜像或容器还没更新。执行 `BUILD=true ./scripts/start.sh`，或至少重启 API 和 Nginx。

- 问题：安全面板显示错误怎么办？

​        解释：先执行 `curl http://localhost:8088/api/security/checks`。如果接口正常，检查浏览器控制台；如果接口异常，查看 `docker compose logs --tail=80 api`。

- 问题：为什么有些检查只是 `warning`，不是直接报错？

​        解释：本地开发和生产环境的要求不同。本章先做上线前提示，不直接阻止系统运行。

- 问题：为什么本章不做登录和权限？

​        解释：登录和权限是完整生产安全体系的一部分。本章目标是先把 Agent 特有的执行边界、沙箱边界和记忆边界结构化。

- 问题：为什么安全检查接口不主动连接 Sandbox 或外部服务？

​        解释：它是配置和边界检查，不是健康探测。健康探测已经放在第 47 章系统诊断里。

## 48.11 本章小结

​        本章完成了安全边界清单：

​        换句话说，第一，新增 `SecurityCheck` 领域实体；第二，新增 `SecurityAuditService`；第三，新增 `/api/security/checks` 接口；第四，新增安全检查测试；第五，前端设置页新增“安全边界”面板；第六，检查配置、上传、Sandbox、外部集成和长期记忆风险。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一章开始，项目不再只关注“能不能跑”，也开始关注“能不能更稳地走向生产环境”。

## 48.12 下一章预告

​        第 49 章会做成熟 Agent 产品体验验收，对话、计划、工具调用、预览、记忆、多 Agent、Harness 和浏览器自动化会放到一起做端到端检查。
