# 第三十五章. A2A 工具入列

## 35.1 本章目标

​        学完本章后，你将能够：

​        放到工程语境里看，第一，理解 A2A 工具接入和第 34 章概念演示的区别；第二，使用 YAML 配置声明可调用的远程 Agent；第三，编写 A2A Client Manager，把 demo/http 两种调用方式封装起来；第四，编写 `A2aService`，让 HTTP API 和 AgentTool 共用同一套应用逻辑；第五，把远程 Agent 注册成 `a2a_call` 工具；第六，让计划执行命中 A2A 工具，并产生 `tool_called` 事件；第七，在前端工具预览中展示 A2A 远程 Agent 的任务结果。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 35.2 最终效果

​        本章结束后，后端会新增真实 A2A 接入接口：

```Plain
GET  /api/a2a/agents
GET  /api/a2a/agents/{agent_key}/card
POST /api/a2a/agent-card
POST /api/a2a/message/send
```

​        Agent 工具列表会新增：

```Plain
a2a_call
```

​        在页面里创建会话，发送任务：

```Plain
请调用远程研究 Agent，帮我整理 A2A 工具接入要点
```

​        然后点击：

```Plain
创建计划 -> 执行计划
```

​        预期结果：

​        展开来看，第一，事件记录中出现 `tool_called`；第二，工具名显示 `a2a_call`；第三，右侧工具预览显示 A2A 远程 Agent、task ID、状态、协作步骤和远程输出。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 35.3 本章要解决的问题

​        第 34 章只是解释 A2A 的概念：

```Plain
Host Agent
Remote Agent
Agent Card
message/send
Task
```

​        但它还没有进入真实 Agent 执行链路。

​        真实产品里，A2A 不能只是一个单独的演示接口。它应该像文件、Shell、浏览器、搜索和 MCP 一样，进入当前会话的工具系统：

```Plain
用户任务
  |
  v
Planner 生成计划
  |
  v
ReAct 执行步骤
  |
  v
调用 a2a_call
  |
  v
写入 tool_called 事件
  |
  v
前端工具预览展示远程 Agent 结果
```

​        所以本章要把 A2A 从“概念演示”推进到“可调用工具”。

## 35.4 本章技术方案

​        本章沿用第 32 章 MCP 工具接入的思路，但注意 A2A 和 MCP 的定位不同。

​        MCP 更像：

```Plain
Agent -> Tool Server -> Tool Result
```

​        A2A 更像：

```Plain
Agent -> Remote Agent -> Task Result
```

​        本章新增分层：

```Plain
config/a2a.yaml
  |
  v
core/a2a_config.py
  |
  v
infrastructure/a2a/client.py
  |
  v
application/a2a_service.py
  |
  +-- HTTP Route
  |
  +-- AgentTool a2a_call
  |
  v
session_events.tool_called
```

​        本章先实现两种 transport：

​        具体来说，第一，`demo`：内置远程 Agent 模拟器，不需要启动外部服务；第二，`http`：真实远程 Agent HTTP 接入示例，约定 `/agent-card` 和 `/message/send`。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        本章暂时不做这些内容：

​        换句话说，第一，不实现完整 A2A 标准里的所有任务生命周期；第二，不做远程 Agent 鉴权；第三，不做多 Agent 编排；第四，不做长期任务轮询；第五，不做前端设置页增删 A2A Agent。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些会在第 36、37 章继续补齐。

## 35.5 新增和修改的文件

```Plain
.env.example
README.md
api/README.md
docker-compose.yml
api/config/a2a.yaml
api/app/core/config.py
api/app/core/a2a_config.py
api/app/domain/a2a/entities.py
api/app/infrastructure/a2a/__init__.py
api/app/infrastructure/a2a/client.py
api/app/application/a2a_service.py
api/app/infrastructure/agent_tools/a2a.py
api/app/infrastructure/agent_tools/builtin.py
api/app/application/react_agent_service.py
api/app/schemas/a2a.py
api/app/presentation/http/routes/a2a.py
ui/app/types.ts
ui/app/lib/a2a-api.ts
ui/app/components/a2a-panel.tsx
ui/app/components/tool-preview-panel.tsx
ui/app/components/chat-workspace.tsx
ui/app/page.tsx
docs/course/chapters/35-a2a-tool.md
```

## 35.6 实施步骤
### 35.6.1 新增 A2A 配置文件

​        创建 `api/config/a2a.yaml`。

​        完整代码如下：

```YAML
a2a:
  enabled: true
  default_agent: demo_researcher

agents:
  demo_researcher:
    enabled: true
    transport: demo
    name: ResearchAssistantAgent
    description: "内置 A2A 演示 Agent，用于第 35 章验证远程 Agent 调用链路。"
    url: "demo://research-assistant"
    version: "0.1.0"
    timeout_seconds: 10
    capabilities:
      - name: research_brief
        description: "阅读任务文本，返回一段结构化研究摘要。"
        input_modes:
          - text
        output_modes:
          - text
      - name: source_outline
        description: "根据主题生成资料收集大纲。"
        input_modes:
          - text
        output_modes:
          - text

  example_http:
    enabled: false
    transport: http
    name: ExternalAgent
    description: "真实 A2A HTTP Agent 配置示例。启用前需要替换成真实地址。"
    url: "http://localhost:9000"
    version: "0.1.0"
    timeout_seconds: 15
    capabilities:
      - name: remote_task
        description: "远程 Agent 暴露的任务能力示例。"
        input_modes:
          - text
        output_modes:
          - text
```

#### 35.6.1.1 代码讲解

​        `a2a.default_agent` 表示默认调用哪个远程 Agent。

​        `agents.demo_researcher` 是课程内置演示 Agent。它的 `transport` 是 `demo`，说明它不会真的发网络请求，而是走后端内置模拟器。

​        `agents.example_http` 是真实远程 Agent 的配置示例。它默认 `enabled: false`，因为本地环境还没有启动这个外部服务。

#### 35.6.1.2 为什么这样设计

​        远程 Agent 不应该写死在代码里。

​        如果以后要增加一个代码审查 Agent、资料检索 Agent 或 PPT 生成 Agent，应该优先改配置，而不是到工具注册表里手写新的逻辑。

### 35.6.2 读取 A2A 配置

​        打开 `api/app/core/config.py`，增加：

```Python
a2a_config_path: str = "config/a2a.yaml"
```

​        创建 `api/app/core/a2a_config.py`。

​        完整代码如下：

```Python
from functools import lru_cache
from pathlib import Path
from typing import Literal

import yaml
from pydantic import BaseModel, Field, model_validator

from app.core.config import settings
from app.core.exceptions import AppException


A2aTransport = Literal["demo", "http"]


# ===================== 第1步：定义远程 Agent 能力配置 =====================
class A2aCapabilityConfig(BaseModel):
    """远程 Agent 在 Agent Card 中声明的一项能力。"""

    name: str
    description: str = ""
    input_modes: list[str] = Field(default_factory=lambda: ["text"])
    output_modes: list[str] = Field(default_factory=lambda: ["text"])


# ===================== 第2步：定义单个远程 Agent 配置 =====================
class A2aAgentConfig(BaseModel):
    """一个可被 Host Agent 调用的远程 Agent 配置。"""

    enabled: bool = True
    transport: A2aTransport
    name: str
    description: str = ""
    url: str
    version: str = "0.1.0"
    timeout_seconds: float = Field(default=10.0, gt=0)
    capabilities: list[A2aCapabilityConfig] = Field(default_factory=list)

    @model_validator(mode="after")
    def validate_transport_fields(self) -> "A2aAgentConfig":
        """根据 transport 校验必需字段。

        demo 使用内置实现，url 只作为展示字段。http 需要真实远程地址，
        第 35 章先约定远程 Agent 暴露 /agent-card 和 /message/send 两个接口。
        """

        if self.transport == "http" and not self.url.startswith(("http://", "https://")):
            raise ValueError("http A2A agent requires http or https url")
        return self


# ===================== 第3步：定义 A2A 总配置 =====================
class A2aDefaults(BaseModel):
    enabled: bool = True
    default_agent: str = "demo_researcher"


class A2aConfig(BaseModel):
    a2a: A2aDefaults
    agents: dict[str, A2aAgentConfig]


# ===================== 第4步：读取并校验 A2A 配置文件 =====================
@lru_cache
def load_a2a_config() -> A2aConfig:
    """读取 A2A YAML 配置。

    这样新增远程 Agent 时，只需要修改配置文件并重启 API。
    工具注册、路由和前端面板都可以从同一份配置中读取能力。
    """

    path = Path(settings.a2a_config_path)
    if not path.is_file():
        raise AppException(
            message=f"A2A config file not found: {settings.a2a_config_path}",
            code=500,
            status_code=500,
        )

    raw_config = yaml.safe_load(path.read_text(encoding="utf-8")) or {}
    config = A2aConfig.model_validate(raw_config)
    if config.a2a.default_agent not in config.agents:
        raise AppException(
            message="default A2A agent is not defined",
            code=500,
            status_code=500,
        )
    return config
```

#### 35.6.2.1 代码讲解

​        `A2aCapabilityConfig` 对应 Agent Card 中的一项能力。

​        `A2aAgentConfig` 对应一个远程 Agent。这里包含：

​        从实现顺序看，第一，是否启用；第二，transport 类型；第三，Agent 名称；第四，Agent 地址；第五，超时时间；第六，能力列表。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        `load_a2a_config()` 使用 `@lru_cache`，表示配置文件只读取一次。这样每次请求不用重复读 YAML。

#### 35.6.2.2 小白容易困惑的点

​        `demo://research-assistant` 不是一个真实网络地址。

​        它只是一个展示地址，用来说明这个 Agent 走的是内置 demo transport。

​        真实 HTTP Agent 应该使用：

```Plain
http://...
https://...
```

### 35.6.3 扩展 A2A 领域对象

​        打开 `api/app/domain/a2a/entities.py`，新增：

```Python
@dataclass(slots=True)
class A2aRemoteAgentInfo:
    key: str
    enabled: bool
    transport: str
    name: str
    description: str
    url: str
    version: str
    capabilities: list[A2aCapability]


@dataclass(slots=True)
class A2aTaskResult:
    agent_key: str
    remote_agent: str
    task_id: str
    status: str
    input_message: list[A2aMessagePart]
    output_message: list[A2aMessagePart]
    steps: list[A2aTaskStep]
```

#### 35.6.3.1 代码讲解

​        `A2aRemoteAgentInfo` 用来展示配置中的远程 Agent 状态。

​        `A2aTaskResult` 用来表示一次远程 Agent 调用结果。

​        注意这里没有直接返回 dict。领域层用明确的数据结构，后续 API、工具和前端都能更稳定地消费。

### 35.6.4 实现 A2A Client Manager

​        创建 `api/app/infrastructure/a2a/client.py`。

​        核心代码如下：

```Python
import uuid
from abc import ABC, abstractmethod
from typing import Any

import httpx

from app.core.a2a_config import A2aAgentConfig, load_a2a_config
from app.core.exceptions import AppException
from app.domain.a2a.entities import (
    A2aAgentCard,
    A2aCapability,
    A2aMessagePart,
    A2aRemoteAgentInfo,
    A2aTaskResult,
    A2aTaskStep,
)


class A2aTransportClient(ABC):
    """A2A 传输层客户端基类。

    上层应用服务只关心“列出 Agent Card”和“发送任务消息”。
    具体是内置 demo、HTTP 远程服务，还是后续更完整的 A2A SDK，
    都封装在 transport client 中。
    """

    def __init__(self, agent_key: str, config: A2aAgentConfig) -> None:
        self.agent_key = agent_key
        self.config = config

    @abstractmethod
    def get_agent_card(self) -> A2aAgentCard:
        """读取远程 Agent Card。"""

    @abstractmethod
    def send_message(self, message: str) -> A2aTaskResult:
        """向远程 Agent 发送一条任务消息。"""


class DemoA2aTransportClient(A2aTransportClient):
    """内置 A2A 演示客户端。

    它模拟真实远程 Agent 的 Agent Card 和 message/send 响应，
    不需要额外启动服务，适合课程稳定验证完整工具事件链路。
    """

    def get_agent_card(self) -> A2aAgentCard:
        # 1. demo transport 不发网络请求，直接把配置转换成 Agent Card。
        #    这样本地环境不用启动远程服务，也能跑通 Host 读取 Agent Card 的流程。
        return _agent_card_from_config(self.config)

    def send_message(self, message: str) -> A2aTaskResult:
        # 1. 清理用户任务文本。远程 Agent 不应该收到空消息。
        clean_message = message.strip()
        if not clean_message:
            raise AppException(
                message="A2A message is required",
                code=400,
                status_code=400,
            )

        # 2. 为本次远程调用生成一个模拟 task_id。
        #    真实 A2A 服务通常会返回自己的任务 ID，本章用 uuid 模拟。
        task_id = f"a2a-task-{uuid.uuid4()}"

        # 3. 组装统一的 A2aTaskResult。
        #    ReAct 工具和 HTTP API 都依赖这个结构，所以这里不直接返回 dict。
        return A2aTaskResult(
            agent_key=self.agent_key,
            remote_agent=self.config.name,
            task_id=task_id,
            status="completed",
            input_message=[A2aMessagePart(kind="text", text=clean_message)],
            output_message=[
                A2aMessagePart(
                    kind="text",
                    text=(
                        f"{self.config.name} 已完成远程协作："
                        f"围绕“{clean_message}”整理了摘要、关键点和下一步建议。"
                    ),
                )
            ],
            steps=[
                A2aTaskStep(
                    index=1,
                    action="agent_card/read",
                    detail=f"读取远程 Agent Card：{self.config.name}。",
                ),
                A2aTaskStep(
                    index=2,
                    action="message/send",
                    detail="Host Agent 把用户任务包装成 A2A message 并发送。",
                ),
                A2aTaskStep(
                    index=3,
                    action="task/completed",
                    detail="远程 Agent 返回 completed 状态和输出消息。",
                ),
            ],
        )


class HttpA2aTransportClient(A2aTransportClient):
    """最小 HTTP A2A 客户端。

    本章先约定远程服务暴露两个 HTTP 接口：
    GET /agent-card 和 POST /message/send。
    后续如果接入更完整 A2A 协议，可以继续替换这里的传输细节。
    """

    def get_agent_card(self) -> A2aAgentCard:
        # 1. 向远程 Agent 读取 Agent Card。
        #    本章约定真实 HTTP Agent 暴露 GET /agent-card。
        payload = self._request("GET", "/agent-card")

        # 2. 把远程响应里的 capabilities 转成领域对象。
        #    如果远程字段缺失，使用空字符串兜底，避免前端渲染崩掉。
        capabilities = [
            A2aCapability(
                name=str(item.get("name") or ""),
                description=str(item.get("description") or ""),
                example=str(item.get("example") or item.get("description") or ""),
            )
            for item in payload.get("capabilities", [])
            if isinstance(item, dict)
        ]

        # 3. 返回统一 Agent Card。
        #    远程没返回的基础字段，使用配置文件中的值补齐。
        return A2aAgentCard(
            name=str(payload.get("name") or self.config.name),
            description=str(payload.get("description") or self.config.description),
            url=str(payload.get("url") or self.config.url),
            version=str(payload.get("version") or self.config.version),
            capabilities=capabilities,
            default_input_modes=list(payload.get("default_input_modes") or ["text"]),
            default_output_modes=list(payload.get("default_output_modes") or ["text"]),
        )

    def send_message(self, message: str) -> A2aTaskResult:
        # 1. 调用远程 Agent 的 message/send。
        #    本章先约定请求体只有 message，后续可扩展 artifacts、metadata 等字段。
        payload = self._request("POST", "/message/send", json={"message": message})

        # 2. 解析远程输入输出消息。
        #    远程服务可能不返回 input_message，所以 fallback 使用本次发送的 message。
        input_message = _message_parts_from_payload(payload.get("input_message"), message)
        output_message = _message_parts_from_payload(payload.get("output_message"), "")

        # 3. 解析远程执行步骤。
        #    steps 会进入前端工具预览，帮助用户看到远程 Agent 做过哪些动作。
        steps = [
            A2aTaskStep(
                index=int(item.get("index") or index),
                action=str(item.get("action") or "remote_step"),
                detail=str(item.get("detail") or ""),
            )
            for index, item in enumerate(payload.get("steps", []), start=1)
            if isinstance(item, dict)
        ]

        # 4. 包装成统一任务结果。
        #    即使真实远程 Agent 字段不完整，主 API 也尽量返回稳定结构。
        return A2aTaskResult(
            agent_key=self.agent_key,
            remote_agent=str(payload.get("remote_agent") or self.config.name),
            task_id=str(payload.get("task_id") or f"a2a-task-{uuid.uuid4()}"),
            status=str(payload.get("status") or "completed"),
            input_message=input_message,
            output_message=output_message,
            steps=steps,
        )

    def _request(
        self,
        method: str,
        path: str,
        json: dict[str, Any] | None = None,
    ) -> dict[str, Any]:
        # 1. 根据配置里的 base url 拼出远程接口地址。
        url = f"{self.config.url.rstrip('/')}{path}"
        try:
            # 2. 发送 HTTP 请求，并使用配置里的 timeout 避免调用长期卡住。
            response = httpx.request(
                method,
                url,
                json=json,
                timeout=self.config.timeout_seconds,
            )
            # 3. 非 2xx 响应直接抛出，统一转成 AppException 给上层。
            response.raise_for_status()
        except httpx.HTTPError as exc:
            raise AppException(
                message=f"A2A HTTP request failed: {exc}",
                code=502,
                status_code=502,
            ) from exc

        # 4. 兼容两种响应格式：
        #    - 直接返回业务 JSON
        #    - 返回统一 ApiResponse，其中业务数据在 data 字段里
        payload = response.json()
        if isinstance(payload, dict) and "data" in payload and isinstance(payload["data"], dict):
            return payload["data"]
        return payload if isinstance(payload, dict) else {}


class A2aClientManager:
    """A2A Client 管理器。

    它读取 A2A 配置，按 agent_key 创建对应传输客户端，
    并向应用服务提供统一的 list / card / message 调用方法。
    """

    def __init__(self) -> None:
        # 1. 读取并缓存 A2A 配置。
        #    load_a2a_config 本身带 lru_cache，所以多次创建 manager 不会重复读文件。
        self.config = load_a2a_config()

    # ===================== 第1步：读取远程 Agent 配置列表 =====================
    def list_agents(self) -> list[A2aRemoteAgentInfo]:
        """返回全部配置过的远程 Agent。"""

        # 1. 遍历 YAML 中声明的 agents。
        # 2. 把配置模型转换成领域对象，供 API 和前端状态面板展示。
        return [
            A2aRemoteAgentInfo(
                key=key,
                enabled=agent.enabled,
                transport=agent.transport,
                name=agent.name,
                description=agent.description,
                url=agent.url,
                version=agent.version,
                capabilities=[
                    A2aCapability(
                        name=capability.name,
                        description=capability.description,
                        example=capability.description,
                    )
                    for capability in agent.capabilities
                ],
            )
            for key, agent in self.config.agents.items()
        ]

    # ===================== 第2步：读取 Agent Card =====================
    def get_agent_card(self, agent_key: str | None = None) -> A2aAgentCard:
        """读取指定远程 Agent 的 Agent Card。"""

        # 1. 如果调用方没有传 agent_key，就使用配置中的默认 Agent。
        key = agent_key or self.config.a2a.default_agent

        # 2. 校验 Agent 是否存在且启用，避免调用被禁用的外部服务。
        agent = self._get_enabled_agent(key)

        # 3. 根据 transport 创建 client，并读取 Agent Card。
        return self._build_transport(key, agent).get_agent_card()

    # ===================== 第3步：发送任务消息 =====================
    def send_message(self, agent_key: str | None, message: str) -> A2aTaskResult:
        """向指定远程 Agent 发送一条任务消息。"""

        # 1. 没有指定 agent_key 时使用默认远程 Agent。
        key = agent_key or self.config.a2a.default_agent

        # 2. 先校验配置状态，再创建对应 transport。
        agent = self._get_enabled_agent(key)

        # 3. 真正调用远程 Agent，并返回统一任务结果。
        return self._build_transport(key, agent).send_message(message)

    def _get_enabled_agent(self, agent_key: str) -> A2aAgentConfig:
        # 1. 检查配置中是否存在这个 Agent。
        agent = self.config.agents.get(agent_key)
        if agent is None:
            raise AppException(
                message=f"A2A agent not found: {agent_key}",
                code=404,
                status_code=404,
            )

        # 2. 检查是否启用。禁用的 Agent 不能被工具和 API 调用。
        if not agent.enabled:
            raise AppException(
                message=f"A2A agent is disabled: {agent_key}",
                code=400,
                status_code=400,
            )
        return agent

    def _build_transport(
        self,
        agent_key: str,
        agent: A2aAgentConfig,
    ) -> A2aTransportClient:
        # 1. demo transport 走内置模拟器，适合课程稳定验证。
        if agent.transport == "demo":
            return DemoA2aTransportClient(agent_key, agent)

        # 2. http transport 走真实远程 Agent 服务。
        if agent.transport == "http":
            return HttpA2aTransportClient(agent_key, agent)

        # 3. 配置里出现未知 transport 时返回明确错误。
        raise AppException(
            message=f"unsupported A2A transport: {agent.transport}",
            code=500,
            status_code=500,
        )


def build_a2a_client_manager() -> A2aClientManager:
    return A2aClientManager()


def _agent_card_from_config(config: A2aAgentConfig) -> A2aAgentCard:
    return A2aAgentCard(
        name=config.name,
        description=config.description,
        url=config.url,
        version=config.version,
        capabilities=[
            A2aCapability(
                name=capability.name,
                description=capability.description,
                example=capability.description,
            )
            for capability in config.capabilities
        ],
        default_input_modes=["text"],
        default_output_modes=["text"],
    )


def _message_parts_from_payload(value: object, fallback: str) -> list[A2aMessagePart]:
    if not isinstance(value, list):
        return [A2aMessagePart(kind="text", text=fallback)] if fallback else []
    parts: list[A2aMessagePart] = []
    for item in value:
        if isinstance(item, dict):
            parts.append(
                A2aMessagePart(
                    kind=str(item.get("kind") or item.get("type") or "text"),
                    text=str(item.get("text") or ""),
                )
            )
    return parts
```

#### 35.6.4.1 代码讲解

​        这个文件是本章后端最关键的部分。

​        `A2aTransportClient` 是抽象基类，它定义两个方法：

```Plain
get_agent_card()
send_message()
```

​        `DemoA2aTransportClient` 是内置演示实现。它不访问网络，直接返回确定性结果。

​        `HttpA2aTransportClient` 是真实 HTTP 接入示例。它约定远程 Agent 提供：

```Plain
GET  /agent-card
POST /message/send
```

​        `A2aClientManager` 负责：

​        放到工程语境里看，第一，读取配置；第二，列出远程 Agent；第三，根据 agent_key 找到配置；第四，根据 transport 创建对应 client；第五，对上层暴露统一方法。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 35.6.4.2 为什么要分 manager 和 transport client

​        因为“选择哪个远程 Agent”和“怎么调用这个远程 Agent”是两件事。

​        manager 负责选择和管理。

​        transport client 负责具体调用。

​        这样后续加入更多传输方式时，不需要改应用服务和路由。

### 35.6.5 实现 A2A 应用服务

​        创建 `api/app/application/a2a_service.py`。

​        完整代码如下：

```Python
from app.domain.a2a.entities import A2aAgentCard, A2aRemoteAgentInfo, A2aTaskResult
from app.infrastructure.a2a.client import A2aClientManager, build_a2a_client_manager


class A2aService:
    """真实 A2A 工具接入的应用服务。

    第 34 章只做概念演示；第 35 章开始，这个服务负责读取 A2A 配置、
    获取远程 Agent Card，并向远程 Agent 发送任务消息。
    """

    def __init__(self, manager: A2aClientManager | None = None) -> None:
        # 1. manager 负责传输层细节，service 负责应用层语义。
        self.manager = manager or build_a2a_client_manager()

    # ===================== 第1步：读取远程 Agent 配置状态 =====================
    def list_agents(self) -> list[A2aRemoteAgentInfo]:
        """返回所有配置过的远程 Agent。"""

        # 1. 这里不直接读取 YAML，而是委托 manager。
        #    service 只表达“列出远程 Agent”这个应用动作。
        return self.manager.list_agents()

    # ===================== 第2步：读取远程 Agent Card =====================
    def get_agent_card(self, agent_key: str | None = None) -> A2aAgentCard:
        """读取指定远程 Agent 的 Agent Card。"""

        # 1. agent_key 可以为空。为空时 manager 会使用配置里的默认 Agent。
        # 2. manager 负责选择 demo/http transport 并读取 Agent Card。
        return self.manager.get_agent_card(agent_key=agent_key)

    # ===================== 第3步：向远程 Agent 发送任务 =====================
    def send_message(self, agent_key: str | None, message: str) -> A2aTaskResult:
        """向远程 Agent 发送一条任务消息，并返回统一任务结果。"""

        # 1. Route 和 AgentTool 都会调用这个方法。
        # 2. 统一从这里进入 manager，避免两边各自实现一套远程调用逻辑。
        # 3. 返回 A2aTaskResult，供 API 响应或工具输出继续包装。
        return self.manager.send_message(agent_key=agent_key, message=message)
```

#### 35.6.5.1 代码讲解

​        `A2aService` 很薄，这是有意的。

​        它不关心 HTTP 细节，也不关心 demo 怎么模拟。它只表达应用层动作：

```Plain
list_agents()
get_agent_card()
send_message()
```

​        HTTP 路由和 AgentTool 都依赖这层服务，避免重复逻辑。

### 35.6.6 扩展 A2A API

​        打开 `api/app/schemas/a2a.py`，新增远程 Agent 列表、Agent Card 请求和任务结果响应模型。

​        关键字段包括：

```Plain
A2aRemoteAgentResponse
A2aRemoteAgentListResponse
A2aAgentCardRequest
A2aMessageInvokeRequest
A2aTaskResultResponse
```

​        打开 `api/app/presentation/http/routes/a2a.py`，新增：

```Plain
GET  /api/a2a/agents
GET  /api/a2a/agents/{agent_key}/card
POST /api/a2a/agent-card
POST /api/a2a/message/send
```

#### 35.6.6.1 业务流程

​        当调用：

```Plain
POST /api/a2a/message/send
```

​        后端流程是：

```Plain
A2A Route
  |
  v
A2aService.send_message()
  |
  v
A2aClientManager.send_message()
  |
  v
DemoA2aTransportClient 或 HttpA2aTransportClient
  |
  v
A2aTaskResult
```

### 35.6.7 把 A2A 注册成 AgentTool

​        创建 `api/app/infrastructure/agent_tools/a2a.py`。

​        完整代码如下：

```Python
import json

from app.application.a2a_service import A2aService
from app.domain.a2a.entities import A2aTaskResult
from app.domain.agent_core.tools import (
    AgentTool,
    ToolDefinition,
    ToolParameter,
    ToolRegistry,
)


def register_a2a_tools(
    registry: ToolRegistry,
    service: A2aService | None = None,
) -> None:
    """把 A2A 远程 Agent 调用入口注册成 AgentTool。

    第 35 章先注册一个通用 `a2a_call`。它通过 agent_key + message
    调用配置文件中的远程 Agent。后续多 Agent 编排章节会把不同角色
    组织成更完整的协作流程。
    """

    # 1. 创建或复用 A2A 应用服务。
    #    测试时可以传入 fake service，生产默认读取配置文件。
    a2a_service = service or A2aService()

    # 2. 注册一个通用 AgentTool。
    #    ReAct 执行计划时只需要知道工具名和参数 schema，不需要知道 A2A client 细节。
    registry.register(
        AgentTool(
            definition=ToolDefinition(
                name="a2a_call",
                description="调用一个已配置 A2A 远程 Agent 完成协作任务。",
                parameters=[
                    ToolParameter(
                        name="agent_key",
                        type="string",
                        description="A2A Agent 配置 key，例如 demo_researcher。",
                        required=False,
                    ),
                    ToolParameter(
                        name="message",
                        type="string",
                        description="要发送给远程 Agent 的任务消息。",
                    ),
                ],
            ),
            # 3. handler 是工具真正执行入口。
            #    AgentTool 会先校验参数，再把 agent_key/message 传给这里。
            handler=lambda agent_key="", message="": _format_a2a_result(
                a2a_service.send_message(
                    agent_key=str(agent_key or "") or None,
                    message=str(message),
                )
            ),
        )
    )


def _format_a2a_result(result: A2aTaskResult) -> str:
    """把 A2A 调用结果格式化成前端工具预览可识别的 JSON。"""

    # 1. 工具 output 目前是字符串，所以先把结构化结果序列化成 JSON。
    # 2. kind 是前端判断展示方式的关键字段；没有它就只能显示普通文本。
    # 3. input/output/steps 都保留下来，方便用户观察远程 Agent 的协作过程。
    return json.dumps(
        {
            "kind": "a2a_task_result",
            "agent_key": result.agent_key,
            "remote_agent": result.remote_agent,
            "task_id": result.task_id,
            "status": result.status,
            "input_message": [
                {"kind": part.kind, "text": part.text}
                for part in result.input_message
            ],
            "output_message": [
                {"kind": part.kind, "text": part.text}
                for part in result.output_message
            ],
            "steps": [
                {
                    "index": step.index,
                    "action": step.action,
                    "detail": step.detail,
                }
                for step in result.steps
            ],
        },
        ensure_ascii=False,
    )
```

​        打开 `api/app/infrastructure/agent_tools/builtin.py`，加入：

```Python
from app.infrastructure.agent_tools.a2a import register_a2a_tools
```

​        在 `build_builtin_tool_registry()` 里加入：

```Python
register_a2a_tools(registry)
```

#### 35.6.7.1 代码讲解

​        `a2a_call` 是本章新增工具。

​        它接收两个参数：

​        展开来看，第一，`agent_key`：要调用哪个远程 Agent；第二，`message`：发送给远程 Agent 的任务内容。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        工具输出不是普通文本，而是 JSON 字符串：

```JSON
{
  "kind": "a2a_task_result",
  "remote_agent": "ResearchAssistantAgent",
  "status": "completed"
}
```

​        前端会根据 `kind` 判断它是 A2A 结果，然后用专门卡片展示。

### 35.6.8 让计划执行能选择 A2A 工具

​        打开 `api/app/application/react_agent_service.py`。

​        在工具选择逻辑中加入：

```Python
if self._needs_a2a(text):
    tool = self.registry.get("a2a_call")
    arguments = {
        "agent_key": "demo_researcher",
        "message": self._extract_a2a_message(text),
    }
```

​        新增：

```Python
def _needs_a2a(self, text: str) -> bool:
    keywords = ["A2A", "a2a", "远程 Agent", "远程智能体", "协作 Agent"]
    return any(keyword in text for keyword in keywords)
```

#### 35.6.8.1 为什么本章还是关键词选择

​        因为本课程现在还在工具系统逐步搭建阶段。

​        第 43 章会进入更完整的工具选择策略。本章先用关键词规则，保证本地验证可以稳定复现 A2A 工具事件。

### 35.6.9 前端新增 A2A 请求函数

​        打开 `ui/app/lib/a2a-api.ts`。

​        完整代码如下：

```TypeScript
import { requestApi } from "./api";
import type {
  A2aAgentCardData,
  A2aConceptsData,
  A2aRemoteAgentListData,
} from "../types";


export function fetchA2aConcepts(): Promise<A2aConceptsData> {
  return requestApi<A2aConceptsData>("/api/a2a/concepts");
}


export function fetchA2aAgentCard(): Promise<A2aAgentCardData> {
  return requestApi<A2aAgentCardData>("/api/a2a/agents/demo_researcher/card");
}


export function fetchA2aAgents(): Promise<A2aRemoteAgentListData> {
  return requestApi<A2aRemoteAgentListData>("/api/a2a/agents");
}
```

#### 35.6.9.1 代码讲解

​        `fetchA2aConcepts()` 仍然读取第 34 章概念说明。

​        `fetchA2aAgentCard()` 改为读取真实配置中的 `demo_researcher` Agent Card。

​        `fetchA2aAgents()` 读取当前配置文件里所有远程 Agent。

### 35.6.10 升级 A2A 面板

​        打开 `ui/app/components/a2a-panel.tsx`。

​        完整代码如下：

```TypeScript
import { Network, RefreshCcw } from "lucide-react";

import type {
  A2aAgentCardData,
  A2aConceptsData,
  A2aRemoteAgentListData,
  LoadState,
} from "../types";

type A2aPanelProps = {
  agentCard: LoadState<A2aAgentCardData>;
  agents: LoadState<A2aRemoteAgentListData>;
  concepts: LoadState<A2aConceptsData>;
  onRefresh: () => void;
};


// ===================== 第1步：展示 A2A 远程 Agent 概念和示例 Agent Card =====================
export function A2aPanel({
  agentCard,
  agents,
  concepts,
  onRefresh,
}: A2aPanelProps) {
  return (
    <div className="rounded-md border border-slate-200 bg-white p-5">
      <div className="flex items-start justify-between gap-3">
        <div>
          <h2 className="flex items-center gap-2 text-base font-semibold text-slate-950">
            <Network size={17} aria-hidden="true" />
            A2A Agent
          </h2>
          <p className="mt-1 text-sm leading-5 text-slate-500">
            理解远程 Agent、Agent Card 和跨 Agent 任务消息
          </p>
        </div>
        <button
          className="inline-flex h-9 w-9 items-center justify-center rounded-md border border-slate-200 bg-white text-slate-600 hover:bg-slate-50"
          onClick={onRefresh}
          title="刷新 A2A"
          type="button"
        >
          <RefreshCcw size={16} aria-hidden="true" />
        </button>
      </div>

      <div className="mt-4 grid gap-4">
        <ConceptsView state={concepts} />
        <AgentListView state={agents} />
        <AgentCardView state={agentCard} />
      </div>
    </div>
  );
}


function ConceptsView({ state }: { state: LoadState<A2aConceptsData> }) {
  if (state.type === "loading") {
    return <p className="text-sm text-slate-500">正在读取 A2A 概念...</p>;
  }
  if (state.type === "error") {
    return <p className="text-sm text-rose-600">{state.message}</p>;
  }
  return (
    <div>
      <h3 className="text-sm font-semibold text-slate-900">核心角色</h3>
      <div className="mt-2 grid gap-2">
        {state.data.roles.map((role) => (
          <div
            className="rounded-md border border-slate-200 bg-slate-50 px-3 py-2"
            key={role.name}
          >
            <div className="text-sm font-medium text-slate-900">
              {role.name}
            </div>
            <p className="mt-1 text-xs leading-5 text-slate-600">
              {role.responsibility}
            </p>
            <p className="mt-1 text-xs leading-5 text-slate-500">
              {role.system_mapping}
            </p>
          </div>
        ))}
      </div>
      <p className="mt-2 text-xs leading-5 text-slate-500">
        下一步：{state.data.next_step}
      </p>
    </div>
  );
}


function AgentListView({ state }: { state: LoadState<A2aRemoteAgentListData> }) {
  if (state.type === "loading") {
    return <p className="text-sm text-slate-500">正在读取 A2A Agent 配置...</p>;
  }
  if (state.type === "error") {
    return <p className="text-sm text-rose-600">{state.message}</p>;
  }
  return (
    <div>
      <h3 className="text-sm font-semibold text-slate-900">已配置远程 Agent</h3>
      <div className="mt-2 grid gap-2">
        {state.data.items.map((agent) => (
          <div
            className="rounded-md border border-slate-200 bg-white px-3 py-2"
            key={agent.key}
          >
            <div className="flex items-center justify-between gap-3 text-sm">
              <span className="font-medium text-slate-900">{agent.name}</span>
              <span className="text-xs text-slate-500">
                {agent.transport} · {agent.enabled ? "enabled" : "disabled"}
              </span>
            </div>
            <p className="mt-1 text-xs leading-5 text-slate-500">
              {agent.description}
            </p>
            <p className="mt-1 truncate text-xs text-sky-700">{agent.url}</p>
          </div>
        ))}
      </div>
    </div>
  );
}


function AgentCardView({ state }: { state: LoadState<A2aAgentCardData> }) {
  if (state.type === "loading") {
    return <p className="text-sm text-slate-500">正在读取 Agent Card...</p>;
  }
  if (state.type === "error") {
    return <p className="text-sm text-rose-600">{state.message}</p>;
  }
  return (
    <div>
      <h3 className="text-sm font-semibold text-slate-900">示例 Agent Card</h3>
      <div className="mt-2 rounded-md border border-slate-200 bg-white px-3 py-2">
        <div className="flex items-center justify-between gap-3 text-sm">
          <span className="font-medium text-slate-900">{state.data.name}</span>
          <span className="text-xs text-slate-500">v{state.data.version}</span>
        </div>
        <p className="mt-1 text-xs leading-5 text-slate-500">
          {state.data.description}
        </p>
        <div className="mt-3 grid gap-2">
          {state.data.capabilities.map((capability) => (
            <div
              className="rounded-md border border-slate-200 bg-slate-50 px-3 py-2"
              key={capability.name}
            >
              <div className="text-xs font-semibold text-slate-900">
                {capability.name}
              </div>
              <p className="mt-1 text-xs leading-5 text-slate-600">
                {capability.description}
              </p>
              <p className="mt-1 text-xs leading-5 text-slate-500">
                示例：{capability.example}
              </p>
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

#### 35.6.10.1 代码讲解

​        `A2aPanel` 现在展示三块内容：

```Plain
核心角色
已配置远程 Agent
示例 Agent Card
```

​        这样读者可以看到 A2A 已经从“概念说明”变成了“配置驱动的远程 Agent 能力”。

### 35.6.11 前端工具预览展示 A2A 结果

​        打开 `ui/app/components/tool-preview-panel.tsx`。

​        新增 `A2aTaskResultPayload` 类型：

```TypeScript
type A2aTaskResultPayload = {
  kind: "a2a_task_result";
  agent_key: string;
  remote_agent: string;
  task_id: string;
  status: string;
  input_message: Array<{ kind: string; text: string }>;
  output_message: Array<{ kind: string; text: string }>;
  steps: Array<{ index: number; action: string; detail: string }>;
};
```

​        新增 `parseA2aTaskResult()` 和 `A2aResultPreview()`。

#### 35.6.11.1 业务流程

​        后端工具输出：

```Plain
JSON 字符串 kind=a2a_task_result
```

​        前端解析：

```Plain
parseA2aTaskResult()
```

​        页面展示：

```Plain
A2aResultPreview
```

​        最终用户看到的是远程 Agent 任务卡片，而不是一段难读的 JSON。

## 35.7 关键理解

​        本章最重要的是理解 A2A 工具接入的位置。

​        A2A 不应该只是一个 API。

​        它应该进入 Agent 当前执行过程：

```Plain
计划步骤 -> 工具调用 -> 事件记录 -> 工具预览
```

​        第二个重点是理解 A2A 和 MCP 的区别。

```Plain
MCP：把外部能力包装成工具
A2A：把另一个 Agent 包装成协作者
```

​        第三个重点是配置驱动。

​        远程 Agent 信息写在 `api/config/a2a.yaml`，不是散落在代码里。

## 35.8 技术难点与亮点

​        技术难点：

​        具体来说，第一，A2A 返回的是任务结果，不是普通工具文本；第二，远程 Agent 调用要能进入现有 `tool_called` 事件；第三，前端要识别 A2A 结果并用专门 UI 展示；第四，demo/http 两种 transport 要使用统一接口。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        换句话说，第一，A2A 能力正式进入工具系统；第二，远程 Agent 可以通过配置声明；第三，A2A 调用过程能在当前会话中被观察；第四，后续多 Agent 编排可以直接复用这套远程 Agent 能力。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 35.9 面试考点

​        从实现顺序看，第一，A2A 和 MCP 的区别是什么？；第二，为什么远程 Agent 要先暴露 Agent Card？；第三，为什么 A2A 调用结果要进入 `tool_called` 事件？；第四，为什么本章要设计 `A2aClientManager`？；第五，demo transport 和 http transport 分别解决什么问题？。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 35.10 运行验证

​        下面命令默认在项目根目录执行。

### 35.10.1 编译 API

```Bash
cd api
uv run python -m compileall app
```

### 35.10.2 检查前端类型

```Bash
cd ../ui
pnpm typecheck
```

### 35.10.3 启动服务

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
docker compose build api ui
docker compose up -d nginx
```

### 35.10.4 验证 A2A 配置列表

```Bash
curl http://localhost:8088/api/a2a/agents
```

​        预期能看到：

```Plain
demo_researcher
ResearchAssistantAgent
transport: demo
```

### 35.10.5 验证 Agent Card

```Bash
curl http://localhost:8088/api/a2a/agents/demo_researcher/card
```

​        预期能看到远程 Agent 的名称、版本和能力列表。

### 35.10.6 验证 A2A message/send

```Bash
curl -X POST http://localhost:8088/api/a2a/message/send \
  -H "Content-Type: application/json" \
  -d '{"agent_key":"demo_researcher","message":"请远程研究 Agent 帮我整理 A2A 工具接入要点"}'
```

​        预期返回中包含：

```Plain
remote_agent: ResearchAssistantAgent
status: completed
steps
output_message
```

### 35.10.7 验证工具注册

```Bash
curl http://localhost:8088/api/agent-core/tools
```

​        预期工具列表中包含：

```Plain
a2a_call
```

### 35.10.8 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        在页面里：

​        放到工程语境里看，第一，创建或选择一个会话；第二，发送任务：`请调用远程研究 Agent，帮我整理 A2A 工具接入要点`；第三，点击“创建计划”；第四，点击“执行计划”；第五，查看右侧“工具预览”。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        预期能看到：

```Plain
工具：a2a_call
A2A 远程 Agent：ResearchAssistantAgent
状态：completed
协作步骤
远程输出
```

## 35.11 常见问题

- 问题：`/api/a2a/agents` 返回配置文件不存在怎么办？

​        解释：确认 `api/config/a2a.yaml` 是否存在，并确认 `.env` 或 Compose 中 `A2A_CONFIG_PATH=config/a2a.yaml`。

- 问题：工具列表里没有 `a2a_call` 怎么办？

​        解释：确认 `api/app/infrastructure/agent_tools/builtin.py` 中调用了 `register_a2a_tools(registry)`，然后重启 API。

- 问题：页面没有出现 A2A 工具结果怎么办？

​        解释：先确认任务文本里包含 `A2A`、`远程 Agent`、`远程研究` 等关键词。本章工具选择还是关键词规则。

- 问题：为什么 demo Agent 不是真实远程服务？

​        解释：本章目标是先打通工具事件链路。真实 HTTP Agent 的配置示例已经保留在 `example_http`，后续可以替换成实际服务。

- 问题：A2A 调用是不是可以替代内部 Agent？

​        解释：不是。内部 Agent 适合本项目自己的核心流程；A2A 适合调用外部或独立部署的协作 Agent。

## 35.12 本章小结

​        本章完成了 A2A 工具接入：

​        展开来看，第一，新增 `api/config/a2a.yaml`；第二，新增 A2A 配置加载；第三，新增 A2A Client Manager；第四，新增 `A2aService`；第五，新增真实 A2A 远程 Agent 列表、Agent Card 和 message/send 接口；第六，新增 `a2a_call` AgentTool；第七，ReAct 执行流程可以选择 A2A 工具；第八，前端可以展示 A2A 远程 Agent 配置和工具调用结果。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一章开始，项目不只是能调用工具，也能把另一个 Agent 当成协作者调用。

## 35.13 下一章预告

​        第 36 章会在 A2A 远程 Agent 能力之上，补齐多 Agent 协作编排，让多个角色围绕同一个任务分工、协作、汇总和评审。
