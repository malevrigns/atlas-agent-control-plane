# 第四十一章. 记忆调阅与上下文注入

> **演进提示**：本章介绍的“相关度 + 重要度 + 新鲜度”是检索起点。升级版还会校验记忆状态、作用域、有效期、权威度、可信度和是否已被替代，且每条注入结果携带 provenance、reason 与 retrieval trace，详见第 69 章。

## 41.1 本章目标

​        学完本章后，你将能够：

​        换句话说，第一，理解为什么长期记忆不能全部塞进模型上下文；第二，根据当前任务、会话标题和最近消息检索相关长期记忆；第三，设计 `MemoryContext`，把长期记忆压缩成可注入上下文；第四，理解相关度、重要度、新鲜度和预算共同参与记忆选择的原因；第五，把长期记忆合并进第 21 章的上下文工程快照；第六，让 PlannerAgent 和 ReActAgent 在执行时读取相关长期记忆；第七，在前端上下文抽屉中查看本次真正注入的长期记忆；第八，编写记忆检索服务的单元测试。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 41.2 最终效果

​        本章结束后，第 40 章保存的长期记忆会真正进入 Agent 执行链路。

​        访问：

```Plain
http://localhost:8088
```

​        你可以完成下面的操作：

```Plain
在设置页新增长期记忆
  |
  v
回到工作台，创建或选择会话
  |
  v
发送和这条记忆相关的任务
  |
  v
点击“上下文”
  |
  v
查看本次注入了哪些长期记忆
```

​        后端会话上下文接口：

```Plain
GET /api/sessions/{session_id}/context
```

​        响应中会新增：

```JSON
{
  "memory_context": {
    "query": "当前任务 + 会话标题 + 最近消息",
    "items": [
      {
        "kind": "user_preference",
        "content": "用户希望代码保留详细分步注释",
        "importance": 5,
        "relevance_score": 0.42,
        "matched_terms": ["代码", "注释"]
      }
    ],
    "candidate_count": 12,
    "omitted_count": 11,
    "total_chars": 24,
    "max_chars": 2400
  }
}
```

​        本章不新增数据库表。

​        第 40 章已经创建了：

```Plain
agent_memories
```

​        本章做的是：

```Plain
读取 agent_memories
  |
  v
过滤禁用、删除、过期记忆
  |
  v
计算相关度分数
  |
  v
按条数和字符预算压缩
  |
  v
注入上下文工程、Planner、ReAct 和前端上下文面板
```

## 41.3 本章要解决的问题

​        第 40 章已经让系统能够保存长期记忆，例如：

```Plain
用户希望后端代码保留详细分步注释
项目使用 FastAPI、Next.js、PostgreSQL 和 Redis
Docker 构建失败时先区分代码错误和网络错误
不能把 API Key 写进代码或提交到 Git
```

​        但保存只是第一步。

​        如果 Agent 执行任务时不读取这些记忆，那么长期记忆只是一个“设置页里的列表”，不会改善任务结果。

​        直觉上，可以把所有长期记忆都放进模型提示词。但这样会带来几个问题：

​        从实现顺序看，第一，记忆越来越多，上下文成本越来越高；第二，和当前任务无关的旧记忆会干扰模型；第三，错误或过期记忆如果没有过滤，会持续污染结果；第四，长内容会挤占最近消息、文件引用和工具结果的位置。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以长期记忆需要先检索、再压缩、再注入。

​        本章实现的核心链路是：

```Plain
当前任务
  |
  +-- 会话标题
  +-- 最近 3 条消息
  |
  v
MemoryRetrievalService
  |
  +-- 过滤不可用记忆
  +-- 计算混合分数
  +-- 应用数量预算
  +-- 应用字符预算
  |
  v
MemoryContext
  |
  +-- ContextEngineeringService
  +-- PlannerService
  +-- ReActAgentService
  +-- 前端上下文面板
```

## 41.4 本章技术方案

​        本章继续沿用当前后端分层：

```Plain
HTTP Route
  |
  v
ContextEngineeringService
  |
  +-- MemoryRetrievalService
  |
  v
UnitOfWork
  |
  v
AgentMemoryRepository
  |
  v
agent_memories
```

​        长期记忆不直接由路由层拼接。

​        原因是 Planner、ReAct、最终 Runner、前端上下文面板都会需要同一份上下文结果。如果每个地方自己检索一遍，就会出现结果不一致、预算不一致、排查困难的问题。

​        所以本章把长期记忆接入第 21 章的上下文工程：

```Plain
SessionContextSnapshot
  |
  +-- messages
  +-- event_summaries
  +-- files
  +-- memory_context
  +-- budget
```

​        这样以后只要拿到 `SessionContextSnapshot`，就能同时得到短期上下文和长期记忆。

​        本章暂时不做这些内容：

​        放到工程语境里看，第一，不做向量数据库；第二，不做 embedding 相似度检索；第三，不做 LLM 记忆重排；第四，不做多用户记忆隔离；第五，不做最终 Agent Runner 对齐。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些能力会在后续模型驱动工具选择、Runner 对齐、评测和安全章节继续增强。

## 41.5 新增和修改的文件

```Plain
.env.example
README.md
docker-compose.yml
api/README.md
ui/README.md
api/app/core/config.py
api/app/domain/context_engineering/entities.py
api/app/domain/memories/repositories.py
api/app/infrastructure/repositories/memory_repository.py
api/app/application/memory_retrieval_service.py
api/app/application/context_engineering_service.py
api/app/application/planner_service.py
api/app/application/react_agent_service.py
api/app/schemas/session.py
api/app/presentation/http/routes/sessions.py
api/tests/test_memory_retrieval_service.py
ui/app/types.ts
ui/app/components/memory-context-section.tsx
ui/app/components/context-panel.tsx
ui/app/components/session-control-bar.tsx
ui/app/components/chat-workspace.tsx
docs/course/chapters/41-memory-retrieval-context.md
```

## 41.6 实施步骤
### 41.6.1 增加记忆检索配置

​        打开 `api/app/core/config.py`，在上下文工程配置区域加入：

```Python
    # ----- Context Memory：长期记忆检索和注入预算 -----
    context_memory_candidate_limit: int = 100
    context_memory_limit: int = 6
    context_memory_max_chars: int = 2400
    context_memory_item_max_chars: int = 500
    context_memory_min_score: float = 0.12
```

​        打开 `.env.example`，加入：

```Plain
CONTEXT_MEMORY_CANDIDATE_LIMIT=100
CONTEXT_MEMORY_LIMIT=6
CONTEXT_MEMORY_MAX_CHARS=2400
CONTEXT_MEMORY_ITEM_MAX_CHARS=500
CONTEXT_MEMORY_MIN_SCORE=0.12
```

​        打开 `docker-compose.yml`，在 `api.environment` 中加入：

```YAML
      CONTEXT_MEMORY_CANDIDATE_LIMIT: ${CONTEXT_MEMORY_CANDIDATE_LIMIT:-100}
      CONTEXT_MEMORY_LIMIT: ${CONTEXT_MEMORY_LIMIT:-6}
      CONTEXT_MEMORY_MAX_CHARS: ${CONTEXT_MEMORY_MAX_CHARS:-2400}
      CONTEXT_MEMORY_ITEM_MAX_CHARS: ${CONTEXT_MEMORY_ITEM_MAX_CHARS:-500}
      CONTEXT_MEMORY_MIN_SCORE: ${CONTEXT_MEMORY_MIN_SCORE:-0.12}
```

#### 41.6.1.1 字段含义

​        展开来看，第一，`context_memory_candidate_limit`：先从数据库最多取多少条候选记忆；第二，`context_memory_limit`：最终最多注入多少条记忆；第三，`context_memory_max_chars`：长期记忆区域总字符预算；第四，`context_memory_item_max_chars`：单条记忆最多保留多少字符；第五，`context_memory_min_score`：低于这个分数的记忆不会注入上下文。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 41.6.1.2 为什么要分候选数量和最终数量

​        候选数量解决“从哪里找”的问题。

​        最终数量解决“给模型看多少”的问题。

​        比如数据库里有 100 条记忆，当前任务真正需要的可能只有 3 条。如果直接把 100 条全塞进上下文，模型会被噪音淹没。

### 41.6.2 扩展上下文领域模型

​        打开 `api/app/domain/context_engineering/entities.py`，加入长期记忆上下文模型：

```Python
@dataclass(slots=True)
class MemoryContextItem:
    """经过检索和压缩、准备注入 Agent 上下文的一条长期记忆。"""

    id: UUID
    kind: MemoryKind
    content: str
    importance: int
    relevance_score: float  # 综合相关度、重要度和新鲜度后的最终分数。
    matched_terms: list[str]  # 与当前任务匹配的关键词，便于解释检索结果。
    original_chars: int  # 裁剪前字符数。
    truncated: bool  # 是否按单条记忆预算裁剪。
    source_session_id: UUID | None
    source_event_id: UUID | None
    updated_at: datetime | None


@dataclass(slots=True)
class MemoryContext:
    """本次任务最终选中的长期记忆上下文。"""

    query: str
    items: list[MemoryContextItem]
    candidate_count: int
    omitted_count: int
    total_chars: int
    max_chars: int
```

​        继续扩展 `ContextBudget`：

```Python
    memory_limit: int  # 本次最多注入多少条长期记忆。
    max_memory_chars: int  # 长期记忆区域允许使用的总字符数。
    included_memories: int  # 本次实际注入的长期记忆数量。
    omitted_memories: int  # 因相关度或预算限制未注入的候选数量。
    total_memory_chars: int  # 本次注入的长期记忆总字符数。
```

​        最后把 `MemoryContext` 放进 `SessionContextSnapshot`：

```Python
@dataclass(slots=True)
class SessionContextSnapshot:
    session_id: UUID
    summary: str
    messages: list[ContextMessage]
    event_summaries: list[ContextEventSummary]
    files: list[ContextFileReference]
    memory_context: MemoryContext
    budget: ContextBudget
```

#### 41.6.2.1 这段代码的业务含义

​        `MemoryContextItem` 表示“本次任务真正使用的一条长期记忆”。

​        它不是数据库原始记录的简单复制，而是多了这些运行时信息：

​        具体来说，第一，`relevance_score`：为什么它能进入上下文；第二，`matched_terms`：它和当前任务命中了哪些词；第三，`truncated`：它有没有因为预算被裁剪；第四，`original_chars`：裁剪前有多长。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这些字段能让前端解释“为什么 Agent 会看到这条记忆”，也方便后续调试错误记忆。

### 41.6.3 扩展长期记忆 Repository

​        打开 `api/app/domain/memories/repositories.py`，在协议中加入：

```Python
    async def list_retrievable(
        self,
        *,
        now: datetime,
        limit: int,
    ) -> list[AgentMemory]:
        raise NotImplementedError
```

​        打开 `api/app/infrastructure/repositories/memory_repository.py`，实现：

```Python
    # ===================== 第8步：读取可参与检索的长期记忆 =====================
    async def list_retrievable(
        self,
        *,
        now: datetime,
        limit: int,
    ) -> list[AgentMemory]:
        stmt = (
            select(AgentMemoryModel)
            .where(
                AgentMemoryModel.deleted_at.is_(None),
                AgentMemoryModel.enabled.is_(True),
                or_(
                    AgentMemoryModel.expires_at.is_(None),
                    AgentMemoryModel.expires_at > now,
                ),
            )
            .order_by(
                AgentMemoryModel.importance.desc(),
                AgentMemoryModel.updated_at.desc(),
            )
            .limit(limit)
        )
        result = await self.db_session.execute(stmt)
        return [model.to_entity() for model in result.scalars()]
```

#### 41.6.3.1 这段代码的业务流程

​        数据库先过滤三类不能用的记忆：

```Plain
已软删除 deleted_at != null
已禁用 enabled = false
已过期 expires_at <= now
```

​        然后按：

```Plain
重要度高
更新时间新
```

​        取一批候选。

​        最终相关度排序不在 Repository 做，而是在 `MemoryRetrievalService` 做。

#### 41.6.3.2 为什么不在 SQL 里算相关度

​        本章使用的是轻量关键词相关度，后续可能替换成：

​        换句话说，第一，PostgreSQL 全文检索；第二，embedding 向量相似度；第三，LLM rerank。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        把检索策略放在应用服务中，后续替换会更容易。

### 41.6.4 实现 MemoryRetrievalService

​        创建：

```Plain
api/app/application/memory_retrieval_service.py
```

​        完整代码如下：

```Python
import math
import re
from datetime import UTC, datetime

from app.application.unit_of_work import UnitOfWork
from app.core.config import settings
from app.domain.context_engineering.entities import MemoryContext, MemoryContextItem
from app.domain.memories.entities import AgentMemory, MemoryKind


class MemoryRetrievalService:
    """把长期记忆候选压缩成适合当前任务的 MemoryContext。

    第 41 章使用可解释的混合评分：
    - 相关度：当前任务和记忆正文匹配了多少关键词。
    - 重要度：第 40 章保存的 1-5 级重要度。
    - 新鲜度：最近更新的记忆获得少量加分。

    第 43 章可以在不改变 MemoryContext 结构的情况下，把相关度部分
    替换成 embedding、全文检索或模型重排。
    """

    def __init__(self, uow: UnitOfWork) -> None:
        self.uow = uow

    # ===================== 第1步：检索并压缩长期记忆 =====================
    async def retrieve(
        self,
        *,
        query: str,
        limit: int | None = None,
        max_chars: int | None = None,
        max_item_chars: int | None = None,
        now: datetime | None = None,
    ) -> MemoryContext:
        """根据当前任务返回少量可注入上下文的长期记忆。"""

        # 1. 统一检索参数。显式参数主要供测试使用，运行时读取全局配置。
        current_time = now or datetime.now(UTC)
        item_limit = limit or settings.context_memory_limit
        total_char_limit = max_chars or settings.context_memory_max_chars
        item_char_limit = max_item_chars or settings.context_memory_item_max_chars

        # 2. 先让数据库过滤禁用、删除和过期记录。
        candidates = await self.uow.memories.list_retrievable(
            now=current_time,
            limit=settings.context_memory_candidate_limit,
        )

        # 3. 计算混合分数，并过滤完全不相关的普通记忆。
        ranked_items = []
        for memory in candidates:
            item = self._rank_memory(
                memory=memory,
                query=query,
                now=current_time,
            )
            if item.relevance_score < settings.context_memory_min_score:
                continue

            # 普通项目事实和任务经验必须与当前任务有关键词交集。
            # 用户偏好和长期约束属于全局记忆，可以在无直接命中时保留。
            is_global_memory = item.kind in {
                MemoryKind.user_preference,
                MemoryKind.constraint,
            }
            if not item.matched_terms and not is_global_memory:
                continue
            ranked_items.append(item)
        ranked_items.sort(
            key=lambda item: (
                item.relevance_score,
                item.importance,
                item.updated_at or datetime.min.replace(tzinfo=UTC),
            ),
            reverse=True,
        )

        # 4. 同时应用“条数预算”和“字符预算”。
        included: list[MemoryContextItem] = []
        used_chars = 0
        for item in ranked_items:
            if len(included) >= item_limit:
                break

            remaining_chars = total_char_limit - used_chars
            if remaining_chars <= 0:
                break

            allowed_chars = min(item_char_limit, remaining_chars)
            compressed = self._compress_item(item, allowed_chars)
            if not compressed.content:
                continue

            included.append(compressed)
            used_chars += len(compressed.content)

        return MemoryContext(
            query=" ".join(query.split())[:1000],
            items=included,
            candidate_count=len(candidates),
            omitted_count=max(len(candidates) - len(included), 0),
            total_chars=used_chars,
            max_chars=total_char_limit,
        )

    # ===================== 第2步：计算单条记忆的混合分数 =====================
    def _rank_memory(
        self,
        *,
        memory: AgentMemory,
        query: str,
        now: datetime,
    ) -> MemoryContextItem:
        query_terms = self._tokenize(query)
        memory_terms = self._tokenize(memory.content)
        matched_terms = sorted(
            query_terms & memory_terms,
            key=lambda value: (-len(value), value),
        )[:12]

        # 1. 关键词重叠形成主要相关度。较长关键词权重略高。
        query_weight = min(
            sum(max(len(term), 1) for term in query_terms) or 1,
            40,
        )
        matched_weight = sum(max(len(term), 1) for term in matched_terms)
        lexical_score = min(matched_weight / query_weight, 1.0)

        # 2. 用户偏好和长期约束具有跨任务价值，即使没有直接命中关键词，
        #    也给一个很小的基础分，之后仍受数量和字符预算控制。
        global_memory_bonus = (
            0.04
            if memory.kind in {MemoryKind.user_preference, MemoryKind.constraint}
            else 0.0
        )

        # 3. importance 已经是 1-5，归一化到 0-1。
        importance_score = max(1, min(5, memory.importance)) / 5

        # 4. 新鲜度使用平滑衰减。越接近当前时间，分数越接近 1。
        updated_at = memory.updated_at or memory.created_at or now
        age_days = max((now - updated_at).total_seconds() / 86400, 0)
        recency_score = math.exp(-age_days / 90)

        final_score = min(
            lexical_score * 0.65
            + importance_score * 0.20
            + recency_score * 0.10
            + global_memory_bonus,
            1.0,
        )

        return MemoryContextItem(
            id=memory.id,
            kind=memory.kind,
            content=memory.content,
            importance=memory.importance,
            relevance_score=round(final_score, 4),
            matched_terms=matched_terms,
            original_chars=len(memory.content),
            truncated=False,
            source_session_id=memory.source_session_id,
            source_event_id=memory.source_event_id,
            updated_at=memory.updated_at,
        )

    # ===================== 第3步：按单条字符预算压缩记忆 =====================
    @staticmethod
    def _compress_item(
        item: MemoryContextItem,
        max_chars: int,
    ) -> MemoryContextItem:
        if max_chars <= 0:
            content = ""
        elif len(item.content) <= max_chars:
            content = item.content
        elif max_chars <= 12:
            content = item.content[:max_chars]
        else:
            content = item.content[: max_chars - 9] + "...[已裁剪]"

        return MemoryContextItem(
            id=item.id,
            kind=item.kind,
            content=content,
            importance=item.importance,
            relevance_score=item.relevance_score,
            matched_terms=item.matched_terms,
            original_chars=item.original_chars,
            truncated=len(content) < item.original_chars,
            source_session_id=item.source_session_id,
            source_event_id=item.source_event_id,
            updated_at=item.updated_at,
        )

    # ===================== 第4步：提取中英文检索词 =====================
    @staticmethod
    def _tokenize(text: str) -> set[str]:
        """提取英文单词和中文 2-4 字片段。

        这是第 41 章的轻量检索实现，不需要额外分词或向量依赖。
        """

        normalized = text.lower()
        terms = set(re.findall(r"[a-z0-9_]+", normalized))
        chinese_blocks = re.findall(r"[\u4e00-\u9fff]+", normalized)
        for block in chinese_blocks:
            for size in (2, 3, 4):
                if len(block) < size:
                    continue
                terms.update(
                    block[index : index + size]
                    for index in range(len(block) - size + 1)
                )
        return terms
```

#### 41.6.4.1 代码讲解

​        `retrieve()` 是入口方法。

​        它分 4 步完成：

1. 读取预算配置。
2. 从数据库拿候选记忆。
3. 给每条记忆打分并过滤。
4. 按条数和字符预算输出 `MemoryContext`。

​        `_rank_memory()` 是打分方法。

​        本章使用混合评分：

```Plain
最终分数 =
  关键词相关度 * 0.65
  + 重要度 * 0.20
  + 新鲜度 * 0.10
  + 全局记忆小加分
```

​        这里的权重不是“唯一正确答案”，而是一个可解释、可测试、适合课程阶段的起点。

#### 41.6.4.2 为什么用户偏好和长期约束可以无命中保留

​        有些记忆是全局规则，例如：

```Plain
用户希望代码保留详细注释
不要提交 API Key
```

​        它们不一定和当前任务有关键词重合，但仍然可能影响执行方式。

​        所以本章允许 `user_preference` 和 `constraint` 获得一个很小的基础分。

​        但项目事实和任务经验必须命中关键词，否则容易把无关项目背景带进当前任务。

### 41.6.5 把长期记忆接入上下文工程

​        打开 `api/app/application/context_engineering_service.py`，在 `build_snapshot()` 中加入任务参数：

```Python
async def build_snapshot(
    self,
    session_id: UUID,
    *,
    task: str | None = None,
) -> SessionContextSnapshot:
```

​        读取会话、消息、事件和文件后，构建记忆检索查询：

```Python
memory_query = self._build_memory_query(
    task=task,
    session_title=session.title,
    messages=messages,
)
memory_context = await MemoryRetrievalService(self.uow).retrieve(
    query=memory_query,
)
```

​        新增查询构造方法：

```Python
@staticmethod
def _build_memory_query(
    *,
    task: str | None,
    session_title: str,
    messages: list[SessionMessage],
) -> str:
    """把当前任务、会话标题和最近消息合并为检索文本。"""

    parts = [task or "", session_title]
    parts.extend(message.content for message in messages[-3:])
    return "\n".join(part.strip() for part in parts if part.strip())
```

​        新增 Agent 提示词渲染方法：

```Python
@staticmethod
def render_for_agent(snapshot: SessionContextSnapshot) -> str:
    """生成 Planner 和 ReAct 可以直接使用的紧凑上下文文本。"""

    sections: list[str] = []

    if snapshot.memory_context.items:
        memory_lines = [
            (
                f"- [{item.kind.value}] {item.content} "
                f"(重要度 {item.importance}，相关度 {item.relevance_score:.2f})"
            )
            for item in snapshot.memory_context.items
        ]
        sections.append("长期记忆：\n" + "\n".join(memory_lines))

    if snapshot.messages:
        message_lines = [
            f"- {message.role}: {message.content}"
            for message in snapshot.messages
        ]
        sections.append("最近消息：\n" + "\n".join(message_lines))

    if snapshot.files:
        file_lines = [
            f"- {file.name} ({file.content_type}, {file.usage_hint})"
            for file in snapshot.files
        ]
        sections.append("文件引用：\n" + "\n".join(file_lines))

    return "\n\n".join(sections)
```

#### 41.6.5.1 这段代码的业务流程

​        上下文工程现在不只处理当前会话的短期信息，还负责找到跨会话的长期记忆。

​        检索查询来自三类文本：

```Plain
当前任务：用户这次真正想做什么
会话标题：这个会话长期围绕什么主题
最近消息：用户刚才补充了哪些细节
```

​        这样比只拿当前任务更稳。

​        如果用户发的是：

```Plain
继续优化这个
```

​        当前任务本身很短，但会话标题和最近消息还能提供上下文线索。

### 41.6.6 扩展上下文接口响应

​        打开 `api/app/schemas/session.py`，新增：

```Python
class MemoryContextItemResponse(BaseModel):
    id: UUID
    kind: str
    content: str
    importance: int
    relevance_score: float
    matched_terms: list[str]
    original_chars: int
    truncated: bool
    source_session_id: UUID | None
    source_event_id: UUID | None
    updated_at: datetime | None


class MemoryContextResponse(BaseModel):
    query: str
    items: list[MemoryContextItemResponse]
    candidate_count: int
    omitted_count: int
    total_chars: int
    max_chars: int
```

​        继续扩展 `SessionContextResponse`：

```Python
class SessionContextResponse(BaseModel):
    session_id: UUID
    summary: str
    messages: list[ContextMessageResponse]
    event_summaries: list[ContextEventSummaryResponse]
    files: list[ContextFileReferenceResponse]
    memory_context: MemoryContextResponse
    budget: ContextBudgetResponse
```

​        打开 `api/app/presentation/http/routes/sessions.py`，在 `to_context_response()` 中映射 `memory_context`。

#### 41.6.6.1 为什么接口要返回 matched_terms

​        长期记忆最怕“黑盒注入”。

​        如果模型做出奇怪决策，开发者需要知道：

```Plain
这次到底给模型看了哪些记忆？
这些记忆为什么被选中？
它们和任务命中了哪些词？
有没有超预算被裁剪？
```

​        `matched_terms` 和 `relevance_score` 就是给调试和前端解释用的。

### 41.6.7 让 Planner 使用长期记忆

​        打开 `api/app/application/planner_service.py`。

​        在 `create_plan()` 中构建上下文快照：

```Python
context_snapshot = await ContextEngineeringService(self.uow).build_snapshot(
    session_id,
    task=clean_task,
)
agent_context = ContextEngineeringService.render_for_agent(context_snapshot)
```

​        调用 LLM 时，把压缩上下文放入系统提示词：

```Python
result = await self.llm_service.chat(
    messages=[
        LLMMessage(
            role="system",
            content=(
                "你是一个 PlannerAgent。请把用户任务拆成 3 到 5 个可执行步骤。"
                "只返回 JSON，不要返回 Markdown。JSON 格式为："
                '{"title":"计划标题","goal":"目标","steps":['
                '{"title":"步骤标题","description":"步骤说明","expected_output":"预期输出"}'
                "]}\n\n"
                "生成计划时必须遵守下面的压缩上下文；不要把上下文原样复制到计划：\n"
                f"{agent_context or '暂无额外上下文'}"
            ),
        ),
        LLMMessage(role="user", content=task),
    ],
    temperature=0.2,
    max_tokens=1200,
)
```

​        保存 `plan_created` 事件时记录本次使用的记忆 ID：

```Python
payload=self._plan_to_payload(
    plan,
    memory_ids=[
        str(item.id)
        for item in context_snapshot.memory_context.items
    ],
)
```

#### 41.6.7.1 为什么要把 memory_ids 写进事件

​        计划生成结果需要可追溯。

​        如果后续用户问：

```Plain
为什么这个计划里提到了代码注释要求？
```

​        我们可以从 `plan_created.payload.memory_ids` 找到当时注入了哪几条长期记忆。

### 41.6.8 让 ReAct 执行读取长期记忆

​        打开 `api/app/application/react_agent_service.py`。

​        在执行计划前构建上下文快照：

```Python
context_snapshot = await ContextEngineeringService(self.uow).build_snapshot(
    session_id,
    task=str(plan.get("goal", "")),
)
memory_context = context_snapshot.memory_context
```

​        执行每个步骤时传入 `memory_context`：

```Python
await self._execute_step(
    session_id=session_id,
    plan=plan,
    step=step,
    index=index,
    memory_context=memory_context,
)
```

​        工具调用事件里记录记忆信息：

```Python
payload={
    "plan_id": plan_id,
    "step_id": step_id,
    "tool_name": tool_result["tool_name"],
    "arguments": tool_result["arguments"],
    "output": tool_result["output"],
    "memory_ids": [str(item.id) for item in memory_context.items],
    "memory_count": len(memory_context.items),
}
```

#### 41.6.8.1 为什么工具选择仍然只看当前任务

​        本章没有让长期记忆直接参与工具选择。

​        原因是长期记忆可能包含历史任务关键词，例如：

```Plain
上次使用浏览器截图排查页面问题
```

​        如果当前任务只是“整理接口文档”，却因为旧记忆里有“浏览器截图”而误触发 BrowserTool，结果会很奇怪。

​        所以本章做了一个保守设计：

```Plain
工具选择：主要看当前任务和当前步骤
工具参数和文本任务：可以追加长期记忆提示
```

​        更完整的模型驱动工具选择会在后续章节把工具 schema、短期上下文、长期记忆和模型决策统一起来。

### 41.6.9 前端新增长期记忆上下文类型

​        打开 `ui/app/types.ts`，新增：

```TypeScript
export type MemoryContextItem = {
  id: string;
  kind: string;
  content: string;
  importance: number;
  relevance_score: number;
  matched_terms: string[];
  original_chars: number;
  truncated: boolean;
  source_session_id: string | null;
  source_event_id: string | null;
  updated_at: string | null;
};

export type MemoryContext = {
  query: string;
  items: MemoryContextItem[];
  candidate_count: number;
  omitted_count: number;
  total_chars: number;
  max_chars: number;
};
```

​        继续把 `memory_context` 放入 `SessionContextData`。

#### 41.6.9.1 字段对应关系

​        前端字段和后端响应保持一致。

​        这里没有改成驼峰，是为了减少接口转换代码，让读者能直接对照后端响应理解页面展示。

### 41.6.10 新增长期记忆展示组件

​        创建：

```Plain
ui/app/components/memory-context-section.tsx
```

​        完整代码如下：

```TypeScript
import { Brain, Link2 } from "lucide-react";

import type { MemoryContext } from "../types";

const memoryKindLabels: Record<string, string> = {
  user_preference: "用户偏好",
  project_fact: "项目事实",
  task_experience: "任务经验",
  constraint: "长期约束",
};

// ===================== 第1步：展示本次真正注入的长期记忆 =====================
export function MemoryContextSection({
  memoryContext,
}: {
  memoryContext: MemoryContext;
}) {
  return (
    <section>
      <div className="flex items-center justify-between gap-3">
        <h3 className="flex items-center gap-2 text-sm font-semibold text-zinc-100">
          <Brain size={16} aria-hidden="true" />
          长期记忆
        </h3>
        <span className="text-xs text-zinc-500">
          {memoryContext.items.length}/{memoryContext.candidate_count} 条
        </span>
      </div>

      <p className="mt-2 text-xs leading-5 text-zinc-500">
        已使用 {memoryContext.total_chars}/{memoryContext.max_chars} 字符预算
      </p>

      <div className="mt-3 grid gap-2">
        {memoryContext.items.length === 0 ? (
          <div className="rounded-md border border-dashed border-white/10 px-3 py-5 text-center text-sm text-zinc-500">
            当前任务没有检索到可注入的长期记忆
          </div>
        ) : (
          memoryContext.items.map((memory) => (
            <article
              className="rounded-md border border-white/10 bg-white/[0.04] p-3"
              key={memory.id}
            >
              <div className="flex flex-wrap items-center gap-2 text-xs">
                <span className="rounded bg-blue-500/10 px-2 py-1 font-medium text-blue-300">
                  {memoryKindLabels[memory.kind] ?? memory.kind}
                </span>
                <span className="text-zinc-500">
                  相关度 {memory.relevance_score.toFixed(2)}
                </span>
                <span className="text-zinc-500">
                  重要度 {memory.importance}
                </span>
              </div>

              <p className="mt-3 whitespace-pre-wrap text-sm leading-6 text-zinc-300">
                {memory.content}
              </p>

              {memory.matched_terms.length > 0 ? (
                <div className="mt-3 flex flex-wrap gap-1.5">
                  {memory.matched_terms.map((term) => (
                    <span
                      className="rounded border border-white/10 px-2 py-1 text-[11px] text-zinc-500"
                      key={term}
                    >
                      {term}
                    </span>
                  ))}
                </div>
              ) : null}

              {memory.source_session_id ? (
                <div className="mt-3 flex items-center gap-1.5 text-[11px] text-zinc-600">
                  <Link2 size={12} aria-hidden="true" />
                  来源会话 {memory.source_session_id.slice(0, 8)}
                </div>
              ) : null}
            </article>
          ))
        )}
      </div>
    </section>
  );
}
```

#### 41.6.10.1 这段代码的业务流程

​        `MemoryContextSection` 只负责展示长期记忆。

​        它不负责请求接口，也不负责状态管理。

​        组件展示的信息包括：

​        从实现顺序看，第一，本次注入了多少条；第二，候选池有多少条；第三，字符预算用了多少；第四，每条记忆的类型、相关度、重要度；第五，任务命中的关键词；第六，记忆来源会话。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        这样上下文抽屉就能回答一个关键问题：

```Plain
Agent 这次到底看到了哪些长期记忆？
```

### 41.6.11 把长期记忆接入上下文抽屉

​        打开 `ui/app/components/context-panel.tsx`。

​        导入：

```TypeScript
import { MemoryContextSection } from "./memory-context-section";
```

​        在已有消息、事件、文件区域前面加入：

```TypeScript
<MemoryContextSection memoryContext={snapshot.memory_context} />
```

​        打开 `ui/app/components/session-control-bar.tsx`，新增“上下文”按钮。

​        打开 `ui/app/components/chat-workspace.tsx`，让右侧抽屉可以展示：

```Plain
工具详情
文件预览
上下文快照
```

​        当用户点击“上下文”时，前端会调用：

```Plain
GET /api/sessions/{session_id}/context
```

​        并在抽屉中展示短期上下文和长期记忆。

#### 41.6.11.1 为什么放在抽屉里

​        长期记忆属于调试和解释信息。

​        它不应该挤占主对话时间线，也不应该一直显示在首页。

​        放到右侧抽屉后，正常对话保持简洁，需要排查上下文时再打开。

### 41.6.12 编写单元测试

​        创建：

```Plain
api/tests/test_memory_retrieval_service.py
```

​        完整代码如下：

```Python
import unittest
from datetime import UTC, datetime, timedelta
from uuid import uuid4

from app.application.memory_retrieval_service import MemoryRetrievalService
from app.domain.memories.entities import AgentMemory, MemoryKind


class FakeMemoryRepository:
    """为单元测试提供固定长期记忆，不连接 PostgreSQL。"""

    def __init__(self, memories: list[AgentMemory]) -> None:
        self.memories = memories

    async def list_retrievable(
        self,
        *,
        now: datetime,
        limit: int,
    ) -> list[AgentMemory]:
        return [
            memory
            for memory in self.memories
            if memory.enabled
            and memory.deleted_at is None
            and (memory.expires_at is None or memory.expires_at > now)
        ][:limit]


class FakeUnitOfWork:
    def __init__(self, memories: list[AgentMemory]) -> None:
        self.memories = FakeMemoryRepository(memories)


def build_memory(
    *,
    kind: MemoryKind,
    content: str,
    importance: int = 3,
    enabled: bool = True,
    updated_at: datetime | None = None,
    expires_at: datetime | None = None,
) -> AgentMemory:
    now = datetime.now(UTC)
    return AgentMemory(
        id=uuid4(),
        kind=kind,
        content=content,
        importance=importance,
        enabled=enabled,
        source_session_id=None,
        source_event_id=None,
        expires_at=expires_at,
        created_at=now,
        updated_at=updated_at or now,
    )


class MemoryRetrievalServiceTest(unittest.IsolatedAsyncioTestCase):
    # ===================== 第1步：验证禁用和过期记忆不会被注入 =====================
    async def test_retrieve_excludes_disabled_and_expired_memories(self) -> None:
        now = datetime.now(UTC)
        service = MemoryRetrievalService(
            FakeUnitOfWork(
                [
                    build_memory(
                        kind=MemoryKind.project_fact,
                        content="项目后端使用 FastAPI 和 PostgreSQL",
                    ),
                    build_memory(
                        kind=MemoryKind.constraint,
                        content="不要提交 API Key",
                        enabled=False,
                    ),
                    build_memory(
                        kind=MemoryKind.task_experience,
                        content="部署前需要执行数据库迁移",
                        expires_at=now - timedelta(days=1),
                    ),
                ]
            )
        )

        context = await service.retrieve(
            query="请检查 FastAPI 项目的数据库架构",
            now=now,
        )

        self.assertEqual(len(context.items), 1)
        self.assertIn("FastAPI", context.items[0].content)

    # ===================== 第2步：验证相关度、重要度和更新时间共同参与排序 =====================
    async def test_retrieve_ranks_relevant_memory_first(self) -> None:
        now = datetime.now(UTC)
        service = MemoryRetrievalService(
            FakeUnitOfWork(
                [
                    build_memory(
                        kind=MemoryKind.project_fact,
                        content="项目使用 FastAPI 构建后端 API",
                        importance=4,
                        updated_at=now - timedelta(days=10),
                    ),
                    build_memory(
                        kind=MemoryKind.user_preference,
                        content="用户偏好中文解释",
                        importance=5,
                        updated_at=now,
                    ),
                    build_memory(
                        kind=MemoryKind.task_experience,
                        content="修改 FastAPI 路由后需要检查 OpenAPI 和编译结果",
                        importance=5,
                        updated_at=now - timedelta(days=1),
                    ),
                ]
            )
        )

        context = await service.retrieve(
            query="请修改 FastAPI 路由并检查接口",
            now=now,
        )

        self.assertEqual(context.items[0].kind, MemoryKind.task_experience)
        self.assertGreater(context.items[0].relevance_score, 0)
        self.assertIn("fastapi", context.items[0].matched_terms)

    # ===================== 第3步：验证数量和字符预算 =====================
    async def test_retrieve_respects_item_and_character_budget(self) -> None:
        now = datetime.now(UTC)
        memories = [
            build_memory(
                kind=MemoryKind.constraint,
                content=f"项目约束 {index}：修改代码后必须执行完整验证。",
                importance=5,
                updated_at=now - timedelta(minutes=index),
            )
            for index in range(8)
        ]
        service = MemoryRetrievalService(FakeUnitOfWork(memories))

        context = await service.retrieve(
            query="修改项目代码并执行验证",
            limit=3,
            max_chars=80,
            max_item_chars=40,
            now=now,
        )

        self.assertLessEqual(len(context.items), 3)
        self.assertLessEqual(context.total_chars, 80)
        self.assertTrue(all(len(item.content) <= 50 for item in context.items))
        self.assertGreater(context.omitted_count, 0)

    # ===================== 第4步：验证无关项目事实不会只靠重要度进入上下文 =====================
    async def test_retrieve_excludes_unmatched_non_global_memory(self) -> None:
        now = datetime.now(UTC)
        service = MemoryRetrievalService(
            FakeUnitOfWork(
                [
                    build_memory(
                        kind=MemoryKind.project_fact,
                        content="移动端项目使用 Flutter 和 SQLite",
                        importance=5,
                    ),
                    build_memory(
                        kind=MemoryKind.project_fact,
                        content="后端 API 使用 FastAPI 和 PostgreSQL",
                        importance=3,
                    ),
                ]
            )
        )

        context = await service.retrieve(
            query="请优化 FastAPI 数据库接口",
            now=now,
        )

        self.assertEqual(len(context.items), 1)
        self.assertIn("FastAPI", context.items[0].content)


if __name__ == "__main__":
    unittest.main()
```

#### 41.6.12.1 测试覆盖了什么

​        这组测试不依赖 PostgreSQL。

​        它只测试检索策略本身：

​        放到工程语境里看，第一，禁用和过期记忆不能进入上下文；第二，相关任务经验应该排在前面；第三，条数预算和字符预算必须生效；第四，无关项目事实不能只靠重要度混进当前任务。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        长期记忆检索是一类容易“看起来差不多但越用越乱”的逻辑，所以这里必须有单元测试兜底。

## 41.7 关键理解

​        本章最重要的是理解：

```Plain
长期记忆不是历史聊天记录的全文回放。
```

​        正确链路应该是：

```Plain
历史信息
  |
  v
抽取成稳定记忆
  |
  v
当前任务检索相关记忆
  |
  v
压缩成 MemoryContext
  |
  v
注入 Planner / ReAct / Runner
```

​        第二个重点是预算。

​        长期记忆必须和最近消息、事件摘要、文件引用共享模型上下文空间。如果长期记忆占得太多，当前任务的最新信息反而会被挤出去。

​        第三个重点是可解释性。

​        本章没有直接引入向量检索，而是先用关键词相关度、重要度、新鲜度做混合评分。这样读者可以清楚看到每条记忆为什么被选中。

## 41.8 技术难点与亮点

​        技术难点：

​        展开来看，第一，长期记忆既要跨会话复用，又不能无脑全量注入；第二，用户偏好、项目事实、任务经验、长期约束的召回策略不同；第三，上下文快照要同时兼容前端展示和 Agent 提示词；第四，Planner 和 ReAct 需要使用同一套上下文结果，避免执行链路前后不一致。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        具体来说，第一，第 40 章保存的长期记忆开始进入真实执行链路；第二，`MemoryContext` 同时服务后端 Agent 和前端解释面板；第三，检索策略可解释、可测试，后续可以平滑升级为 embedding 或 LLM rerank；第四，前端可以看到“本次注入了哪些记忆”，而不是只相信黑盒结果。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 41.9 面试考点

​        换句话说，第一，短期上下文和长期记忆分别解决什么问题？；第二，为什么不能把所有长期记忆都放进 prompt？；第三，相关度、重要度、新鲜度分别代表什么？；第四，用户偏好和项目事实为什么不能用完全相同的过滤策略？；第五，为什么 `MemoryContext` 应该放进统一上下文快照？；第六，记忆检索服务如何写单元测试，为什么不应该依赖真实数据库？。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 41.10 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

### 41.10.1 运行后端测试

```Bash
cd api
uv run python -m unittest discover -s tests -v
```

​        预期能看到：

```Plain
test_retrieve_excludes_disabled_and_expired_memories ... ok
test_retrieve_ranks_relevant_memory_first ... ok
test_retrieve_respects_item_and_character_budget ... ok
test_retrieve_excludes_unmatched_non_global_memory ... ok
```

### 41.10.2 编译后端代码

```Bash
uv run python -m compileall app
```

​        预期没有 Python 语法错误。

### 41.10.3 检查前端类型和构建

```Bash
cd ../ui
pnpm typecheck
pnpm build
```

​        预期没有 TypeScript 报错，Next.js 构建成功。

### 41.10.4 启动服务

​        回到项目根目录：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
docker compose build api ui
docker compose up -d nginx
docker compose exec api uv run alembic upgrade head
```

​        本章没有新增迁移，`alembic upgrade head` 只是确认数据库已经处于最新版本。

### 41.10.5 创建一条长期记忆

```Bash
curl -X POST http://localhost:8088/api/memories \
  -H "Content-Type: application/json" \
  -d '{"kind":"user_preference","content":"用户希望后端代码保留详细分步注释，并解释每个函数的业务流程","importance":5}'
```

​        再创建一条项目事实：

```Bash
curl -X POST http://localhost:8088/api/memories \
  -H "Content-Type: application/json" \
  -d '{"kind":"project_fact","content":"AtlasAgent 后端使用 FastAPI、PostgreSQL、Redis 和 SQLAlchemy","importance":4}'
```

### 41.10.6 创建会话并发送任务

```Bash
curl -X POST http://localhost:8088/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"title":"第 41 章记忆检索测试"}'
```

​        把返回的 `id` 替换到下面命令：

```Bash
curl -N -X POST http://localhost:8088/api/sessions/{session_id}/messages/stream \
  -H "Content-Type: application/json" \
  -d '{"content":"请优化 FastAPI 后端代码，并注意代码注释和业务流程解释"}'
```

### 41.10.7 验证上下文接口

```Bash
curl http://localhost:8088/api/sessions/{session_id}/context
```

​        预期响应中能看到：

```Plain
memory_context
用户希望后端代码保留详细分步注释
FastAPI
included_memories
total_memory_chars
```

### 41.10.8 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        操作：

​        从实现顺序看，第一，打开或创建一个会话；第二，发送和长期记忆相关的任务；第三，点击会话控制栏里的“上下文”；第四，确认右侧抽屉出现“长期记忆”区域；第五，查看相关度、重要度、命中关键词和字符预算。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 41.11 常见问题

- 问题：设置页新增了记忆，但上下文面板没有显示怎么办？

​        解释：先确认当前任务和记忆内容是否相关。项目事实和任务经验必须命中关键词才会进入上下文。用户偏好和长期约束更容易跨任务进入，但仍受分数和预算限制。

- 问题：为什么本章不使用向量数据库？

​        解释：本章先稳定数据结构、预算和注入链路。向量检索属于检索策略升级，后续可以在不改变 `MemoryContext` 的情况下替换相关度计算。

- 问题：为什么长期记忆没有直接改变工具选择？

​        解释：这是有意控制风险。旧记忆里的工具关键词可能误导当前任务。本章先让记忆影响文本参数和计划生成，后续模型驱动工具选择会统一处理。

- 问题：`memory_context.items` 为空是不是错误？

​        解释：不一定。如果当前任务和已有记忆不相关，空结果是合理的。长期记忆宁可少注入，也不要把无关信息塞进模型。

- 问题：Docker 构建很慢怎么办？

​        解释：本章主要改 API 和 UI 代码，通常只需要 `docker compose build api ui`。如果本机镜像已经是最新，也可以先本地运行测试和前端构建，再由你手动决定是否重新构建 Docker 镜像。

## 41.12 本章小结

​        本章完成了长期记忆从“保存”到“使用”的关键一步：

​        放到工程语境里看，第一，新增长期记忆检索配置；第二，新增 `MemoryContext` 和记忆预算字段；第三，Repository 支持读取可检索记忆；第四，实现 `MemoryRetrievalService`；第五，上下文工程快照包含长期记忆；第六，PlannerAgent 计划生成时注入长期记忆；第七，ReActAgent 工具执行时携带长期记忆提示和记忆 ID；第八，前端上下文抽屉展示本次注入的长期记忆；第九，增加记忆检索单元测试。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一章开始，Agent 不再只依赖当前会话最近消息，而是能跨会话召回用户偏好、项目事实、任务经验和长期约束。

## 41.13 下一章预告

​        第 42 章会进入 Agent Runner 最终对齐，把 Planner、ReAct、TaskRunner、工具调用、上下文工程、长期记忆、多 Agent 协作和沙箱能力收敛为更统一的执行主链路。
