# 第四十章. 长期记忆系统立档

> **演进提示**：本章的四类记忆和人工确认流程仍然保留为兼容入口。第 69 章会进一步加入 `scope`、`status`、`confidence`、`authority`、证据、有效期和替代关系，并使用 Memory Write Gate 阻止无证据内容直接进入可检索长期记忆。

## 40.1 本章目标

​        学完本章后，你将能够：

​        展开来看，第一，理解 Agent 的短期记忆和长期记忆分别解决什么问题；第二，理解为什么不能把全部历史消息永久塞进模型上下文；第三，设计 `agent_memories` 长期记忆表；第四，使用领域实体和 Repository 协议隔离业务层与 SQLAlchemy；第五，手动新增、查看、禁用和删除长期记忆；第六，从会话消息和事件中抽取记忆候选；第七，理解为什么候选记忆需要确认后再写入数据库；第八，在设置页管理用户偏好、项目事实、任务经验和长期约束。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 40.2 最终效果

​        本章结束后，后端会新增长期记忆接口：

```Plain
GET    /api/memories
POST   /api/memories
PATCH  /api/memories/{memory_id}
DELETE /api/memories/{memory_id}
POST   /api/memories/extract
```

​        访问：

```Plain
http://localhost:8088
```

​        点击左侧“设置”，页面会新增“长期记忆”区域。

​        你可以完成下面的操作：

```Plain
手动新增长期记忆
  |
  v
查看记忆类型、重要度和启用状态
  |
  +-- 临时禁用记忆
  |
  +-- 重新启用记忆
  |
  +-- 删除错误记忆
```

​        还可以输入一个会话 ID：

```Plain
读取会话消息和事件
  |
  v
按规则抽取候选记忆
  |
  v
用户检查候选内容
  |
  v
点击确认后写入 agent_memories
```

​        本章只完成长期记忆的“保存和管理”。

​        第 41 章才会完成：

```Plain
当前任务 -> 检索相关记忆 -> 压缩为 MemoryContext -> 注入 Agent 上下文
```

​        所以本章新增一条记忆后，Agent 暂时不会自动在新任务中使用它，这是正常现象。

## 40.3 本章要解决的问题

​        前面的章节已经实现了会话消息、事件、上下文快照和上下文裁剪。

​        这些数据主要解决当前任务的执行问题，例如：

```Plain
用户刚才说了什么
当前计划有哪些步骤
已经调用了哪些工具
当前步骤执行到哪里
附件里有哪些文件
```

​        这些内容属于短期记忆。

​        但是跨会话工作时，还会遇到另一类信息：

```Plain
用户希望所有后端代码都保留详细分步注释
项目使用 FastAPI、Next.js、PostgreSQL 和 Redis
Docker 构建失败时应该先区分代码错误和镜像网络错误
不能把 API Key 写进代码或提交到 Git
```

​        这些信息即使换了一个新会话，仍然有价值，属于长期记忆。

​        如果每次新建会话都要求用户重新说明这些背景，Agent 的使用体验会很差。

​        如果直接把所有历史会话全部放进模型上下文，又会出现：

​        具体来说，第一，上下文越来越长；第二，Token 成本持续增加；第三，旧消息会干扰当前任务；第四，错误信息可能被长期放大；第五，模型难以找到真正相关的内容。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        所以本章建立独立的长期记忆系统。

## 40.4 短期记忆和长期记忆

​        先看两者的区别：

| 对比项   | 短期记忆                       | 长期记忆                               |
| -------- | ------------------------------ | -------------------------------------- |
| 使用范围 | 当前会话、当前任务             | 跨会话                                 |
| 典型内容 | 最近消息、计划、步骤、工具结果 | 用户偏好、项目事实、长期约束、历史经验 |
| 数据规模 | 随任务执行快速变化             | 数量较少、内容稳定                     |
| 生命周期 | 任务结束后价值可能降低         | 经过确认后长期保存                     |
| 使用方式 | 构建当前上下文快照             | 检索少量相关内容后注入上下文           |
| 风险     | 上下文过长                     | 错误记忆长期污染                       |

​        可以把它们理解成：

```Plain
短期记忆 = 当前工作台上正在使用的材料
长期记忆 = 经过整理后放进知识柜的稳定信息
```

​        长期记忆不是“永久保存全部聊天记录”。

​        它应该只保存跨会话仍然有价值的信息。

## 40.5 本章技术方案

​        本章继续使用当前后端分层：

```Plain
HTTP Route
  |
  v
MemoryService
  |
  v
UnitOfWork
  |
  v
AgentMemoryRepository
  |
  v
SqlAlchemyAgentMemoryRepository
  |
  v
agent_memories
```

​        记忆候选抽取链路如下：

```Plain
POST /api/memories/extract
  |
  v
检查会话是否存在
  |
  v
读取 session_messages
  |
  v
读取 session_events
  |
  v
按关键词识别候选类型
  |
  v
去重并返回候选
  |
  v
前端等待用户确认
```

​        确认候选时才真正写入数据库：

```Plain
候选记忆
  |
  | 用户点击确认
  v
POST /api/memories
  |
  v
保存正文、重要度、来源会话、来源事件和 metadata
```

### 40.5.1 为什么本章先使用规则抽取

​        长期记忆抽取最终可以由 LLM 完成。

​        但是在基础链路阶段，先使用规则抽取有三个好处：

​        换句话说，第一，结果可预测，方便验证；第二，不依赖外部模型网络；第三，可以先把数据结构、确认流程和管理能力稳定下来。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        第 41 章接入检索后，可以继续升级为：

```Plain
规则初筛 + LLM 结构化抽取 + 相似度去重 + 人工或策略确认
```

## 40.6 新增和修改的文件

```Plain
README.md
api/README.md
ui/README.md
api/app/domain/memories/__init__.py
api/app/domain/memories/entities.py
api/app/domain/memories/repositories.py
api/app/application/memory_service.py
api/app/application/unit_of_work.py
api/app/infrastructure/database/models/agent_memory.py
api/app/infrastructure/database/models/__init__.py
api/app/infrastructure/repositories/memory_repository.py
api/app/schemas/memory.py
api/app/presentation/http/routes/memories.py
api/app/presentation/http/router.py
api/migrations/versions/202606230001_create_agent_memories.py
ui/app/types.ts
ui/app/lib/memory-api.ts
ui/app/components/memory-settings-panel.tsx
ui/app/components/settings-workspace.tsx
docs/course/chapters/40-long-term-memory.md
```

## 40.7 实施步骤
### 40.7.1 定义长期记忆领域实体

​        创建：

```Plain
api/app/domain/memories/entities.py
```

​        完整代码如下：

```Python
from dataclasses import dataclass, field
from datetime import datetime
from enum import StrEnum
from uuid import UUID


class MemoryKind(StrEnum):
    """长期记忆的业务分类。

    第 40 章先使用四类稳定分类：
    - user_preference：用户偏好，例如喜欢中文解释、希望代码多注释。
    - project_fact：项目事实，例如项目使用 FastAPI、Next.js、PostgreSQL。
    - task_experience：历史任务经验，例如某类任务应该先跑哪些验证。
    - constraint：长期约束，例如不要暴露密钥、不要提交某类文件。
    """

    user_preference = "user_preference"
    project_fact = "project_fact"
    task_experience = "task_experience"
    constraint = "constraint"


@dataclass(slots=True)
class AgentMemory:
    """跨会话保存的一条长期记忆。"""

    id: UUID
    kind: MemoryKind
    content: str
    importance: int
    enabled: bool
    source_session_id: UUID | None
    source_event_id: UUID | None
    expires_at: datetime | None
    metadata: dict[str, object] = field(default_factory=dict)
    created_at: datetime | None = None
    updated_at: datetime | None = None
    deleted_at: datetime | None = None


@dataclass(slots=True)
class MemoryCandidate:
    """从消息或事件中抽取出来、等待确认的记忆候选。"""

    kind: MemoryKind
    content: str
    importance: int
    reason: str
    source_session_id: UUID | None = None
    source_event_id: UUID | None = None
    metadata: dict[str, object] = field(default_factory=dict)
```

#### 40.7.1.1 代码讲解

​        `MemoryKind` 使用 `StrEnum`，数据库和 API 中保存的是稳定字符串。

​        四种分类分别解决：

​        从实现顺序看，第一，`user_preference`：用户希望系统如何回答或工作；第二，`project_fact`：项目本身长期稳定的背景信息；第三，`task_experience`：历史任务中总结出的执行经验；第四，`constraint`：后续任务必须遵守的限制。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        `AgentMemory` 是已经确认并写入数据库的正式记忆。

​        `MemoryCandidate` 只是候选，它比正式记忆多一个 `reason` 字段，用来解释为什么系统认为这段内容值得保存。

​        候选和正式记忆分开，可以避免下面的问题：

```Plain
普通聊天内容 -> 被误判为长期事实 -> 永久影响后续任务
```

### 40.7.2 定义 Repository 协议

​        创建：

```Plain
api/app/domain/memories/repositories.py
```

​        完整代码如下：

```Python
from datetime import datetime
from typing import Protocol
from uuid import UUID

from app.domain.memories.entities import AgentMemory, MemoryKind


class AgentMemoryRepository(Protocol):
    """长期记忆仓库协议。

    应用服务只依赖这个协议，不直接依赖 SQLAlchemy。
    第 41 章做记忆检索时，也会继续通过这个协议扩展查询方法。
    """

    async def add(
        self,
        *,
        kind: MemoryKind,
        content: str,
        importance: int,
        source_session_id: UUID | None,
        source_event_id: UUID | None,
        expires_at: datetime | None,
        metadata: dict[str, object],
    ) -> AgentMemory:
        raise NotImplementedError

    async def get(self, memory_id: UUID) -> AgentMemory | None:
        raise NotImplementedError

    async def list_active(
        self,
        *,
        kind: MemoryKind | None = None,
        enabled_only: bool = False,
        limit: int = 100,
    ) -> list[AgentMemory]:
        raise NotImplementedError

    async def update(
        self,
        memory_id: UUID,
        *,
        content: str | None = None,
        importance: int | None = None,
        enabled: bool | None = None,
        expires_at: datetime | None = None,
        metadata: dict[str, object] | None = None,
    ) -> AgentMemory | None:
        raise NotImplementedError

    async def soft_delete(self, memory_id: UUID) -> AgentMemory | None:
        raise NotImplementedError
```

#### 40.7.2.1 为什么要先定义协议

​        `MemoryService` 不应该知道数据库使用 SQLAlchemy、PostgreSQL 还是其他实现。

​        应用层只需要知道：

```Plain
可以新增记忆
可以按条件查询记忆
可以更新记忆
可以软删除记忆
```

​        这样第 41 章增加向量检索或混合检索时，业务服务不需要直接改成数据库代码。

### 40.7.3 创建 SQLAlchemy 模型

​        创建：

```Plain
api/app/infrastructure/database/models/agent_memory.py
```

​        核心结构如下：

```Python
from datetime import datetime
from uuid import UUID, uuid4

from sqlalchemy import Boolean, DateTime, ForeignKey, Integer, String, Text, func, text
from sqlalchemy.dialects.postgresql import JSONB
from sqlalchemy.dialects.postgresql import UUID as PgUUID
from sqlalchemy.orm import Mapped, mapped_column

from app.domain.memories.entities import AgentMemory, MemoryKind
from app.infrastructure.database.base import Base


class AgentMemoryModel(Base):
    """长期记忆数据库模型。"""

    __tablename__ = "agent_memories"

    # ===================== 第1步：定义记忆主体字段 =====================
    id: Mapped[UUID] = mapped_column(
        PgUUID(as_uuid=True),
        primary_key=True,
        default=uuid4,
    )
    kind: Mapped[str] = mapped_column(String(64), nullable=False)
    content: Mapped[str] = mapped_column(Text, nullable=False)
    importance: Mapped[int] = mapped_column(Integer, nullable=False, default=3)
    enabled: Mapped[bool] = mapped_column(Boolean, nullable=False, default=True)

    # ===================== 第2步：保存记忆来源和生命周期 =====================
    source_session_id: Mapped[UUID | None] = mapped_column(
        PgUUID(as_uuid=True),
        ForeignKey("sessions.id", ondelete="SET NULL"),
    )
    source_event_id: Mapped[UUID | None] = mapped_column(
        PgUUID(as_uuid=True),
        ForeignKey("session_events.id", ondelete="SET NULL"),
    )
    expires_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))
    metadata_json: Mapped[dict[str, object]] = mapped_column(
        "metadata",
        JSONB,
        nullable=False,
        default=dict,
        server_default=text("'{}'::jsonb"),
    )

    # ===================== 第3步：记录审计时间 =====================
    created_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        nullable=False,
        server_default=func.now(),
    )
    updated_at: Mapped[datetime] = mapped_column(
        DateTime(timezone=True),
        nullable=False,
        server_default=func.now(),
        onupdate=func.now(),
    )
    deleted_at: Mapped[datetime | None] = mapped_column(DateTime(timezone=True))

    # ===================== 第4步：把 ORM 模型转换为领域实体 =====================
    def to_entity(self) -> AgentMemory:
        return AgentMemory(
            id=self.id,
            kind=MemoryKind(self.kind),
            content=self.content,
            importance=self.importance,
            enabled=self.enabled,
            source_session_id=self.source_session_id,
            source_event_id=self.source_event_id,
            expires_at=self.expires_at,
            metadata=dict(self.metadata_json or {}),
            created_at=self.created_at,
            updated_at=self.updated_at,
            deleted_at=self.deleted_at,
        )
```

#### 40.7.3.1 字段含义

​        放到工程语境里看，第一，`kind`：记忆业务分类；第二，`content`：记忆正文；第三，`importance`：重要度，范围为 1-5；第四，`enabled`：是否允许后续检索使用；第五，`source_session_id`：记忆来自哪个会话；第六，`source_event_id`：记忆来自哪个执行事件；第七，`expires_at`：可选过期时间；第八，`metadata`：抽取来源、规则和后续扩展信息；第九，`deleted_at`：软删除时间。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

#### 40.7.3.2 为什么来源外键使用`SET NULL`

​        如果来源会话被删除，不应该连带删除已经确认的长期记忆。

​        因此这里使用：

```Plain
ON DELETE SET NULL
```

​        删除来源后，只清空来源 ID，记忆正文仍然保留。

#### 40.7.3.3 为什么还需要`enabled` 和 `deleted_at`

​        两者用途不同：

```Plain
enabled=false：暂时不让 Agent 使用，但可以随时重新启用
deleted_at!=null：用户明确删除，普通查询不再返回
```

### 40.7.4 创建数据库迁移

​        创建：

```Plain
api/migrations/versions/202606230001_create_agent_memories.py
```

​        迁移会创建：

```Plain
agent_memories
```

​        并增加三个索引：

```Plain
kind + enabled
importance + updated_at
source_session_id
```

​        这些索引分别服务于：

​        展开来看，第一，按类型和启用状态过滤；第二，按重要度和更新时间排序；第三，从来源会话追溯候选记忆。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        关键迁移代码如下：

```Python
def upgrade() -> None:
    op.create_table(
        "agent_memories",
        sa.Column("id", postgresql.UUID(as_uuid=True), nullable=False),
        sa.Column("kind", sa.String(length=64), nullable=False),
        sa.Column("content", sa.Text(), nullable=False),
        sa.Column("importance", sa.Integer(), nullable=False, server_default="3"),
        sa.Column("enabled", sa.Boolean(), nullable=False, server_default=sa.true()),
        sa.Column("source_session_id", postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column("source_event_id", postgresql.UUID(as_uuid=True), nullable=True),
        sa.Column("expires_at", sa.DateTime(timezone=True), nullable=True),
        sa.Column(
            "metadata",
            postgresql.JSONB(astext_type=sa.Text()),
            nullable=False,
            server_default=sa.text("'{}'::jsonb"),
        ),
        sa.Column(
            "created_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.Column(
            "updated_at",
            sa.DateTime(timezone=True),
            server_default=sa.func.now(),
            nullable=False,
        ),
        sa.Column("deleted_at", sa.DateTime(timezone=True), nullable=True),
        sa.ForeignKeyConstraint(
            ["source_event_id"],
            ["session_events.id"],
            ondelete="SET NULL",
        ),
        sa.ForeignKeyConstraint(
            ["source_session_id"],
            ["sessions.id"],
            ondelete="SET NULL",
        ),
        sa.PrimaryKeyConstraint("id"),
    )
```

### 40.7.5 实现长期记忆 Repository

​        创建：

```Plain
api/app/infrastructure/repositories/memory_repository.py
```

​        Repository 负责：

```Plain
领域数据 <-> SQLAlchemy Model
```

​        新增记忆的核心代码：

```Python
async def add(
    self,
    *,
    kind: MemoryKind,
    content: str,
    importance: int,
    source_session_id: UUID | None,
    source_event_id: UUID | None,
    expires_at: datetime | None,
    metadata: dict[str, object],
) -> AgentMemory:
    # 1. 构建数据库模型，但不在 Repository 中提交事务。
    model = AgentMemoryModel(
        kind=kind.value,
        content=content,
        importance=importance,
        source_session_id=source_session_id,
        source_event_id=source_event_id,
        expires_at=expires_at,
        metadata_json=metadata,
    )
    self.db_session.add(model)

    # 2. flush 把 INSERT 发送给数据库，refresh 读取数据库生成字段。
    await self.db_session.flush()
    await self.db_session.refresh(model)

    # 3. 返回领域实体，不把 SQLAlchemy Model 暴露给应用层。
    return model.to_entity()
```

​        列表查询的核心代码：

```Python
async def list_active(
    self,
    *,
    kind: MemoryKind | None = None,
    enabled_only: bool = False,
    limit: int = 100,
) -> list[AgentMemory]:
    # 1. 默认过滤已经软删除的记录。
    stmt = select(AgentMemoryModel).where(
        AgentMemoryModel.deleted_at.is_(None)
    )

    # 2. 根据调用方参数增加过滤条件。
    if kind is not None:
        stmt = stmt.where(AgentMemoryModel.kind == kind.value)
    if enabled_only:
        stmt = stmt.where(AgentMemoryModel.enabled.is_(True))

    # 3. 重要度高、最近更新的记忆优先返回。
    stmt = stmt.order_by(
        AgentMemoryModel.importance.desc(),
        AgentMemoryModel.updated_at.desc(),
    ).limit(limit)

    result = await self.db_session.execute(stmt)
    return [model.to_entity() for model in result.scalars()]
```

#### 40.7.5.1 为什么 Repository 不执行`commit`

​        一个业务动作未来可能同时写入：

```Plain
长期记忆
会话事件
审计记录
```

​        如果每个 Repository 自己提交，就无法保证这些写入属于同一个事务。

​        所以 Repository 只负责数据读写，`MemoryService` 决定何时提交。

### 40.7.6 把记忆仓库加入 Unit of Work

​        打开：

```Plain
api/app/application/unit_of_work.py
```

​        导入：

```Python
from app.infrastructure.repositories.memory_repository import (
    SqlAlchemyAgentMemoryRepository,
)
```

​        在构造函数中加入：

```Python
self.memories = SqlAlchemyAgentMemoryRepository(db_session)
```

​        现在应用服务可以通过：

```Python
self.uow.memories
```

​        访问长期记忆仓库。

### 40.7.7 实现 MemoryService

​        创建：

```Plain
api/app/application/memory_service.py
```

​        这是本章最重要的业务文件。

#### 40.7.7.1 手动创建记忆

```Python
# ===================== 第2步：手动新增长期记忆 =====================
async def create_memory(
    self,
    *,
    kind: MemoryKind,
    content: str,
    importance: int = 3,
    source_session_id: UUID | None = None,
    source_event_id: UUID | None = None,
    expires_at: datetime | None = None,
    metadata: dict[str, object] | None = None,
) -> AgentMemory:
    # 1. 清理并校验正文。
    clean_content = content.strip()
    if not clean_content:
        raise AppException(
            message="memory content is required",
            code=400,
            status_code=400,
        )

    # 2. 把重要度限制在 1-5。
    safe_importance = max(1, min(5, importance))

    # 3. 通过 Repository 写入当前数据库事务。
    memory = await self.uow.memories.add(
        kind=kind,
        content=clean_content,
        importance=safe_importance,
        source_session_id=source_session_id,
        source_event_id=source_event_id,
        expires_at=expires_at,
        metadata=metadata or {},
    )

    # 4. 由应用服务统一提交事务。
    await self.uow.commit()
    return memory
```

​        这里必须清理空白内容，因为长期记忆会在第 41 章进入模型上下文。

#### 40.7.7.2 从会话抽取候选

```Python
# ===================== 第5步：从会话中抽取长期记忆候选 =====================
async def extract_candidates(
    self,
    session_id: UUID,
) -> list[MemoryCandidate]:
    # 1. 检查会话是否存在。
    session = await self.uow.sessions.get(session_id)
    if session is None:
        raise AppException(
            message="session not found",
            code=404,
            status_code=404,
        )

    # 2. 消息适合抽取用户偏好，事件适合抽取任务经验。
    messages = await self.uow.session_messages.list_by_session(session_id)
    events = await self.uow.session_events.list_by_session(session_id)

    # 3. 从消息正文中抽取候选。
    candidates: list[MemoryCandidate] = []
    for message in messages:
        candidates.extend(
            self._extract_from_text(
                text=message.content,
                source_session_id=session_id,
                source_event_id=None,
                source="message",
            )
        )

    # 4. 把事件 payload 转成文本，再抽取任务经验或项目事实。
    for event in events:
        text = " ".join(str(value) for value in event.payload.values())
        candidates.extend(
            self._extract_from_text(
                text=text,
                source_session_id=session_id,
                source_event_id=event.id,
                source=f"event:{event.type.value}",
            )
        )

    # 5. 去掉相同类型和相同正文的重复候选。
    return self._deduplicate_candidates(candidates)
```

#### 40.7.7.3 候选分类规则

```Python
if self._contains_any(
    clean_text,
    ["我喜欢", "我希望", "偏好", "以后", "记住"],
):
    candidates.append(
        MemoryCandidate(
            kind=MemoryKind.user_preference,
            content=clean_text[:500],
            importance=4,
            reason="文本中出现偏好或记住类表达。",
            source_session_id=source_session_id,
            source_event_id=source_event_id,
            metadata={"source": source},
        )
    )
```

​        同样的方式还会识别：

```Plain
项目、架构、技术栈、数据库、前端、后端
  -> project_fact

必须、不要、不能、要求、约束
  -> constraint

验证、报错、修复、提交、部署、经验
  -> task_experience
```

#### 40.7.7.4 为什么不自动保存

​        假设用户说：

```Plain
这次临时不要使用浏览器工具
```

​        如果系统自动把它保存成永久约束，后续所有任务都可能错误地禁用浏览器。

​        所以本章采用：

```Plain
抽取候选 -> 显示原因 -> 用户确认 -> 正式保存
```

### 40.7.8 定义 API Schema

​        创建：

```Plain
api/app/schemas/memory.py
```

​        创建请求包含：

```Python
class MemoryCreateRequest(BaseModel):
    kind: str = Field(min_length=1, max_length=64)
    content: str = Field(min_length=1, max_length=2000)
    importance: int = Field(default=3, ge=1, le=5)
    source_session_id: UUID | None = None
    source_event_id: UUID | None = None
    expires_at: datetime | None = None
    metadata: dict[str, object] = Field(default_factory=dict)
```

​        候选响应包含：

```Python
class MemoryCandidateResponse(BaseModel):
    kind: str
    content: str
    importance: int
    reason: str
    source_session_id: UUID | None
    source_event_id: UUID | None
    metadata: dict[str, object]
```

​        `reason` 只存在于候选响应中，用来帮助用户判断是否应该确认。

### 40.7.9 新增长期记忆 API

​        创建：

```Plain
api/app/presentation/http/routes/memories.py
```

#### 40.7.9.1 读取记忆列表

```Python
@router.get("", response_model=ApiResponse[MemoryListResponse])
async def list_memories(
    kind: str | None = Query(default=None),
    enabled_only: bool = Query(default=False),
    limit: int = Query(default=100, ge=1, le=500),
    service: MemoryService = Depends(build_memory_service),
) -> ApiResponse[MemoryListResponse]:
    memory_kind = parse_memory_kind(kind) if kind else None
    memories = await service.list_memories(
        kind=memory_kind,
        enabled_only=enabled_only,
        limit=limit,
    )
    return ApiResponse(
        data=MemoryListResponse(
            items=[to_memory_response(memory) for memory in memories]
        )
    )
```

​        可以使用查询参数：

```Plain
kind=user_preference
enabled_only=true
limit=20
```

#### 40.7.9.2 创建、更新和删除

```Plain
POST   /api/memories
PATCH  /api/memories/{memory_id}
DELETE /api/memories/{memory_id}
```

​        删除接口调用的是软删除，不会立刻从数据库物理移除记录。

#### 40.7.9.3 抽取候选

```Python
@router.post(
    "/extract",
    response_model=ApiResponse[MemoryCandidateListResponse],
)
async def extract_memory_candidates(
    payload: MemoryExtractRequest,
    service: MemoryService = Depends(build_memory_service),
) -> ApiResponse[MemoryCandidateListResponse]:
    candidates = await service.extract_candidates(payload.session_id)
    return ApiResponse(
        data=MemoryCandidateListResponse(
            items=[
                to_candidate_response(candidate)
                for candidate in candidates
            ]
        )
    )
```

​        最后在：

```Plain
api/app/presentation/http/router.py
```

​        注册：

```Python
api_router.include_router(memories.router)
```

### 40.7.10 定义前端类型和 API

​        打开：

```Plain
ui/app/types.ts
```

​        新增：

```TypeScript
export type MemoryKind =
  | "user_preference"
  | "project_fact"
  | "task_experience"
  | "constraint";

export type AgentMemoryItem = {
  id: string;
  kind: MemoryKind;
  content: string;
  importance: number;
  enabled: boolean;
  source_session_id: string | null;
  source_event_id: string | null;
  expires_at: string | null;
  metadata: Record<string, unknown>;
  created_at: string | null;
  updated_at: string | null;
};
```

​        创建：

```Plain
ui/app/lib/memory-api.ts
```

​        这个文件集中管理长期记忆请求：

```TypeScript
export function fetchMemories(): Promise<AgentMemoryItem[]> {
  return requestApi<AgentMemoryListData>("/api/memories").then(
    (data) => data.items,
  );
}

export function createMemory(payload: {
  kind: MemoryKind;
  content: string;
  importance: number;
  source_session_id?: string | null;
  source_event_id?: string | null;
  expires_at?: string | null;
  metadata?: Record<string, unknown>;
}): Promise<AgentMemoryItem> {
  return requestApi<AgentMemoryItem>("/api/memories", {
    method: "POST",
    body: JSON.stringify(payload),
  });
}
```

​        候选确认时要把来源一起提交：

```TypeScript
await createMemory({
  kind: candidate.kind,
  content: candidate.content,
  importance: candidate.importance,
  source_session_id: candidate.source_session_id,
  source_event_id: candidate.source_event_id,
  metadata: candidate.metadata,
});
```

​        如果只保存正文，会失去“这条记忆从哪里来”的追溯能力。

### 40.7.11 实现长期记忆设置面板

​        创建：

```Plain
ui/app/components/memory-settings-panel.tsx
```

​        组件按职责拆成：

```Plain
MemorySettingsPanel
  |
  +-- MemoryCreateForm
  |
  +-- MemoryCandidateExtractor
  |
  +-- MemoryList
  |
  +-- KindPill
```

#### 40.7.11.1 MemorySettingsPanel

​        它负责：

​        具体来说，第一，加载记忆列表；第二，保存手动录入的记忆；第三，请求候选抽取；第四，确认候选；第五，启用或禁用记忆；第六，删除记忆。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        核心状态：

```TypeScript
const [memories, setMemories] = useState<LoadState<AgentMemoryItem[]>>({
  type: "loading",
});
const [candidates, setCandidates] = useState<
  LoadState<MemoryCandidateItem[]>
>({ type: "ready", data: [] });
const [draft, setDraft] = useState<MemoryDraft>({
  kind: "user_preference",
  content: "",
  importance: 3,
});
```

#### 40.7.11.2 为什么不把状态放进全局 session store

​        长期记忆面板只在设置页使用。

​        如果把它直接塞进会话 store，会让会话状态和系统设置状态互相污染。

​        所以本章让组件自己管理局部状态。后续如果多个页面都要读取长期记忆，再单独建立 memory store。

#### 40.7.11.3 启用和禁用

```TypeScript
async function toggleMemory(memory: AgentMemoryItem) {
  await updateMemory(memory.id, {
    enabled: !memory.enabled,
  });
  await loadMemories();
}
```

​        禁用不会删除数据。

​        它适合处理：

```Plain
这条记忆目前不希望 Agent 使用
但未来可能重新启用
```

#### 40.7.11.4 接入设置工作台

​        打开：

```Plain
ui/app/components/settings-workspace.tsx
```

​        导入：

```TypeScript
import { MemorySettingsPanel } from "./memory-settings-panel";
```

​        在模块配置和运行时集成之间加入：

```TypeScript
<MemorySettingsPanel />
```

​        这样长期记忆属于系统设置，而不是继续堆进对话工作台右侧。

## 40.8 关键理解

### 40.8.1 长期记忆不是聊天记录备份

​        聊天记录记录“发生过什么”。

​        长期记忆保存“未来仍然值得使用什么”。

### 40.8.2 记忆抽取和记忆写入是两个动作

```Plain
extract = 发现可能有价值的信息
create = 确认后正式保存
```

​        把两个动作分开，可以降低错误记忆污染。

### 40.8.3 记忆必须可管理

​        长期记忆可能过时，也可能抽取错误。

​        因此必须提供：

```Plain
查看
禁用
重新启用
删除
来源追溯
过期时间
```

### 40.8.4 本章还没有做检索

​        `GET /api/memories` 只是管理列表。

​        真正执行任务时，不能把所有记忆全部塞进上下文。

​        第 41 章会根据：

```Plain
当前任务相关度
重要度
更新时间
过期状态
```

​        选出少量相关记忆。

## 40.9 技术难点与亮点

​        技术难点：

​        换句话说，第一，区分短期上下文和跨会话长期记忆；第二，避免把普通聊天内容自动保存成永久事实；第三，保留来源会话和来源事件，支持记忆追溯；第四，同时支持禁用、过期和软删除三种生命周期状态；第五，让领域层不直接依赖 SQLAlchemy。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        项目亮点：

​        从实现顺序看，第一，长期记忆不是简单消息复制，而是独立领域模型；第二，候选确认机制降低错误记忆污染风险；第三，记忆类型、重要度和来源为第 41 章混合检索打基础；第四，前端管理入口进入设置页，不干扰真实对话工作台。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 40.10 面试考点

​        放到工程语境里看，第一，Agent 的短期记忆和长期记忆有什么区别？；第二，为什么不能把全部历史消息直接放进模型上下文？；第三，为什么长期记忆需要重要度？；第四，为什么候选记忆不应该默认自动保存？；第五，`enabled`、`expires_at` 和 `deleted_at` 分别解决什么问题？；第六，为什么来源外键使用 `ON DELETE SET NULL`？；第七，Repository 为什么不应该自己执行 `commit()`？；第八，长期记忆检索为什么不能只按创建时间排序？。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 40.11 运行验证

​        下面命令默认在项目根目录执行：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
```

### 40.11.1 编译后端代码

```Bash
cd api
uv run python -m compileall app
```

​        预期没有 Python 语法错误。

### 40.11.2 检查 Alembic 迁移头

```Bash
uv run alembic heads
```

​        预期包含：

```Plain
202606230001 (head)
```

### 40.11.3 检查前端类型

```Bash
cd ../ui
pnpm typecheck
```

​        预期没有 TypeScript 错误。

### 40.11.4 构建并启动服务

​        回到项目根目录：

```Bash
cd /Users/atlas/Desktop/github/atlas-agents
docker compose build api ui
docker compose up -d api ui nginx
```

​        执行数据库迁移：

```Bash
docker compose exec api uv run alembic upgrade head
```

### 40.11.5 手动新增长期记忆

```Bash
curl -X POST http://localhost:8088/api/memories \
  -H "Content-Type: application/json" \
  -d '{
    "kind":"user_preference",
    "content":"用户希望后端代码保留详细的分步注释和业务解释",
    "importance":5
  }'
```

​        预期返回：

```Plain
kind: user_preference
importance: 5
enabled: true
```

​        记下返回中的 `id`。

### 40.11.6 查询记忆列表

```Bash
curl http://localhost:8088/api/memories
```

​        只查询已启用的用户偏好：

```Bash
curl "http://localhost:8088/api/memories?kind=user_preference&enabled_only=true"
```

### 40.11.7 禁用和重新启用记忆

​        把 `{memory_id}` 替换成创建接口返回的 ID：

```Bash
curl -X PATCH http://localhost:8088/api/memories/{memory_id} \
  -H "Content-Type: application/json" \
  -d '{"enabled":false}'
```

​        重新启用：

```Bash
curl -X PATCH http://localhost:8088/api/memories/{memory_id} \
  -H "Content-Type: application/json" \
  -d '{"enabled":true}'
```

### 40.11.8 从会话中抽取候选

​        先创建会话：

```Bash
curl -X POST http://localhost:8088/api/sessions \
  -H "Content-Type: application/json" \
  -d '{"title":"第 40 章长期记忆测试"}'
```

​        使用返回的会话 ID 发送一条包含偏好和约束的消息：

```Bash
curl -N -X POST \
  http://localhost:8088/api/sessions/{session_id}/messages/stream \
  -H "Content-Type: application/json" \
  -d '{
    "content":"请记住：我希望教程使用中文解释，而且代码必须保留详细分步注释"
  }'
```

​        抽取候选：

```Bash
curl -X POST http://localhost:8088/api/memories/extract \
  -H "Content-Type: application/json" \
  -d '{"session_id":"{session_id}"}'
```

​        预期候选中至少可能出现：

```Plain
user_preference
constraint
```

​        接口只返回候选，不会自动写入 `agent_memories`。

### 40.11.9 页面验证

​        访问：

```Plain
http://localhost:8088
```

​        操作：

​        展开来看，第一，点击左侧“设置”；第二，找到“长期记忆”区域；第三，选择“用户偏好”；第四，输入一条记忆内容；第五，设置重要度并点击“新增记忆”；第六，确认记忆出现在列表；第七，点击“禁用”，确认状态变成“已禁用”；第八，再次点击“启用”；第九，把测试会话 ID 输入“从会话抽取候选”；第十，点击“抽取”；第十一，检查候选类型、正文、重要度和抽取原因；第十二，点击“确认”，确认候选进入正式记忆列表。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

## 40.12 常见问题

- 问题：访问 `/api/memories` 返回数据库表不存在怎么办？

​        解释：还没有执行第 40 章迁移。运行 `docker compose exec api uv run alembic upgrade head`。

- 问题：页面设置里没有“长期记忆”怎么办？

​        解释：当前 UI 容器可能还是旧镜像。执行 `docker compose build ui`，再执行 `docker compose up -d --force-recreate ui nginx`。

- 问题：输入会话 ID 后没有候选怎么办？

​        解释：本章使用关键词规则抽取。确认会话消息包含“我希望”“记住”“必须”“项目”“验证”等关键词。没有候选不代表接口失败。

- 问题：为什么确认候选后还要重新读取列表？

​        解释：页面以数据库结果为准，重新读取可以拿到数据库生成的 ID、时间和最终状态。

- 问题：为什么删除记忆后数据库里可能还有记录？

​        解释：本章使用软删除。`deleted_at` 会被写入时间，普通列表接口不再返回这条记录。

- 问题：为什么本章新增记忆后，Agent 对话没有自动使用？

​        解释：本章只实现保存和管理。第 41 章会实现相关记忆检索和上下文注入。

- 问题：长期记忆能不能保存 API Key？

​        解释：不应该。密钥、密码、Token 等敏感信息不应进入普通长期记忆。后续安全章节会继续增加敏感信息过滤。

## 40.13 本章小结

​        本章完成了长期记忆的基础闭环：

​        具体来说，第一，新增长期记忆领域模型和 Repository 协议；第二，新增 `agent_memories` 数据表和迁移；第三，支持用户偏好、项目事实、任务经验和长期约束；第四，支持重要度、来源、过期时间、禁用和软删除；第五，新增长期记忆增删改查接口；第六，支持从会话消息和事件中抽取候选；第七，候选经过用户确认后才写入数据库；第八，设置页新增长期记忆管理面板。这些点放在一起看，构成了本章叙述中需要连续理解的一条线索，而不是彼此孤立的项目清单。

​        从这一章开始，AtlasAgent 已经具备跨会话沉淀稳定信息的基础。

## 40.14 下一章预告

​        第 41 章会实现记忆检索与上下文注入。

​        届时 Agent 会在执行任务前，根据当前任务选出少量相关长期记忆，并和短期上下文一起交给 Planner、ReAct 和最终 Agent Runner 使用。
