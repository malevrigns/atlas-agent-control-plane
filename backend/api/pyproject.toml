[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "atlas-agents-api"
version = "0.1.0"
description = "FastAPI backend service for AtlasAgent."
requires-python = ">=3.11"
dependencies = [
  "alembic>=1.16,<2.0",
  # SQLite 异步驱动：单机 / 零依赖模式下的数据库后端。
  "aiosqlite>=0.20,<1.0",
  "asyncpg>=0.30,<1.0",
  "beautifulsoup4>=4.12,<5.0",
  "fastapi>=0.115,<1.0",
  "httpx>=0.28.1",
  "pydantic-settings>=2.14.2,<3.0",
  "pypdf>=5,<7",
  "python-multipart>=0.0.31",
  "pyyaml>=6.0.3",
  "redis>=5.2,<6.0",
  "sqlalchemy[asyncio]>=2.0,<3.0",
  "starlette>=1.3.1",
  "uvicorn[standard]>=0.34,<1.0",
]

[tool.setuptools.packages.find]
where = ["."]
include = ["app*"]
