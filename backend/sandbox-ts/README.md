# AtlasAgent Sandbox (TypeScript)

Hono replacement for the Python sandbox. File jail, shell sessions, VNC status, and `/api/status` match the old contract.

```bash
pnpm install
pnpm test
pnpm dev
```
