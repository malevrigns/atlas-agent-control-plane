# AtlasAgent API (TypeScript)

Hono + Drizzle port of the FastAPI control plane. HTTP/SSE/auth contracts match `backend/api`.

```bash
pnpm install
pnpm test
pnpm dev
```

See [docs/TYPESCRIPT_RUNTIME.md](../../docs/TYPESCRIPT_RUNTIME.md) for the migration map and what is still Python.
